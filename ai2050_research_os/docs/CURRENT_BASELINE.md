# 当前研究基线：Phase 1210–1235

## 一、当前最准确的项目状态

截至 2026-08-11，可见材料支持以下基线：

1. 已知真值系统中，多种功能商、时钟和时序联盟相机能够被严格校准；
2. 这些仪器迁移到自由训练网络或预训练语言模型时，存在行为广度、干预语义、载体控制和形成条件等外部效度断点；
3. Qwen3 对若干受控对象—关系—值原子操作具有稳定行为，但统一母任务、家族门和答案边界单点补丁均出现限定失败；
4. Phase 1235 证明对象—marker 的候选路径、前缀树、固定句式内容和自然句式内容稳定，而严格短字符串合同高度依赖提示载体与模板；
5. 当前没有证据授权把这种行为稳定性命名为协议不变的隐藏内容状态；
6. 当前没有完成同一对象从行为到响应预测、必要性、救援、完整生成、组合、跨模型和训练形成的纵向闭环；
7. 现有数学足以表达当前边界，新数学升级门未打开。

## 二、已迁移的关键证据

| 编号 | 证据等级 | 核心结论 | 对当前框架的约束 |
|---|---|---|---|
| K190 | E3-Q 负边界 | 自然化 Qwen3 中整状态相机未通过 matched-null、carrier 和高阶幅度门 | 干预算子必须先校准语义与特异性 |
| K191 | E3-KT | 七个状态快照不能唯一确定第八个精确语义目标 | 先证明目标可识别，允许拒答 |
| K192 | E3-KT | 功能商比精确全状态目标需要更少的类型化观察 | 候选状态应定义为相对功能查询的等价类 |
| K193 | E3-KT | 六类形成时钟在脚本化已知真值中可恢复 | 测量构念可校准，不等于自由形成规律 |
| K194 | E3 负边界 | 形成时钟迁移到自由网络未在确认集通过行为广度 | 形成过程高度条件化 |
| K195 | E3 负边界 | 稠密采样增加前缀数量，但未解决跨架构起点广度 | 数据更多不等于目标可预测 |
| K196 | E3 负边界 | 当前形成前摘要未超过设计基线和 seed 置换 | 关闭当前摘要的强预测版本 |
| K197 | E3 负边界 | 对象—关系—值统一母任务行为总门失败 | 禁止用过宽总门授权统一物理扫描 |
| K198 | E2 负边界 | 操作轨通过但家族轨失败，错误有程序指纹 | 原子操作必须独立授权 |
| K199 | E3 行为 | 五个 Qwen3 原子作用域获得窄行为授权 | 仅授权对应对象的物理测量 |
| K200 | E3 负边界 | 五个作用域均未通过答案边界单点因果交接 | 反对单点 whole-residual 充分性强版本 |
| K203 | E3-KT | 固定计算布局下三类时序联盟功能商相机通过 | 仪器可行，但自然模型外部效度仍为零 |
| K210 | E3 类型边界 | 候选与句式内容稳定，严格短字符串不稳定 | 内容、格式、协议和解码必须分账 |

这是“关键证据子集”，不是 K1–K210 的替代总账。当前本地快照没有完整给出 K201–K202、K204–K209 的正式原文；本框架不补写或猜测缺失定义，后续迁移时应链接原始条目。

## 三、当前核心瓶颈

\[
\boxed{
\text{尚未形成一个可预测未知干预的全局响应张量，}
\text{也未识别能压缩该张量的功能状态和转移结构。}
}
\]

直接执行旧 Phase 1236 的问题在于：它受 Phase 1235 异质总门阻断，同时仍然只围绕一个先验对象采集隐态。新战役应把下一步改写为：

1. 用全新材料冻结内容、格式、协议和解码的类型化资格；
2. 同时注册九种候选机制和零假说；
3. 先做全域低成本轮廓和小型响应张量试采集；
4. 由结构算法生成候选；
5. 主动选择候选分歧最大的干预；
6. 最后才在封存集裁决。

## 四、当前禁止事项

- 不从 Phase 1235 的成功子集直接扫描 hidden state（隐藏状态）；
- 不把 `Marker: gold` 之类内容正确、格式错误的输出事后改成严格短答成功；
- 不继续围绕某个单层、单通道或单补丁做连续相邻 Phase；
- 不把已知真值相机成功外推为自然语言机制；
- 不把跨层相似、线性可读或高余弦写成自然因果使用；
- 不因当前数学骨架很长而声称需要新基础数学。

## 五、WP00 完成状态与当前唯一优先决策

WP00 已完成以下工程闭合：22 个 GPT/GLM 文档 Git blob 快照、复合 Phase `record_id`、四种类型化构念、对象/合同/运行/产物/勘误账本、24 GPU-hours 战役预算、2 GPU-hours 下一合同预算、严格合同 Schema 和五文件冻结 manifest。

WP00 没有运行模型，也没有产生新的神经机制结论。当前唯一优先决策是执行 `EXP-C001-WP01-001` 的无模型预审计。只有代码、材料、环境和独立结果审计器完成并追加进新 manifest，`run_ready` 才能从 `false` 改为 `true`；在此之前不得运行 Qwen3，更不得进入 TB03/TB04。
