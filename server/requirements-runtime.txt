# Runtime dependencies imported by server/server.py and the vendored TransformerLens package.
# Torch, NumPy, SciPy, Transformers, Accelerate, Safetensors and SentencePiece
# are kept in the project environment because their builds are CUDA/Python specific.
einops>=0.6.0
beartype>=0.14.1
better-abc>=0.0.3
fancy-einsum>=0.0.3
jaxtyping>=0.2.11
typeguard>=4.2
transformers-stream-generator>=0.0.5
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
scikit-learn>=1.5
pandas>=2.2
datasets>=2.7.1
wandb>=0.13.5
