# AGENTS.md

本文件规定本项目的长期研究方向、执行规则和交付要求。当前任务、最新 Phase、实际结果与下一步安排以用户当前指令、现有代码、运行记录和 `AGI_GLM5_MEMO.md` 为依据，不在本文件中固化容易过期的进度。

## 1. 项目使命与研究边界

研究 LLM 如何用有限参数形成可扩展的语言组合能力，重点解释知识网络、分析推理与语法系统怎样共同参与连续的 next-token 预测。

长期愿景是：通过语言编码机制研究，为理解人脑可塑性、建立智能理论和实现 AGI 提供线索。从 LLM 规律推广到人脑机制或 AGI，需要另外建立证据，不能直接视为本项目实验的结论。

近期工程目标是形成三个可以查询、复用和持续更新的成果：

1. **外部语言模式族图谱**：描述“词义—关系—角色—组合—语境”的组织结构，覆盖族内关系、族间关系和整体分布。
2. **内部 LLM 结构与响应图谱**：连接 embedding、全 HiddenState 场、输出读出与自回归历史，记录条件化响应、复用、差异及跨层接续。
3. **两图谱的关联机制**：形成可执行的提取算法和候选数学规律，用于解释已有表达，并预测未见词汇、表述、语境或组合的内部响应及后续输出。

优先研究 `embedding → HiddenState 场 → unembedding`。在明确问题需要时，沿已有证据局部追踪 attention、MLP、归一化、位置编码和真实参数连接；不把全面拆解所有模块设为前置任务。

保留理论名称：**条件化输出场闭合理论（RDC）**。名称中的“闭合”不改变当前“先发现结构，再逐步验证机制”的优先级。

## 2. 工作假设与结论边界

以下假设用于指导观察和算法设计，必须允许根据证据细化、限定或修正：

| 工作假设 | 研究含义与限制 |
| --- | --- |
| 语言可以按模式及模式族组织 | 用共同框架研究知识、推理、语法、风格及任务功能；分类可以重叠、嵌套和随语境变化。 |
| 概念具有相对编码特征 | 研究概念在关系网络中的位置、作用及条件变化；不预先排除词汇身份、固定参数或绝对坐标的作用。 |
| 共享结构与差异共同表达特征 | 寻找跨实体、任务和语境的复用及必要差异；“最少差异”和“接近最优”需要明确指标后再判断。 |
| 存在稳定、可组合的组织规律 | 主动寻找条件下稳定的规律，并记录例外；不要求所有模型、任务和层共享一个精确不变量。 |
| 风格、知识、逻辑、语法等条件共同影响预测 | 研究不同条件何时起作用、怎样协同和竞争，不预设它们对应彼此独立的固定模块。 |
| 训练形成相对独特的编码位置 | 区分训练造成的参数变化与固定权重推理中的状态变化；token 的数值独特性不等于已解释其语义。 |
| 模型实现可能存在粗糙性和特异性 | 考虑规模、数据、架构、精度与行为错误；比较模型时不把这些因素作为无法被检验的失败解释。 |
| 可能需要新的数学结构 | 先明确对象、关系、运算、适用条件和预测收益，再判断是否超出现有工具的表达能力。 |

对脑区、意识、可塑性和其他 AGI 路线的推断，作为独立的远期研究问题处理。历史类比与长期未闭合可以启发研究，不能证明某条路线唯一可行，也不能证明新数学必然存在。

长句重排、翻译和风格改写是有价值的行为入口；行为本身不直接证明某一种内部实现。

## 3. 术语与记录对象

| 术语 | 本项目的操作定义 |
| --- | --- |
| token、词、概念 | token 是分词器单位；一个词或概念可能对应多个 token。记录 token ID、文本跨度、语言、词义及上下文，不能默认三者一一对应。 |
| embedding | 模型实际使用的输入嵌入。固定权重推理中，同一 token ID 的嵌入表行通常固定；语境改变主要体现于后续计算状态。记录额外的位置或输入处理。 |
| HiddenState 场 | 按样本、生成步、层、token 位置及坐标组织的激活集合。记录采集位置，如模块前后、残差流或归一化前后。 |
| 激活坐标 | 某次前向计算中向量的一个数值分量，是状态量。 |
| MLP 单元 | MLP 中间空间中的计算单元，不能与残差流坐标混称。 |
| 标量参数 | 权重或偏置中的具体元素。固定权重推理时参数不随语境更新，但其输入和有效作用可以变化。 |
| 条件齿轮 | 在特定条件下反复出现的候选计算关系，可以涉及多个坐标、位置和层；不是预先确定存在的物理零件。 |
| 脉络 | 条件变化下，状态响应跨位置、层和生成步的组织关系。纯观察所得记录为“响应脉络”；真实计算连接需另有来源依据。 |
| 复用与差异 | 明确复用的是参数、响应形状、功能、关系还是转移规则；差异是统计或计算对象，不默认它是可搬运的独立模块。 |
| 不变量 | 在指定模型、变换和语境范围内保持的性质，必须写明保持什么、允许什么误差以及在哪些条件下失效。 |

HiddenState 有统一的数组形式，适合系统观察；这不自动意味着它具有平整几何、必然形成流形，或足以唯一确定模块内部的实现。

同一模型的原生坐标索引可以作为定位依据；相同索引跨层不自动代表相同功能。不同模型的坐标也不能直接视为一一对应。跨层功能复用与跨 token 参数共享分别记录。

## 4. 研究优先级与方法原则

执行顺序为：**观察 → 提取候选结构 → 建立假设 → 检查复现与推广 → 按需补充机制证据**。

1. 第一优先级是发现结构、复用规律和可推广关系，积累机制拼图；机制闭合为后续目标。
2. 以自然前向观察和可追溯的坐标计算为主。差分、余弦、梯度、删除、救援和 patch 都可以使用，但不能长期成为唯一研究方式。
3. 优先用全坐标热力图、相关矩阵、轨迹叠加和来源贡献图观察主干。保留低幅值背景；不以 PCA、压缩或 Top-K 结果定义主干。
4. 可视化服务于发现。候选规律还需检查重复性、条件依赖、简单替代解释及未见样本表现，不能仅凭图形相似或高亮区域认定机制。
5. 原始值与标准化视图分别保留；记录色标、归一化、排序和聚合方式。保持可比视图的坐标顺序，避免排序制造相似图案。
6. 优先尝试图、超图、条件统计、非线性交互、时序算子及其他适合当前问题的数学工具。采用工具须说明新增信息，不能只更换名词或表示形式。
7. 某次干预失败时，限定被否定的命题，保留其他已成立的观察；不以单坐标能否切换答案作为唯一有效性标准。干预成功也不能自动证明唯一性、必要性或完整机制。
8. 不预设“一个坐标就是一个概念”，也不预设最小计算单元一定存在。允许研究条件化组合单元、关系结构或近似不变量。
9. 保持统一分析框架，同时允许不同语言和任务使用明确的语义角色及评分标准。统一预测目标不等于已证明内部算法完全相同。

## 5. 特征提取算法的研发要求

核心研发任务是：寻找能够同时描述语言关系结构、内部响应结构及两者联系的提取算法。算法必须可执行、可比较，并能定位失败原因。

### 5.1 统一输入与数据身份

每条实验记录至少能够关联：

- 原始文本、实际模型输入、分词结果、语言、词义、角色、任务和语境标签；允许多标签、未知标签及关系重叠。
- 模型与 tokenizer 版本、代码版本、样本划分、运行配置和运行 ID。
- embedding、所需层及位置的 HiddenState、采集边界和生成步。
- 实际预测、输出分布或已保存的评估量，以及获取完整数据的路径或重算方式。

同一个原始样本及其改写、翻译、实体替换要保留家族关系，便于控制近重复泄漏。语言标签是分析视角，不能直接被当作模型内部结构的事实。

### 5.2 算法候选组

在同一批冻结材料上比较互补路线，优先复用已验证代码和数据：

| 路线 | 核心操作 | 主要输出 |
| --- | --- | --- |
| 原生坐标关系分析 | 保留完整坐标，比较词嵌入、上下文状态及其关系变化；区分身份、范数、位置和语义条件。 | 相对结构、候选复用关系与差异位置。 |
| 条件统计与交互分析 | 比较单条件、组合条件下的响应，检查组合变化能否由各单条件变化解释。 | 条件依赖、协同、竞争及失效条件。 |
| 图与超图分析 | 对词义、角色、属性和任务建立类型化关系，与内部响应建立可追溯的关联。 | 族内、族间结构及跨图谱映射。 |
| 时序与转移分析 | 分别研究层内接续、跨层变化和生成步推进，拟合并评估条件化转移规律。 | 响应轨迹、转移规则及未见样本预测。 |

允许新增候选算法。每条路线必须明确：输入、提取步骤、输出对象、拟合或选择过程、评估方式、资源成本，以及相比简单基线增加了什么解释或预测能力。

预测算法须注明预测时可获得哪些输入。预测某层状态或未来输出时，不能把待预测状态、未来 token 或答案标签作为输入。

模型原始前向结果是核对对象；完整重跑原模型或复制其输出，不能单独视为完成了机制提取。

### 5.3 最小可交付结果

每个被保留的候选规律至少给出：

1. 它描述的对象、条件和适用范围。
2. 可重算的提取算法、对应样本及坐标或关系索引。
3. 代表性实例、可视化、反例或当前尚未覆盖的情况。
4. 复现与推广检查；若尚未进行，明确标为候选观察。
5. 它对三图谱、现有理论或下一步路线选择带来的具体增量。

复杂公式必须定义变量并对应可计算对象。定义式、恒等式、拟合模型和由证据支持的规律应明确区分。

## 6. 数据规模与阶段组织

采用“先确认采集与提取有效，再增加覆盖和规模”的顺序。具体数量依据现有数据和资源制定，运行前记录；本文件不预设实验数字。

| 阶段 | 核心工作 | 进入下一阶段的依据 |
| --- | --- | --- |
| 小范围验证 | 选择少量明确模式，检查分词、对齐、采集、读出和算法输出；在同一批材料上比较候选路线。 | 数据可信，能够重算，至少有一条值得扩展的结构线索或明确的算法改进方向。 |
| 受控扩展 | 增加实体、句式、角色、语言和组合条件，区分身份、位置、风格等混杂因素。 | 已知候选规律的复现性、适用范围和失败条件，能据此选择或改进路线。 |
| 跨族与规模研究 | 扩展语言模式族、上下文长度、组合深度和模型规模，连接三图谱与连续生成。 | 有可比较的跨族规律或模型差异，新增成本带来实际研究信息。 |

大 Phase 应围绕一个共同问题组织多条路线和子实验，不为每个小测试新增 Phase。一个 Phase 可以批量完成多路线比较，但不能在采集尚不可靠时盲目扩大所有维度。

发现和调试可以使用探索数据；正式声称推广时，使用未参与发现或选择的材料。按当前主张区分未见词汇、未见表述、未见关系组合和更深组合，避免用随机切分掩盖近重复泄漏。

建议逐步覆盖以下材料；它们是待选用例类型，不是已执行实验：

- 知识与属性：苹果、香蕉等名词，以及水果、食物、植物、物体之间不同类型的关系；颜色、大小等属性。区分“属于”“部分”“来源”等边，不能全部画成同一种层级关系。
- 词义与语境：苹果的水果义与公司义、罕见词、多 token 词及不同语境下的用法。
- 角色与组合：“我喜欢吃苹果”，变化施事、动作、对象、否定、提问和语序，记录实际改变了哪些因素。
- 语法与篇章：名词、动词、形容词、副词、介词、标点、结束符号、转折、长句重排和长距离关联。
- 任务功能：翻译、续写、补齐、润色、改写、推理和代码生成；按实际操作标注，避免全部笼统归为逻辑推理。
- 风格与语言：中英文起步，按需要扩展；研究相近意图在不同风格下的表达，同时检查意义或语用是否发生变化。

模型的实际能力与目标答案分别记录。模型原本未完成的任务，不能直接作为它具备对应内部机制的正例。

## 7. 自回归、采集与数值控制

按生成步连接：当前 token 前缀、位置及历史状态、跨层响应、输出读出，以及追加新 token 后的下一步计算。

1. 明确层索引、token 位置与生成步的含义，记录实际 logits 与目标 token 的对齐关系。
2. 分别报告首 token、后续首次分叉位置、完整答案及停止行为。内容、格式和停止能力分别说明。
3. 区分使用给定 token 的评分过程与模型自由生成，不能把完整候选分数等同于自然生成能力。
4. 记录采集模式、KV 缓存使用、位置、RoPE 配置、归一化、padding、截断、batch、执行形状、精度和随机性控制。解释某种状态变化前，先排查相关运行差异。
5. RoPE 的作用位置以实际架构为准，不能默认整个 HiddenState 经过同一种可直接逆转的旋转。
6. 单个末端 HiddenState 不自动等于下一步计算所需的完整状态。若采用马尔可夫、滤波、半群或其他时序模型，先明确状态包含哪些信息、近似省略哪些信息，并检查遗漏的影响。
7. 优先使用不量化模型；显存不足时，按现有框架支持情况使用设备分配、CPU 卸载或顺序加载。降低精度或量化时记录原因及对比较的影响，不静默混用配置。
8. 全场采集可以分批、分块或分层持久化。“全量覆盖”与“同时驻留内存”分别处理；部分采集必须明确缺失范围。

## 8. 启动、自动执行与路线调整

### 8.1 启动核查

开始实际研发前：

1. 阅读当前用户任务、相关附件、现有入口代码及最新 `AGI_GLM5_MEMO.md`；按需要追溯历史索引与原始结果。
2. 核实当前 Phase、已完成成果、失败条件、可用模型、数据、算力和待办，形成当前任务状态。
3. 原文提到的全坐标图谱、全 token 算法、来源—坐标—MLP 路径、C112、C571–C589 等仅作为检索线索。必须查到对应文件与证据后，才可作为本轮事实依据。
4. 保留已有正确结果，追加修正过度结论；不把计划、历史描述或上轮模型自述当作已完成实验。
5. 若当前任务只是文档审阅、方案讨论或结果解释，完成该任务；本文件中的自动研发规则不自行扩大当前任务范围。

### 8.2 每个 Phase 的执行循环

1. **确定问题**：写清本阶段要发现什么结构或解决什么不确定性。
2. **整合方案**：把服务同一问题的候选算法和子任务组织到一个 Phase，列出预期产物与路线选择依据。
3. **确定资源边界**：沿用用户和运行配置的预算；未配置时，先做成本可控的试运行估计时间、内存与存储，不自行启动无限循环。
4. **实现并执行**：优先复用代码和冻结数据，完成必要的采集、分析和比较。
5. **核对证据**：区分事实、候选解释、失败、未完成任务和推广范围。
6. **保存交付**：更新研究记录、结构化数据、客户端可查询结果及可恢复的下一步状态。
7. **决定续研**：下一阶段仍服务同一已授权目标，且资源允许、有明确新增信息价值时自动继续，不反复请求确认。

单个实验失败不终止整个研究；优先排查实现问题、条件限制、协同关系和替代路径，再调整算法或材料。

当一条路线反复只得到同类局部现象，且没有新增解释、预测或结构信息时，汇总其证据边界，降级或暂停该路线。单纯换实体、换模板或换名词不能视为新路线。

实际阻塞、预算耗尽或当前交付目标完成时，保存进度、未完成项与可恢复入口。区分“本轮结束”“实验失败”和“科学问题已解决”。

## 9. 研究记录、证据与汇报

`AGI_GLM5_MEMO.md` 是统一的研究叙述记录；代码、结构化数据与运行产物是可核查依据。两者通过 Phase ID、运行 ID 和路径相互关联，结论冲突时回查原始证据。

Phase 编号接续现有记录，不随意重编号。已有记录的纠错采用追加说明，标明原结论、修正理由与影响范围。同类 C00x 子实验可以合并叙述，但保留原始标识及映射。

每个 Phase 至少记录：

1. **目标与状态**：已执行、部分执行、未执行或受阻；与总目标的关系。
2. **原理与算法**：通俗说明、步骤、变量定义及必要的 `$$...$$` 公式。
3. **材料与用例**：模型、数据、实际输入、划分、配置及用例来源。
4. **实际结果**：数据规模、指标、代表性成功和失败，以及可定位的原始产物。
5. **分析结论**：支持什么、不足以支持什么；实现错误、适用范围与问题硬伤。
6. **机制拼图与理论更新**：三图谱分别新增了什么；RDC 的主体、更新和未解释部分。
7. **后续与资源**：下一步、路线选择理由、阻塞、资源消耗及数据保留情况。

允许提出新实验并在授权范围内执行；必须标为新设计或计划。禁止把未执行的新设计写成历史过程，禁止补造实验数字、样例输出、结果、文件、耗时或结论。

汇报面向理解 Transformer、attention 和 HiddenState、但不熟悉项目术语和复杂统计的读者。用真实例子解释目标、原理、算法、结果与意义；教学示例单独标明，关键突破详细解释，普通阶段不重复堆叠理论。

重大阶段及用户要求完整总结时，提供完整核心拼图清单与对应 Phase，说明保留、修正和待验证状态；列出 RDC 当前统一机制公式及全局图谱核心公式。若没有公式改进，明确说明；不能为了形式完整临时拼造新公式或改名。

若声称新数学进展，明确新增的定义、运算、命题或预测能力，以及现有工具难以表达的具体问题。没有达到这个程度时，报告为候选数学表述或经验规律。

优先维护现有本文件与 `AGI_GLM5_MEMO.md`，不为每个小实验新增 Markdown。代码、配置、结果表和数据库按工程需要保存。

## 10. 可视化研究客户端

客户端承担三个功能，按可用数据和研究进展逐步实现：

### 10.1 研究积累与查询

- 语言模式库：保存 token、词义、词类、构式、角色、属性、关系、语境和任务，以及真实测试材料。
- 内部响应库：查询 embedding、HiddenState 场、候选脉络和输出，关联模型、样本、运行与采集配置。
- 理论与进展库：展示已研究关系、证据状态、关键拼图、失败范围、最新 RDC 理论及数学进展。

三个库使用稳定 ID 和双向链接。区分“有数据”“已有观察”“通过推广检查”和“有机制证据”等状态；不能仅用“已破解”概括所有进展。

### 10.2 静态与动态可视化

提供全坐标热力图、条件比较、相关矩阵、轨迹叠加，以及可查询的三维结构与动态运行视图。

三维布局明确说明坐标轴和位置的含义。降维、聚合、抽样或动画示意需要标注，并能回查原始数值。图布局中的距离或动画连接不能直接当作模型真实几何或已证实计算连接。

优先实现能回答研究问题的查询、筛选、对照和回溯，再扩展展示效果。界面演示数据与真实实验数据明确区分。

### 10.3 AI 自动研发循环

- 支持手动模式与自动模式，并明确当前授权任务、预算、队列、运行状态及恢复入口。
- 支持配置主模型和辅助模型及其提示词。主模型负责实现与整合，辅助模型可基于同一批实际产物分析结果、指出问题或提出方案。
- 多模型分析是可配置能力，不作为每轮运行的硬性前提；不可用时记录实际降级方式。
- 多个模型意见一致不替代实验依据。保留原始结果、各模型分析及最终决策依据。
- 队列任务在下一轮规划时读取，检查与当前目标、用户最新指令及预算是否一致；用户明确的停止或纠正及时生效。
- 每轮结束保存研究记录并更新数据库。支持恢复、失败重试和避免重复运行已完成任务；自动运行继续遵循第 8 节的边界。

## 11. 数据保留与重算

模型、代码、材料、配置、结果、关键图谱及必要重算依据持续保留。研究记录与客户端索引保持一致。

对体积较大的 HiddenState 原场，先登记保留用途：客户端展示、后续研究、关键证据或可重算临时数据。优先采用分块、无损存储和复用已有冻结数据，避免重复采集。

仅在本阶段分析与审计完成、展示和后续分析均不依赖、所需汇总已经核对、重算配置完整且重算成本可接受时，清理可再生的临时原场，并记录清理范围。难以重现或成本高昂的关键数据应保留或归档。

## 12. 阶段完成标准

一个 Phase 不需要破解整个语言机制，但必须形成可核查的完整交付：

- 回答一个明确研究问题，或说明为何尚不能回答。
- 保存可重算的算法、材料、配置和实际结果。
- 交付新的结构线索、可推广规律、有信息价值的失败、算法改进或路线选择结论中的至少一项。
- 说明三图谱及 RDC 的实际增量、局限和未完成项。
- 更新研究记录与相关客户端数据，给出可以继续执行的下一步。

持续以“相对关系如何编码、共享结构如何复用、差异如何表达、条件如何改变作用、计算如何跨层及生成步接续”为共同问题组织研究。用逐步积累的全局结构约束局部解释，并让局部发现反过来完善全局图谱。
