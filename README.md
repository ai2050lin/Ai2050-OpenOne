# OpenOne / AI2050

OpenOne 是一个深度神经网络逆向工程研究项目，目标是破解语言能力在深度神经网络中的编码与运行机制，并寻找能够解释、预测和控制智能系统的基础计算原理。

项目尝试回答一个核心问题：

> 深度神经网络如何把对象、关系、规则和上下文组织成语言能力？这种能力背后是否存在可验证、可干预、可复现的数学结构？

仓库同时包含科研实验、证据管理、3D 可视化和 AI 自动研发工具。当前研究仍在推进中，尚未完成语言编码理论、智能统一理论或 AGI 系统。

## 项目大纲

### 核心猜想

以下内容是项目的研究方向，不是已经证明的结论：

1. 人脑具有极高可塑性。语言能力可能是神经网络被经验反复塑造后形成的系统能力，而这种可塑性可能是智能的重要来源。
2. 注意力、自回归训练和大规模语言材料，使深度神经网络部分恢复了支撑语义、逻辑、语法和生成控制的内部数学结构。
3. 如果能够恢复语言编码的可执行机制，就可能反推神经网络的塑造规律，为智能理论和可控制 AGI 架构提供基础。

本项目假设语言背后存在一套系统结构。在找到核心规律之前，局部相关、热点神经元和漂亮几何都可能只是投影或虚影；真正的突破应当像一个正确物理理论一样，能够统一解释已有结果，并对新材料和干预结果作出准确预测。

### 系统工作假设

1. **模式本体**：知识网络、分析推理和语法规则可以暂时统一为不同尺度、不同功能的模式。
2. **相对编码**：概念身份可能来自它在关系网络中的位置和作用，而不是孤立、固定的坐标。
3. **复用与差分**：可共享结构尽量复用，不能复用的部分通过较小差异表达独特特征。
4. **多尺度组织**：族内结构、族间关系和全局模式族分布可能共同平衡能力、容量与计算效率。
5. **动态生态位**：训练和上下文持续调整词的含义与用法，使每个模式形成条件化、相对独特的功能位置。
6. **多场竞争**：风格、逻辑、语法、知识和输出协议共同参与下一个 token 的候选竞争。
7. **实现粗糙度**：模型规模、训练数据和架构限制会产生噪声、捷径与模型特异实现，特别是小模型不能直接代表大模型或人脑。

这些假设相互关联，但必须分别接受实验检验。观察到模式复用不能证明结构最优；观察到条件化状态也不能直接证明人脑采用相同机制。

### 三级破解目标

#### 1. 具体模式

恢复特殊词、翻译、转折、标点、终止等模式从输入条件到输出选择的完整计算链：模式何时形成、由什么组件推进、怎样改变候选竞争。

#### 2. 模式族

分析水果、动物、颜色、翻译和语法等模式族内部及族间的复用、差分、抽象和条件化路由，寻找决定模式身份的最小差异。

#### 3. 整体网络

重建模式族在位置、层、Attention、MLP 和 residual 中的全局分布，解释网络如何同时获得表达能力、效率与稳定性，并寻找可跨任务复用的最小计算单元或不变量。

### 主要难点

1. 语言同时混合语义、语法、知识、风格和协议，任何测试都可能包含多个因素，很难获得只改变一个变量的纯净实验。
2. 现有数学工具通常只能描述局部投影，尚不能直接表达完整的条件化、分布式计算结构。
3. 编码可能同时包含相对关系、复用、差分、抽象与精确化，静态距离容易产生误导。
4. 规律跨越大量层、位置和组件；可读热点、强激活或高相似度不一定是模型实际使用的机制。
5. 紧凑结构、模型噪声和小模型粗糙度会产生大量虚影。当前仍缺少能够贯穿全网的最小计算单元或稳定不变量。

### 核心目标

找到一个算法或不变量，能够：

- 解释深度神经网络的运行机制；
- 预测内部状态和输出如何随条件变化；
- 对关键机制进行精确修改、恢复和控制；
- 在跨材料、跨任务和跨模型测试中保持有效；
- 为新的智能架构和 AGI 系统提供可执行理论。

## 当前研究进展

Phase 1237 已完成公开文档基线同步；当前实质研究基线是 **Phase 1236：C001-WP00 研究操作系统闭合**。其中最新科学结果是 Phase 1235 的 Qwen3 类型化生成边界；Phase 1236–1237 分别是工程基础设施和文档同步成果，不是新的神经机制发现。

项目已经从“寻找固定语义向量或单个概念神经元”，转向“先冻结纵向研究对象和同类型构念，再用未知世界、未知干预和完整生成轨迹竞争性辨识全局结构”。当前活动战役为 **C001：全局结构辨识战役**。

### 近期阶段推进

| 阶段 | 核心问题 | 主要结果 | 方法论推进 |
| --- | --- | --- | --- |
| Phase 1151-1188 | 已知真值相机能否辨认机制形态，并安全外推到自由网络 | 建立双路径、救援、反泄漏负控和类型化三证体系；自由网络三证通过其限定门，但形成前缀预测被反证 | 把“相机能识别”与“自然网络存在同一机制”分账 |
| Phase 1189-1200 | 功能结构如何形成，最小救援联盟和脉络临摹是否可识别 | 获得局部商空间转移、同父因果分叉、稀疏联盟与 embedding 主导救援等限定结果；长期实现身份和通用临摹仍不可识别 | 从单点热点转向响应等价类、联盟和时间制度 |
| Phase 1201-1212 | 如何把语义对象、证据和测试权限类型化 | 建立显式拒答编译、对象—属性母模式、语义干预编译器与功能目标商；固定跨材料语义桥仍未闭合 | 先定义功能目标商，再选择内部相机 |
| Phase 1213-1223 | 自由 Transformer 中哪些行为对象真正具备内部研究资格 | 五个 Qwen3 原子操作作用域获得行为授权；答案边界 whole-residual 单深度交接在 discovery 与 holdout 均失败 | 家族总门降级为原子门，单点充分性强版本关闭 |
| Phase 1224-1235 | 时序联盟、去答案载荷材料和多种读出是否支持统一内容状态 | 已知真值时序联盟相机通过；多轮反泄漏任务暴露捷径与接口混淆；Phase 1235 中候选评分、trie、固定句式和自然内容稳定，严格短串不稳定 | 内容、格式、自然生成、停止—缓存必须独立判定 |
| Phase 1236 / C001-WP00 | 如何让来源、对象、合同、运行和裁决可追溯且不能越级 | 23 个不可变来源、复合 Phase 身份、类型化注册表、严格 schema、冻结 manifest 和 18/18 无模型审计通过 | Phase 只作批次；纵向对象、构念和证据权限成为主轴 |

### 当前可以说什么

- 项目测试中的语言信息更符合上下文条件化、分布式展开和读出类型依赖的过程，而不是跨材料固定不变的执行向量。
- 候选内排序、全词表首 token、严格短串、固定句式、自然生成和停止属于不同数学事件，必须分别记账。
- Qwen3 的对象—marker 绑定在 Phase 1235 的完整候选评分、无候选序列评分、trie、固定句式内容和自然句式内容上稳定；这只是限定行为资格，不是 hidden 模块发现。
- 五个原子操作作用域曾获得独立行为授权，但答案边界单点 whole-residual 交接失败，说明行为稳定不推出单点可搬运状态。
- 已知真值相机可以恢复某些功能商、联盟和时间制度；其成功只证明测量工具在已知适用域可用，不证明真实 LLM 必然采用相同结构。
- 所有正式结论现在都能沿 `不可变来源 → Phase 记录 → 证据 → 对象/构念 → 合同 → 运行/产物 → 裁决` 追溯。

### 当前不能说什么

- 尚未找到跨模型、跨任务稳定成立的语言编码不变量、最小计算单元或完整相对脉络。
- 尚未证明存在协议不变的“内容状态”，也不能把候选排序稳定解释为一个独立内容模块。
- 可读、相关、可补丁和行为正确都不能单独证明必要性、充分性、特异性、未知预测与生成闭环。
- Phase 1236 的 18/18 审计只证明研究账本和冻结合同自洽，不是模型机制证据。
- 当前结果不能直接外推到其他模型或人脑，不能据此宣称语言数学结构或 AGI 理论已经破解。

详细进展以 [GLM5 研究备忘录](research/glm5/docs/AGI_GLM5_MEMO.md) 和 [C001 研究操作系统](research/ai2050_research_os/README.md) 的可校验账本为准。

## 当前理论框架

当前使用的工作名称是 **条件化输出场闭合理论**，组织原则是 **复用—差分—条件化**（Reuse-Difference-Conditioning, RDC），数学对象暂时描述为条件计算动力系统。

核心主张是：

1. 语义不预设为一个固定向量，而是在输入、上下文和组件作用下逐层展开的过程。
2. 输出不是单一开关，而是知识、逻辑、风格、格式、终止和候选答案等多个场的竞争结果。
3. 当前优先研究的机制对象不是某个静态状态，而是对象在一组冻结干预和读出下的未来响应等价类及其有向转移。
4. 内容选择、严格格式、自然生成和停止—缓存是不同构念；它们可以相互作用，但不能互相代替授权。
5. 复用和差分得到了一些局部观察支持，但其全局结构、形成规律和效率最优性仍是假说。

该框架尚未完成，不应被表述为已成立的统一理论。

## 测试原理与公式

### 四状态绑定效应

```text
I_bind = 1/2 [(m_orig,post - m_orig,pre)
              - (m_swap,post - m_swap,pre)]
```

该设计用于消除候选静态偏好、时间方向偏好和档案格式偏好，保留真正的“时间条件到实体选择”效应。

### 自然状态转移

```text
X_(l+1) = F_l(X_l; q, c, Θ)
```

其中 `X_l` 是第 `l` 层状态，`q` 是查询条件，`c` 是上下文，`Θ` 是模型参数。研究对象是状态如何变化，而不是仅测量某个静态向量的位置。

### 同路径干预响应

```text
ρ_l(α) = M(F_>l((1-α)X_l^a + αX_l^b))
```

在同一执行路径内，将条件 `a` 的状态向条件 `b` 插值，并观察候选 margin 或真实生成如何变化。

### 机制边闭合门

```text
E_(u→v) = I ∧ S ∧ N ∧ C ∧ P ∧ R
```

一条机制边必须同时满足：

- `I`：身份等价，no-op 或自写回不产生仪器漂移；
- `S`：充分性，状态移植能够选择性改变目标；
- `N`：必要性，消融会破坏对应能力；
- `C`：特异性，无关任务和负控制不被同样破坏；
- `P`：独立预测，在留出材料上预先预测结果；
- `R`：重复，在独立材料或模型上再次成立。

## 下一项关键任务

当前活动工作包是 **C001-WP01：相机与行为资格**。合同 `EXP-C001-WP01-001` 已预注册，但 `run_ready=false`；这表示问题和预算已冻结，不表示可以运行模型。

下一项任务仅限 **无模型预审计**：

```text
生成全新的非双射对象—标记世界与四个互斥数据房间
→ 验证答案载荷不进入输入
→ 验证同 token 袋绑定交换、目标改变、非目标空操作和查询切换碰撞
→ 计算常量、首/末记录、位置、无序值袋等替代程序上界
→ 冻结模板、解析器、代码、材料、环境和独立结果审计器
→ 复核四类构念门与 2 GPU-hours 硬预算
→ 全部通过后才把合同提升为 run_ready
```

任一预审计失败都必须停止。当前不得运行 Qwen3，不得自动转向 GLM4、DS7B，也不得采集 hidden、Attention、MLP 或 neuron。

## 研究纪律

1. **先行为，后内部**：行为门未通过时，内部观测不能解释为目标机制。
2. **先相关，后因果**：差分、余弦、probe 和热点只提供候选，不能替代干预。
3. **先仪器，后机制**：数值健康、no-op、自补丁和路径等价必须先通过。
4. **预注册与硬停止**：实验对象、阈值、分区和负控在运行前冻结；失败后不事后改门。
5. **独立确认**：discovery、confirmation 和留出预测使用不重叠材料。
6. **分账记录**：材料、行为、hidden 相关、组件因果和真实生成分别记录，不能越级升级。
7. **不可变来源**：证据必须绑定 Git commit、blob、内容哈希与复合 Phase 身份，活动文件路径本身不是历史证据。
8. **类型化授权**：一个构念通过只授权该构念；格式失败不能自动否定内容，内容成功也不能绕过生成门。

## 证据等级

客户端和研究记录统一采用以下证据链：

1. **研究对象冻结**：任务、材料、候选、分区、负控和哈希可审计。
2. **数值与仪器资格**：运行路径、精度、no-op 和自补丁满足身份等价。
3. **行为通过**：目标效应稳定超过候选先验、模板和 matched-null 负控。
4. **Hidden 重复**：层间状态事件在独立材料或模型中重复。
5. **组件与状态因果**：干预选择性改变目标 margin，并通过必要性和充分性检验。
6. **预测重复**：预注册结果在留出材料和模型上成立。
7. **生成闭合**：干预稳定改变真实生成输出，并可通过自然重算或状态恢复复原。

任何较低等级结果都不能自动升级为更高等级结论。

## 可视化客户端

客户端以 3D 主空间为核心，理论分析和自动研发作为独立工作界面。

### 3D 机制工作区

主空间保留 Layer 3D 模型和组件细节模型，围绕 Layer 提供三种视图：

- **机制观察**：查看真实前向传播、层内事件和候选竞争；
- **证据比较**：比较原始、反事实、干预和匹配负控制；
- **成果展示**：只显示达到声明证据等级的重复或因果结果。

底部机制工作区显示候选差值、反事实、干预结果和数据来源。缺少实验数据时必须显示“暂无数据”，不能用演示值补齐。

### 理论分析

- **项目大纲**：核心猜想、系统假设、三级破解目标、难点和证据边界；
- **语言分析**：模式本体、相对编码、复用差分、多尺度组织和研究图谱；
- **智能理论**：三条研究路线的理论总结、证据等级和可证伪任务；
- **模型与系统**：六阶段进展、测试公式、模型运行和系统状态。

### 自动研发

自动研发作为独立侧边栏运行：

```text
任务定义
→ 研究对象与仪器门检查
→ 协议和负控冻结
→ 单模型顺序运行
→ 辅助 AI 审阅
→ 主 AI 综合
→ 生成标准结果文件
→ 人工确认或进入下一轮
```

AI 可以提出假设、编写脚本和整理证据，但不能自动把探索性结果升级为正式机制结论。

## 快速启动

### 前端

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\start_visualization.ps1
```

默认地址：`http://localhost:5173`

人工标注工作台：`http://localhost:5173/annotation.html`

启动器会从 PATH、`AI2050_NODE_HOME`、本机 WorkBuddy 和 Codex 运行时中寻找满足 Vite 7 要求的 Node.js，并自动补全本次进程的 PATH。首次安装依赖时使用 `-Install`：

```powershell
.\scripts\start_visualization.ps1 -Install
```

### 后端

首次使用或环境不完整时，先把后端运行依赖安装到仓库 `.venv`：

```powershell
.\.venv\Scripts\python.exe .\server\install_deps.py
```

该安装器优先使用 `uv`，兼容没有内置 pip 的 uv 虚拟环境。随后启动后端：

```powershell
.\.venv\Scripts\python.exe -m server.server
```

默认接口地址：`http://localhost:5001`

如果重复启动后端，启动器会识别已经运行的 AI2050 服务并直接复用，不再抛出
`WinError 10048`。如果 5001 被其他程序占用，请先查看占用者：

```powershell
Get-NetTCPConnection -LocalPort 5001 -State Listen
```

也可以让后端和前端同时改用另一个端口，例如 5002：

```powershell
$env:AI2050_PORT = '5002'
$env:VITE_API_BASE = 'http://localhost:5002'
$env:VITE_ANALYSIS_API_BASE = 'http://localhost:5002'
```

只验证 API 和可视化、不加载本地模型时：

```powershell
$env:AI2050_SKIP_MODEL_LOAD = '1'
.\.venv\Scripts\python.exe -m server.server
```

默认关闭 Uvicorn 自动重载，避免 VS Code 调试器脱离到子进程。确实需要开发期热重载时显式设置：

```powershell
$env:AI2050_RELOAD = '1'
.\.venv\Scripts\python.exe -m server.server
```

前端可以在后端不可用时读取静态证据；实时模型分析、自动研发和配置测试需要后端连接。

### 构建与检查

```bash
cd frontend
npm run lint
npm run build
```

## 本地模型测试规则

正式模型测试使用本地 CUDA 模型，并依次运行和释放，避免同时占用显存：

1. Qwen3；
2. GLM4；
3. DS7B。

重要结果必须扩大数据量，并在独立材料、留出分区或其他模型上再次确认。

## 标准结果格式

新的测试脚本应直接生成客户端可读取的结构化结果：

```text
manifest.json       实验身份、Phase、模型、协议版本和文件哈希
samples.jsonl       输入样本、分区、目标、候选和负控
metrics.json        行为指标、仪器指标、证据门和失败原因
events.jsonl        层、位置、组件和 hidden 事件
causal_graph.json   干预节点、机制边、必要性、充分性和恢复结果
report.md           原理、公式、结果、限制与结论
```

最小 `manifest.json` 示例：

```json
{
  "schema_version": "mechanism_case.v1",
  "case_id": "temporal-binding-sequence-alignment-qwen3-4b",
  "phase": 1140,
  "experiment_id": "temporal_binding_sequence_alignment",
  "model": "qwen3-4b",
  "evidence_level": "causal_candidate",
  "status": "exploratory",
  "protocol_frozen": true,
  "instrument_identity_passed": true,
  "sequence_scope": "candidate_prediction_span",
  "sample": {
    "prompt": "registered temporal binding sample"
  },
  "events": [],
  "artifacts": {
    "samples": "samples.jsonl",
    "metrics": "metrics.json",
    "events": "events.jsonl",
    "causal_graph": "causal_graph.json"
  }
}
```

客户端不得根据文件名猜测证据等级，必须读取 manifest 中的明确字段。

`MechanismCase` JSON Schema 位于 [shared/schemas/mechanism_case.schema.json](shared/schemas/mechanism_case.schema.json)。客户端兼容旧图谱和真实 Trace 数据，也可以直接加载 `mechanism_case.v1` 文件。

## 目录结构

```text
frontend/                         可视化客户端与人工标注工作台
server/                           FastAPI 后端与运行接口
research/IntelligentTheory.md     智能理论总整理
research/gpt5/docs/               GPT5 研究记录
research/glm5/docs/               GLM5 研究记录
research/MainAnalysis/            主分析与研究总结
research/ai2050_research_os/      C001 注册表、冻结合同、清单、校验器与生成看板
tests/glm5/                       GLM5 正式测试脚本
tests/glm5_temp/                  GLM5 临时脚本
tests/glm5/result/                GLM5 测试结果
tests/gpt5/                       GPT5 测试脚本与结果
models/                           本地模型配置或权重入口
```

## 研究入口

- [当前研究总结](research/MainAnalysis/20260806_01_当前研究总结.md)
- [当前智能理论](research/IntelligentTheory.md)
- [GLM5 研究备忘录](research/glm5/docs/AGI_GLM5_MEMO.md)
- [C001 研究操作系统](research/ai2050_research_os/README.md)
- [GPT5 研究备忘录](research/gpt5/docs/AGI_GPT5_MEMO.md)
- [主分析文档](research/MainAnalysis/)

## 谨慎结论

项目已经积累了候选竞争、条件化内容、复用拓扑、动态路由、类型化行为门、已知真值相机、联盟救援和多读出边界等拼图，并建立了不可变证据链、冻结合同和机器可拒绝的权限系统。

但目前仍没有找到跨模型稳定成立的完整语言机制，没有证明协议不变内容状态，没有完成真实生成闭环，也没有完成智能统一理论。最新进展的核心不是“又找到一个内部开关”，而是把研究对象升级为可长期追踪的功能对象和未来响应，并确保任何局部正结果不能越级冒充全局机制。
