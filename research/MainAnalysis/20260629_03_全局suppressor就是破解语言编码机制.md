提问：
自回归机制，本来就会导致整个神经网络形成某种多维度的路线和脉络，比如  语义，逻辑，语法，     suppressor 就是这种机制的体现，如果能找到全局的suppressor结构，也意味这破解了语言的编码结构，   以上分析是否正确，应该如何改进     


回答：
# 总体判断

你的分析**大方向正确**，而且很接近当前研究的核心转折点：

```text
自回归训练机制会迫使整个神经网络形成多维度路线和脉络；
这些路线包括语义路线、语法路线、格式路线、逻辑路线、回声路线、解释路线、短答路线等；
生成时不是某一条路线单独输出，而是多条路线在读出端竞争；
suppressor（抑制器）就是控制“哪些路线不该赢”的机制。
```

但是需要收紧一处：

```text
找到全局 suppressor 结构，
不等于单独破解全部语言编码结构；
但会破解语言编码结构中极关键的一半：
路线选择、竞争压制、生成闭合机制。
```

完整语言编码至少包含两类机制：

```text
1. constructive mechanism（构造机制）：
   如何把 source token（源词元）、语义、关系、上下文、值信息写入内部状态。

2. selective mechanism（选择机制）：
   如何让正确路线赢，错误路线、格式路线、回声路线、散文路线、受体路线被压制。
```

suppressor（抑制器）主要属于第二类。它不是全部编码，但它可能是**从内部表征走向可控输出的关键门控结构**。

---

# 一、为什么自回归机制会形成多维路线？

自回归训练目标是：

[
\mathcal{L}
===========

-\log P_\theta(x_{t+1}\mid x_{\leq t})
]

模型只看已有序列：

[
x_{\leq t}
]

然后预测下一个 token（词元）：

[
x_{t+1}
]

这迫使网络学会很多不同类型的条件结构：

```text
语义：
  当前应该输出什么概念、对象、值。

语法：
  当前应该接名词、动词、标点、从句还是结束。

逻辑：
  当前前提推出哪个结论。

格式：
  当前是在 JSON、列表、短答、解释、代码还是翻译模式。

语用：
  当前用户要求直接回答、解释、比较、推理还是拒答。

续写：
  token0 之后应该继续答案，还是进入散文、格式或停止。
```

这些都被统一压缩成一个任务：

[
P(x_{t+1}\mid x_{\leq t})
]

所以自回归训练会自然形成多条路线：

[
\mathcal{R}
===========

{
R_{\text{semantic}},
R_{\text{syntax}},
R_{\text{logic}},
R_{\text{format}},
R_{\text{prose}},
R_{\text{echo}},
R_{\text{answer}},
R_{\text{continuation}}
}
]

其中每条路线都有自己的候选 token 集合和内部状态支持。

---

# 二、suppressor 到底是什么？

suppressor（抑制器）不是一定对应某个“抑制神经元”。在人工神经网络里，它更准确是一个**功能角色**：

```text
某个组件、方向、通道、head、MLP neuron 或残差子空间，
能够降低某类不该赢的竞争路线的读出分数，
从而提高目标路线胜出的概率。
```

它可以有多种实现：

```text
1. 直接压低竞争 token 的 logit。
2. 改变 hidden state，使它远离竞争 token 的 unembedding 方向。
3. 抑制 format / prose / echo route 的上游状态。
4. 增强目标答案，同时相对降低竞争者。
5. 改变 continuation state，使模型不进入错误续写路线。
```

如果目标答案是 (y^*)，竞争路线集合为 (\mathcal{C})，生成闭合要求：

[
\ell(y^*)
---------

\max_{c\in\mathcal{C}}
\ell(c)

>

0
]

其中：

[
\ell(y)
=======

W_U(y)^\top h_{\text{final}}
]

suppressor 的作用就是降低：

[
\max_{c\in\mathcal{C}}\ell(c)
]

或者更具体：

[
Suppress(R)
===========

*

\Delta
\max_{c\in R}
\ell(c)
]

其中 (R) 是某一类竞争路线，例如：

```text
recipient route（受体答案路线）
format route（格式路线）
echo route（回声路线）
prose route（散文路线）
punctuation route（标点路线）
wrong value route（错误值路线）
```

---

# 三、你的判断哪里正确？

## 1. 自回归训练确实会产生路线图谱

因为所有 token 都在同一个词表中竞争：

[
P(y|x_{\leq t})
===============

\operatorname{softmax}(W_U h_t)
]

所以模型必须学会：

```text
什么时候输出语义值；
什么时候输出标点；
什么时候输出语法连接词；
什么时候输出解释前缀；
什么时候停止；
什么时候继续。
```

这自然形成多路线系统。

例如同一个事实：

```text
apple.category = fruit
```

可能产生不同路线：

```text
短答路线：
  fruit

散文路线：
  An apple is a fruit.

格式路线：
  "category": "fruit"

回声路线：
  apple.category

解释路线：
  The category of apple is fruit because...
```

这些路线不是外部人为分类，而是自回归预测中真实竞争的生成吸引子。

---

## 2. suppressor 是路线竞争机制的体现

Phase 743-745 已经显示：

```text
只增强 donor answer 不够；
压掉当前 top competitor 也不够；
必须压制 route-level competitors。
```

这说明：

```text
suppressor 不是边缘机制；
它是生成选择的核心机制。
```

没有 suppressor，就会出现：

```text
正确答案已经被增强，
但 format route 接管；
或者 echo route 接管；
或者 recipient answer 继续占优；
或者 prose prefix 先输出。
```

这正是当前苹果—水果—属性实验中看到的现象。

---

## 3. 如果能找到全局 suppressor 结构，会非常接近语言编码机制

这一点基本正确。

因为语言生成不是单纯：

```text
把正确 token 推高。
```

而是：

```text
让正确 token 赢。
```

“赢”意味着：

[
\ell(y_{\text{target}})

>

\ell(y_{\text{recipient}})
]

[
\ell(y_{\text{target}})

>

\ell(y_{\text{format}})
]

[
\ell(y_{\text{target}})

>

\ell(y_{\text{echo}})
]

[
\ell(y_{\text{target}})

>

\ell(y_{\text{prose}})
]

所以如果找到全局 suppressor 结构，就等于找到了：

```text
语言系统如何选择当前该输出哪种路线，
以及如何排除其他路线。
```

这是语言编码结构中非常关键的部分。

---

# 四、你的判断需要怎么修正？

核心修正是：

```text
suppressor 不是全部编码机制；
suppressor 是生成闭合和路线选择机制。
```

完整语言编码至少需要：

```text
source：
  信息从哪里来。

writer：
  哪些组件把源信息写入状态。

rewriter：
  哪些 MLP / residual 组件把信息重写为读出可用形式。

booster：
  哪些组件增强目标路线。

suppressor：
  哪些组件压制非目标路线。

readout：
  目标和竞争者如何在词表空间比较。

continuation：
  token0 后是否继续正确路线。

closure：
  生成是否完整闭合。
```

所以更完整的结构是：

[
LanguageEncoding
================

Source
+
Writer
+
Rewriter
+
Booster
+
Suppressor
+
Readout
+
Continuation
+
Closure
]

suppressor 是其中非常关键的一环，但不是唯一一环。

---

# 五、全局 suppressor 结构应该怎么定义？

不能只定义成“压低某个 token 的组件”。应该定义成**路线级抑制场**。

设路线集合为：

[
\mathcal{R}
===========

{
R_1,R_2,\dots,R_n
}
]

每条路线 (R_i) 有一组 token：

[
V_{R_i}
\subset
\mathcal{V}
]

该路线的读出分数定义为：

[
S_{R_i}(h)
==========

\max_{y\in V_{R_i}}
W_U(y)^\top h
]

目标路线为 (R^*)，闭合要求：

[
S_{R^*}(h)
----------

\max_{R\neq R^*}
S_R(h)

>

0
]

suppressor 对某路线 (R) 的作用可以定义为：

[
Suppress_u(R)
=============

*

\Delta_u S_R(h)
]

如果某组件 (u) 让路线 (R) 的分数下降，则：

[
Suppress_u(R)>0
]

目标路线的增强是：

[
Boost_u(R^*)
============

\Delta_u S_{R^*}(h)
]

组件对闭合的总贡献是：

[
Closure_u
=========

Boost_u(R^*)
+
\sum_{R\neq R^*}
w_R Suppress_u(R)
]

其中 (w_R) 表示竞争路线的重要性。

---

# 六、全局 suppressor 图谱应该是什么样？

它应该是一个矩阵，而不是一个单点列表。

定义 suppressor matrix（抑制矩阵）：

[
M_{u,R}
=======

Suppress_u(R)
]

其中：

```text
u：
  某个神经元、head、channel、MLP feature 或残差方向。

R：
  某条路线，例如 format、echo、recipient、prose、punctuation。
```

完整图谱是：

[
M
=

\begin{bmatrix}
Suppress_{u_1}(R_1) & Suppress_{u_1}(R_2) & \cdots \
Suppress_{u_2}(R_1) & Suppress_{u_2}(R_2) & \cdots \
\vdots & \vdots & \ddots
\end{bmatrix}
]

这比“找到一个 suppressor”更合理。

因为真实模型中可能存在：

```text
专门压 format route 的组件；
专门压 echo route 的组件；
压 recipient answer 的组件；
压 prose prefix 的组件；
压标点 / 换行的组件；
同时增强 value answer 并压 echo 的混合组件。
```

所以全局 suppressor 结构应该是：

```text
route-specific suppressor field（路线特异抑制场）
+
context-conditioned suppressor gate（上下文条件化抑制门）
```

---

# 七、为什么 suppressor 可能是语言编码的关键？

因为语言不仅要表达“什么是对的”，还要知道“什么现在不该说”。

例如：

```text
Question:
  What is the category of apple?
Answer:
```

正确输出是：

```text
fruit
```

模型必须同时抑制：

```text
apple        对象回声
category     关系回声
The          散文路线
Answer       格式路线
red          其他属性值
sweet        其他关系值
.            停止 / 标点
```

所以语言能力不是单纯“激活 fruit”，而是：

[
\ell(\text{fruit})

>

\ell(\text{apple})
]

[
\ell(\text{fruit})

>

\ell(\text{category})
]

[
\ell(\text{fruit})

>

\ell(\text{The})
]

[
\ell(\text{fruit})

>

\ell(\text{red})
]

这就是 suppressor 的重要性。

---

# 八、如何改进你的理论表述？

你可以把原判断：

```text
如果找到全局 suppressor 结构，就破解了语言编码结构。
```

改成更严格版本：

```text
如果找到全局 route-level suppressor 结构，
并把它与 source-writer-rewriter-booster-readout-continuation 图谱连接起来，
就可以破解语言编码结构中最核心的生成选择机制。
```

或者更完整：

```text
语言编码结构 = 路线生成机制 + 路线抑制机制 + 读出闭合机制。
```

公式：

[
LanguageEncoding
================

RouteConstruction
+
RouteSuppression
+
ReadoutClosure
]

展开：

[
LanguageEncoding
================

Source
+
Writer
+
Rewriter
+
Booster
+
Suppressor
+
Competition
+
Continuation
]

其中 suppressor 的地位是：

```text
它决定哪些潜在路线被阻止；
没有它，就没有稳定生成。
```

---

# 九、如何系统研究全局 suppressor？

建议分 6 步。

---

## 1. 建立路线词表

先定义路线集合：

```text
value_answer route（值答案路线）
recipient route（受体路线）
format route（格式路线）
echo route（回声路线）
prose route（散文路线）
punctuation route（标点路线）
continuation route（续写路线）
reasoning route（推理路线）
translation route（翻译路线）
code route（代码路线）
```

每条路线对应一组 token：

[
V_R
]

---

## 2. 计算路线分数

[
S_R(h)
======

\max_{y\in V_R}
W_U(y)^\top h
]

或者用平均：

[
S_R(h)
======

\operatorname{logsumexp}_{y\in V_R}
W_U(y)^\top h
]

更平滑：

[
S_R(h)
======

\log
\sum_{y\in V_R}
\exp(W_U(y)^\top h)
]

---

## 3. 计算组件对路线的 boost / suppression

对组件 (u) 做干预，得到：

[
\Delta_u S_R
============

## S_R(h^{do(u)})

S_R(h)
]

如果：

[
\Delta_u S_R>0
]

它增强路线 (R)。

如果：

[
\Delta_u S_R<0
]

它抑制路线 (R)。

---

## 4. 建立 suppressor matrix

[
M_{u,R}
=======

*

\Delta_u S_R
]

然后聚类：

```text
哪些组件专门压 format？
哪些组件专门压 echo？
哪些组件压 prose？
哪些组件压 wrong value？
哪些组件同时 boost answer + suppress format？
```

---

## 5. 做上下文条件化

suppressor 不是固定的，它依赖任务。

所以要定义：

[
M_{u,R}(x)
]

也就是：

```text
在不同 prompt、不同任务、不同输出格式下，
同一组件抑制哪些路线？
```

例如：

```text
短答任务：
  压 prose，压 format，压 echo。

解释任务：
  不应压 prose。

JSON 任务：
  不应压 format，而应压自由文本。

翻译任务：
  压源语言回声，增强目标语言路线。
```

这一步非常关键，否则会误把“当前任务的抑制器”当成全局抑制器。

---

## 6. 验证生成闭合

最终要看：

[
GenerationClosure
\Longleftrightarrow
S_{R^*}(h)
----------

\max_{R\neq R^*}S_R(h)>0
]

并且续写闭合：

[
P(y_{1:m}^{R^*}|x,y_0^{R^*})>\tau
]

只有同时满足：

```text
目标路线赢；
后续路线保持；
非目标路线被压；
```

才能说 suppressor 图谱真正解释生成。

---

# 十、这和“语义、逻辑、语法路线”如何对应？

可以把语言能力看成多路线集合：

```text
语义路线：
  value / concept / relation / entity

逻辑路线：
  premise / inference / conclusion

语法路线：
  noun / verb / punctuation / clause / agreement

格式路线：
  JSON / list / short answer / explanation

对话路线：
  answer / refuse / clarify / ask back

回声路线：
  copy object / copy relation / repeat prompt

续写路线：
  continue answer / stop / explain / elaborate
```

每个任务都要求选择某些路线、压制另一些路线。

例如短答任务：

```text
增强：
  semantic value route

抑制：
  prose route
  explanation route
  echo route
  format prefix route
```

解释任务：

```text
增强：
  prose route
  reasoning route

抑制：
  over-short answer route
```

翻译任务：

```text
增强：
  target language route

抑制：
  source language echo route
```

代码任务：

```text
增强：
  syntax / indentation / variable route

抑制：
  natural language prose route
```

所以 suppressor 是一个**任务条件化路线选择系统**。

---

# 十一、当前实验对这个判断的支持

Phase 743-745 已经给出强支持：

```text
qwen3：
  压 recipient 后 format 接管，
  说明存在多路线接力竞争。

GLM4：
  donor force 已强，剩余 echo / other_vocab 抑制不足，
  说明闭合接近但仍需路线抑制。

DS7B：
  format + echo 是主竞争场，
  说明短答失败常常不是语义缺失，而是路线选择失败。
```

这些都支持你的判断：

```text
语言编码结构不只是语义编码；
还包含大量路线抑制机制。
```

---

# 十二、但为什么不能只研究 suppressor？

因为 suppressor 只告诉我们：

```text
什么不该输出。
```

但语言还需要知道：

```text
什么应该输出。
```

所以完整机制必须同时有：

```text
booster：
  提高目标路线。

suppressor：
  降低非目标路线。

binder：
  绑定对象、关系、值。

writer：
  把源信息写入当前位置。

rewriter：
  把状态变成读出可用形式。

continuation controller：
  控制后续生成是否保持路线。
```

如果只找到 suppressor，没有找到 source / writer / rewriter，就会知道“怎么不说错”，但不知道“怎么说对”。

---

# 十三、改进后的总理论公式

可以写成：

[
h_{\text{final}}
================

h_{\text{base}}
+
C_{\text{source}}
+
C_{\text{writer}}
+
C_{\text{rewriter}}
+
C_{\text{booster}}
+
C_{\text{suppressor}}
+
\epsilon
]

读出：

[
\ell(y)
=======

W_U(y)^\top h_{\text{final}}
]

路线分数：

[
S_R(h)
======

\log
\sum_{y\in V_R}
\exp(\ell(y))
]

目标路线闭合：

[
Closure(R^*)
\Longleftrightarrow
S_{R^*}(h)
----------

\max_{R\neq R^*}
S_R(h)

>

0
]

其中：

[
S_{R^*}(h)
==========

S_{R^*}^{base}
+
Boost(R^*)
]

[
S_R(h)
======

## S_R^{base}

Suppress(R)
]

所以：

[
Closure(R^*)
\Longleftrightarrow
Boost(R^*)
+
\sum_{R\neq R^*}Suppress(R)

>

Gap(R^*)
]

这就是路线级语言生成闭合公式。

---

# 十四、最重要的结论

你的分析可以改成下面这个更精确版本：

```text
自回归训练会把语言能力组织成多维路线图谱。
这些路线包括语义、语法、逻辑、格式、回声、解释、续写等。
生成不是单路线输出，而是多路线竞争。
suppressor 是控制非目标路线不赢的机制。
如果能找到全局、任务条件化、路线级 suppressor 结构，
并把它与 booster、writer、rewriter、readout、continuation 图谱连接起来，
就能破解语言编码结构中最关键的生成选择机制。
```

---

# 十五、下一步应该怎么做？

我建议下一步理论和实验都围绕：

```text
Global Route Suppressor Atlas
全局路线抑制器图谱
```

来设计。

具体 Phase 可以定义为：

```text
Phase 746: Natural Route Suppressor Localization
自然路线级抑制器定位
```

核心任务：

```text
1. 建立路线词表：
   value、recipient、format、echo、prose、punctuation、continuation、reasoning、translation 等。

2. 对每个组件计算：
   Boost(target route)
   Suppress(non-target route)

3. 建立 suppressor matrix：
   M_{u,R}

4. 找到任务条件化 suppressor：
   短答时压 prose；
   解释时不压 prose；
   翻译时压源语言；
   JSON 时压自由文本。

5. 从 whole component 下钻到：
   head / channel / neuron / SAE feature。

6. 最终验证：
   route-level suppression 能否稳定提升 generation closure。
```

成功后，你就会得到一个真正接近语言编码结构的东西：

```text
不是某个词的向量图谱，
而是语言路线的增强—抑制—闭合图谱。
```

---

# 最终回答

你的判断**基本正确，但需要补一半结构**：

```text
suppressor 是自回归机制形成的核心体现之一；
它控制语义、逻辑、语法、格式等路线之间的竞争；
找到全局 suppressor 结构，将极大推进语言编码机制破解。
```

但更准确地说：

```text
全局 suppressor 结构不是全部语言编码，
而是语言编码中“路线选择与生成闭合”的核心。
```

完整破解需要：

```text
source / writer / rewriter / booster / suppressor / readout / continuation
```

共同成图。

如果要把它压缩成一句话：

```text
语言编码不是把正确答案写进网络，
而是让正确路线在所有可能路线中胜出；
suppressor 就是控制“哪些路线不能胜出”的全局机制。
```

所以，你现在提出的方向很值得继续。下一步不应再只找“苹果神经元”或“水果 head”，而应建立：

```text
全局路线级 suppressor 图谱。
```

这可能确实是破解语言编码结构的关键入口之一。

---

# 十六、Phase 748 实测补充：自然路线抑制器矩阵

本轮把上面的理论判断推进到可测对象：

```text
不是只问某个组件是否提升 donor answer（供体答案），
而是测量某个组件对多条竞争路线的抑制矩阵。
```

核心定义如下。

路线读出分数：

$$
S_R(h)=\max_{y\in V_R} W_U(y)^\top \operatorname{Norm}(h)
$$

组件对路线的抑制量：

$$
Suppress_u(R)=S_R(h_{\text{base}})-S_R(h_{\text{do}(u)})
$$

组件对目标答案的增强量：

$$
Boost_u=\ell_{y^*}(h_{\text{do}(u)})-\ell_{y^*}(h_{\text{base}})
$$

路线抑制矩阵：

$$
M_{u,R}=Suppress_u(R)
$$

其中：

```text
u：
  whole component（整体组件），例如 L32:mlp_out 或 L23:attn_out。

R：
  route（路线），例如 recipient_answer、format_or_schema、echo_object_or_relation、
  punctuation_or_stop、other_vocab、other_semantic_value。
```

测试使用三轮：

```text
smoke：
  确认脚本、hook、路线分类、输出文件正常。

main：
  每个模型 6 组 pair，每组 2 个 audit，每个 audit 取 3 个 combo candidate。

confirm：
  每个模型 8 组 pair，每组 2 个 audit，每个 audit 取 3 个 combo candidate，
  top-k 词表范围扩大到 20。
```

脚本位置：

```text
tests/gpt5/phase748_natural_route_suppressor_matrix.py
tests/gpt5/run_phase748_natural_route_suppressor_matrix_round.sh
```

结果位置：

```text
results/glm5_phase748_natural_route_suppressor_matrix/
```

---

# 十七、三模型客观结果

## 1. qwen3

confirm 轮最重要结果：

```text
L32:mlp_out
  target boost = -0.047
  route suppression = 2.961
  route coverage = 3.875
  margin gain = 0.525
  donor top1 = 0.000

L33:attn_out
  target boost = 2.547
  route suppression = 2.531
  route coverage = 2.188
  margin gain = 2.950
  donor top1 = 0.000

L32:attn_out
  target boost = 3.066
  route suppression = 1.180
  route coverage = 1.938
  margin gain = 3.105
  donor top1 = 0.000
```

最清楚的现象：

```text
qwen3 的 L32:mlp_out 更像“纯 suppressor（抑制器）”：
  它几乎不增强目标答案，甚至略微降低目标答案，
  但稳定压低多条竞争路线。

qwen3 的 L32/L33 attention output（注意力输出）更像混合机制：
  同时增强目标答案和改变路线竞争场。
```

L32:mlp_out 的路线切片：

```text
format_or_schema:
  suppression = 0.430
  positive rate = 0.875

other_vocab:
  suppression = 0.516
  positive rate = 1.000

punctuation_or_stop:
  suppression = 0.570
  positive rate = 0.875

recipient_answer:
  suppression = 0.586
  positive rate = 0.750

echo_object_or_relation:
  suppression = 0.812
  positive rate = 1.000
```

严格解释：

```text
qwen3 已经出现较清楚的路线级抑制矩阵；
但 suppressor 本身没有让 donor answer 成为 top1。
所以 qwen3 证明的是“抑制矩阵存在”，不是“自然闭合已经完成”。
```

---

## 2. GLM4

confirm 轮最重要结果：

```text
L34:attn_out
  target boost = 1.504
  route suppression = 0.525
  route coverage = 2.500
  margin gain = 1.447
  donor top1 = 0.875

L35:attn_out
  target boost = 0.543
  route suppression = 0.611
  route coverage = 2.812
  margin gain = 0.552
  donor top1 = 0.625

L36:mlp_out
  target boost = -0.441
  route suppression = 1.332
  route coverage = 3.125
  margin gain = -0.443
  donor top1 = 0.312
```

最清楚的现象：

```text
GLM4 的 donor answer 基线本来就更强，
所以很多组件表现为 maintenance candidate（维持候选），
而不是从错误状态修复到正确状态的 closure candidate（闭合候选）。
```

GLM4 的 L34:attn_out 更像：

```text
booster（增强器） + suppressor（抑制器）的混合维护机制。
```

L36:mlp_out 更像：

```text
广域抑制但目标不受益的组件；
它压掉一些竞争路线，但同时不一定提升最终闭合。
```

严格解释：

```text
GLM4 支持“路线抑制和目标增强可共存”，
但因为基线 donor answer 已经较强，
它不适合作为判断 suppressor 是否独立完成闭合的唯一证据。
```

---

## 3. DS7B

confirm 轮最重要结果：

```text
L23:attn_out
  target boost = 1.762
  route suppression = 1.637
  route coverage = 2.875
  margin gain = 2.042
  donor top1 = 0.250

L22:attn_out
  target boost = 1.531
  route suppression = 2.809
  route coverage = 2.688
  margin gain = 2.040
  donor top1 = 0.188

L26:mlp_out
  target boost = 0.586
  route suppression = 2.215
  route coverage = 3.438
  margin gain = 0.995
  donor top1 = 0.000

L25:mlp_out
  target boost = 0.414
  route suppression = 2.055
  route coverage = 3.125
  margin gain = 0.732
  donor top1 = 0.000
```

最清楚的现象：

```text
DS7B 的 L22-L23 attention output（注意力输出）同时具有：
  目标增强；
  recipient_answer route（受体答案路线）抑制；
  多路线竞争场重排。

DS7B 的 L25-L26 MLP output（多层感知机输出）更像：
  broad route suppressor（广域路线抑制器），
  但它本身不足以完成 donor top1 闭合。
```

关键路线切片：

```text
L22:attn_out 对 recipient_answer:
  suppression = 3.062
  positive rate = 0.833

L23:attn_out 对 recipient_answer:
  suppression = 1.458
  positive rate = 0.833

L25:mlp_out 对 recipient_answer:
  suppression = 1.208
  positive rate = 1.000

L26:mlp_out 对 other_vocab:
  suppression = 0.469
  positive rate = 0.875
```

严格解释：

```text
DS7B 最支持“路线级 suppressor matrix（抑制矩阵）”存在；
但 top1 闭合仍然较低，说明自然闭合还需要 booster、readout、continuation 等机制共同完成。
```

---

# 十八、对附件理论的判断

附件中关于 Phase 745 的判断基本正确：

```text
Phase 745 不是普通 patch 成功率优化，
而是证明 token0 生成失败是路线级竞争失败。
```

Phase 748 对这个判断做了进一步补充：

```text
不仅需要 route-level multi-competitor suppression（路线级多竞争者抑制），
而且可以对每个组件建立 M_{u,R} 抑制矩阵。
```

附件中“全局 suppressor 就是破解语言编码机制”的判断需要收紧：

```text
全局 suppressor 不是完整语言编码机制；
但它很可能是语言编码机制中最关键的选择闭合机制。
```

更准确版本：

```text
语言编码机制 = 构造机制 + 选择机制 + 读出闭合机制。

构造机制：
  source / writer / rewriter / booster

选择机制：
  route suppressor matrix / context-conditioned suppressor gate

读出闭合机制：
  readout competition / continuation / generation closure
```

因此：

```text
找到全局 suppressor 图谱，
不能单独宣布破解全部语言编码；
但可以宣布已经抓住“路线选择如何发生”的核心入口。
```

---

# 十九、当前硬伤

1. 当前证据仍是 whole-component donor-recipient delta（整体组件供体-受体差分），不是自然前向链路的直接证明。

2. 当前组件粒度仍然太粗。L32:mlp_out、L23:attn_out 这类结果只能说明该整体组件含有抑制功能，不能说明具体是哪一个 head（注意力头）、channel（通道）或 neuron（神经元）。

3. 路线分类基于 top-k token（前 k 个词元）和人工 taxonomy（分类表），仍可能漏掉真实竞争路线。

4. 小模型内部结构可能存在偏差，尤其是 DS7B 和 qwen3 的闭合失败，不能直接等同于大模型机制。

5. suppressor 与 booster 经常混合，说明真实机制不是单一抑制，而是：

```text
目标增强 + 竞争路线压制 + 读出方向重排
```

三者共同作用。

---

# 二十、下一阶段任务

当前任务和接下来的任务仍属于同一阶段：

```text
从路线级图谱推进到组件内部图谱。
```

下一阶段建议定义为：

```text
Phase 749:
Suppressor Component Decomposition
抑制器组件分解
```

目标不是继续扩大普通 patch（修补）数量，而是下钻 Phase 748 找到的关键组件：

```text
qwen3:
  L32:mlp_out
  L33:attn_out
  L32:attn_out

GLM4:
  L34:attn_out
  L35:attn_out
  L36:mlp_out

DS7B:
  L22:attn_out
  L23:attn_out
  L25:mlp_out
  L26:mlp_out
```

需要回答三个问题：

```text
1. 这些组件内部是否存在更小粒度的 suppressor 单元？

2. suppressor 单元是 route-specific（路线特异）还是 broad/global（广域全局）？

3. suppressor 单元和 booster 单元是否可以分离？
```

优先测：

```text
attention output（注意力输出）：
  分解到 head 级别。

MLP output（多层感知机输出）：
  分解到 channel / neuron 级别。
```

阶段性成功标准：

```text
如果某些 head / channel 能稳定复现 M_{u,R} 的路线抑制轮廓，
并且跨 qwen3、GLM4、DS7B 出现相似的路线抑制角色，
则全局 suppressor 图谱可以从 whole-component 层级推进到可解释结构层级。
```

---

# 二十一、Phase 749 实测补充：抑制器组件分解

时间：

```text
2026-06-29 10:33
```

## 1. 测试目标

Phase 748 已经证明：

```text
whole-component donor-recipient delta（整体组件供体-受体差分）
可以形成 route-level suppressor matrix（路线级抑制矩阵）。
```

但它仍然有一个关键硬伤：

```text
L33:attn_out 或 L32:mlp_out 有 suppressor 效果，
不等于已经知道内部哪个 head（注意力头）、channel（通道）或 neuron（神经元）承担该效果。
```

因此 Phase 749 的任务是把 Phase 748 的关键组件向下分解：

```text
attention output（注意力输出）：
  分解到 head / headset（注意力头 / 注意力头集合）。

MLP output（多层感知机输出）：
  分解到 residual output channel / channelset（残差输出通道 / 通道集合）。
```

注意：

```text
MLP channelset（多层感知机通道集合）不是严格 neuron（神经元）证据。
它只能说明残差输出维度上存在可定位的 suppressor 子空间。
```

## 2. 生成脚本和命令

新增脚本：

```text
tests/gpt5/phase749_suppressor_component_decomposition.py
tests/gpt5/run_phase749_suppressor_component_decomposition_round.sh
```

三轮测试命令：

```bash
tests/gpt5/run_phase749_suppressor_component_decomposition_round.sh smoke --max-pairs 1 --top-audits 1 --top-candidates 1 --max-components 1 --top-k-vocab 10 --max-topk-tokens 6 --max-route-classes 4 --top-heads-per-component 2 --random-heads-per-component 1 --headset-sizes 1 2 --channelset-sizes 1 4 --individual-channels 2 --log-every 1

tests/gpt5/run_phase749_suppressor_component_decomposition_round.sh main --max-pairs 4 --top-audits 2 --top-candidates 3 --max-components 3 --top-k-vocab 16 --max-topk-tokens 10 --max-route-classes 6 --top-heads-per-component 4 --random-heads-per-component 2 --headset-sizes 1 2 4 --channelset-sizes 1 4 16 64 --individual-channels 4 --log-every 2

tests/gpt5/run_phase749_suppressor_component_decomposition_round.sh confirm --max-pairs 6 --top-audits 2 --top-candidates 3 --max-components 3 --top-k-vocab 18 --max-topk-tokens 12 --max-route-classes 7 --top-heads-per-component 4 --random-heads-per-component 2 --headset-sizes 1 2 4 --channelset-sizes 1 4 16 64 --individual-channels 4 --log-every 2
```

结果目录：

```text
results/glm5_phase749_suppressor_component_decomposition/
```

确认轮摘要：

```text
results/glm5_phase749_suppressor_component_decomposition/confirm/phase749_cross_model_summary.md
```

## 3. 测试原理

对 attention output（注意力输出）：

```text
1. 捕获 donor（供体）和 recipient（受体）在指定层 attention o_proj（输出投影）前的 head 表示。
2. 计算每个 head 的 donor-recipient delta（供体-受体差分）。
3. 只把某个 head 或 topH{1,2,4} headset（前若干注意力头集合）的 delta 投影回 residual stream（残差流）。
4. 用同一个 route-level max-logit matrix（路线级最大 logit 矩阵）测量：
   target boost（目标增强）
   route suppression（路线抑制）
   margin gain（间隔增益）
   delta fraction（相对整体组件的效果比例）
```

对 MLP output（多层感知机输出）：

```text
1. 捕获 donor 和 recipient 的 MLP residual output delta（残差输出差分）。
2. 按通道对 route suppression / margin gain 进行排序。
3. 测试单通道和 topC{1,4,16,64} channelset（通道集合）。
4. 与 whole-component（整体组件）效果比较。
```

因此 Phase 749 不是简单的 patch 成功率测试，而是：

```text
把 Phase 748 的整体 suppressor matrix（抑制矩阵）
拆成更小粒度的可解释候选单元。
```

## 4. 三模型客观结果

### qwen3

最强结果：

```text
L33:attn_out:topH4
donor top1 = 0.000
target boost = 1.589
route suppression = 3.406
coverage = 3.92
margin gain = 2.261
delta fraction = 0.856
effect = global_suppressor_margin_candidate
```

关键含义：

```text
qwen3 的 L33 attention suppressor（第33层注意力抑制器）
可以被 4 个 head 的集合复现大部分路线抑制效果。
```

对比整体组件：

```text
L33:attn_out whole-component
target boost = 2.948
route suppression = 2.656
margin gain = 3.343
```

这说明：

```text
L33 topH4 不只是弱替代，
甚至在 route suppression 上超过 whole-component，
但 target boost 和最终闭合仍弱于整体组件。
```

MLP 结果：

```text
L32:mlp_out whole-component:
  route suppression = 3.094

L32:mlp_out:topC64:
  route suppression = 1.979
  coverage = 4.50
  delta fraction = 0.365
```

解释：

```text
L32 MLP 的 suppressor 可以部分定位到 residual output channelset，
但 topC64 只复现约三分之一到一半的整体效果，
不能说明已经找到神经元级 suppressor。
```

### GLM4

GLM4 的结果整体更像 maintenance（维护），不是强修复：

```text
L36:mlp_out whole-component:
  donor top1 = 0.333
  target boost = -0.344
  route suppression = 1.086
  margin gain = -0.359

L36:mlp_out:topC64:
  target boost = -0.026
  route suppression = 0.654
  delta fraction = 0.299
```

注意力结果：

```text
L34:attn_out:topH4:
  donor top1 = 0.667
  target boost = 0.729
  route suppression = 0.659
  delta fraction = 0.668

L35:attn_out:topH4:
  donor top1 = 0.333
  target boost = 0.099
  route suppression = 0.841
  delta fraction = 0.662
```

解释：

```text
GLM4 的子组件能复现部分抑制形状，
但 target boost 和 margin gain 不强，
更像已经正确路线的维护器，而不是错误路线的强纠偏器。
```

### DS7B

DS7B 的 attention headset 结果最稳定：

```text
L22:attn_out whole-component:
  donor top1 = 0.167
  target boost = 1.208
  route suppression = 3.323
  margin gain = 1.739

L22:attn_out:topH4:
  donor top1 = 0.167
  target boost = 0.958
  route suppression = 2.768
  margin gain = 1.432
  delta fraction = 0.708
```

另一个强结果：

```text
L23:attn_out whole-component:
  donor top1 = 0.250
  target boost = 1.620
  route suppression = 2.057

L23:attn_out:topH4:
  donor top1 = 0.250
  target boost = 1.266
  route suppression = 1.911
  delta fraction = 0.739
```

MLP 结果：

```text
L25:mlp_out whole-component:
  donor top1 = 0.167
  target boost = 0.354
  route suppression = 2.292

L25:mlp_out:topC64:
  donor top1 = 0.167
  target boost = 0.359
  route suppression = 1.177
  delta fraction = 0.310
```

解释：

```text
DS7B 的 L22 / L23 attention suppressor（第22/23层注意力抑制器）
可以明显下钻到少量 head set。
MLP channelset 也有路线压制，但解释力弱于 attention headset。
```

## 5. 对 Phase 745 附件理论的校正

附件中关于 Phase 745 的判断总体正确：

```text
当前不是继续堆普通 patch，
而是在寻找路线级抑制与读出闭合的关键结构。
```

但经过 Phase 748 和 Phase 749 后，最准确说法应改为：

```text
全局 suppressor 是破解语言编码机制的关键入口，
但不是语言编码机制本身的全部。
```

现在更接近真实机制的是：

```text
source（源词元）
  -> writer（写入器）
  -> skeleton carrier（骨架承载器）
  -> rewriter / booster（重写器 / 增强器）
  -> route suppressor matrix（路线抑制矩阵）
  -> readout competition（读出竞争）
  -> continuation closure（续写闭合）
```

Phase 749 的新增价值在于：

```text
route suppressor matrix 不是只能在整体层看见；
在 qwen3 和 DS7B 中，它可以被少量 attention head set 部分复现。
```

这比 Phase 748 前进了一层：

```text
whole-component suppressor
  -> headset-level suppressor candidate
  -> channelset-level partial suppressor candidate
```

## 6. 当前硬伤和边界

1. attention head（注意力头）分解是基于 o_proj projected delta（输出投影后的差分），说明 head 输出对 residual stream（残差流）的贡献有该效果，但还不是完整自然前向机制证明。

2. MLP channel（多层感知机通道）结果只是 residual output channel（残差输出通道），不是中间激活 neuron（神经元）。不能把 topC64 说成 64 个神经元。

3. qwen3 和 DS7B 的 donor top1 rate（供体第一名率）仍偏低，说明 suppressor 能重排路线，但不一定能独立完成最终生成闭合。

4. GLM4 的结果更多是维护型，不是强纠偏型，说明不同模型内部实现可能存在结构偏差。

5. 路线分类仍依赖 top-k token（前 k 个词元）和人工 taxonomy（分类表），仍可能漏掉真实内部路线。

6. 当前仍是小模型测试，不能直接外推为大模型或人脑语言机制。

## 7. 阶段性结论

客观结果支持：

```text
1. route-level suppressor matrix 是真实可测的。
2. attention suppressor 可以下钻到少量 head set。
3. MLP suppressor 可以部分下钻到 residual output channel set。
4. booster 和 suppressor 仍明显纠缠，尚未完全分离。
5. suppressor 是语言编码机制中的选择闭合核心，但不是全部构造机制。
```

因此当前图谱应更新为：

```text
构造侧：
  source / writer / rewriter / booster

选择侧：
  route suppressor matrix / headset suppressor / channelset suppressor

闭合侧：
  readout competition / continuation closure
```

## 8. 下一阶段任务

Phase 750 仍属于同一个阶段目标：

```text
从 whole-component suppressor
推进到 natural subunit suppressor graph。
```

下一步不应该继续扩大 patch 数量，而应该验证：

```text
1. top headset 是否在自然 forward（自然前向）中必要？
2. top headset 是否能被 source token attention（源词元注意力）解释？
3. top headset 的 value vector（值向量）是否携带 suppressor 内容？
4. MLP channelset 是否来自少量中间 neuron，还是只是分布式残差投影？
```

最关键的下一阶段实验：

```text
Phase 750:
Natural Subunit Suppressor Necessity Test
自然子单元抑制器必要性测试
```

核心操作：

```text
对 Phase 749 找到的 topH4 / topC64：
  1. 在原始 recipient forward（受体自然前向）中做 erase / mask / replace。
  2. 不引入 donor delta，直接观察目标 token、竞争路线、读出闭合是否退化。
  3. 如果自然路径中移除这些子单元会系统性破坏路线选择，
     才能把它们从 patch candidate（修补候选）
     提升为 natural suppressor unit（自然抑制单元）。
```

如果 Phase 750 成立，研究会从：

```text
差分修补图谱
```

推进到：

```text
自然前向功能图谱
```

这才是真正接近语言编码机制的下一步。

---

# 二十二、Phase 750 实测补充：自然子单元抑制器必要性测试

时间：

```text
2026-06-29 10:44
```

## 1. 为什么必须做 Phase 750

Phase 749 证明：

```text
部分 attention headset（注意力头集合）和 MLP channelset（多层感知机通道集合）
可以复现 donor-recipient delta（供体-受体差分）中的路线抑制效果。
```

但这仍然不等于自然机制：

```text
patch candidate（修补候选）
不等于
natural functional unit（自然功能单元）。
```

Phase 750 因此不再注入 donor delta（供体差分），而是在模型自然前向中直接擦除 Phase 749 指向的子单元，观察：

```text
1. target answer（目标答案）是否下降；
2. competitor routes（竞争路线）是否释放；
3. readout margin（读出间隔）是否变差；
4. top1 closure（第一名闭合）是否丢失。
```

这一步回答的是：

```text
这些子单元是不是自然前向路径里的必要结构？
```

## 2. 生成脚本和命令

新增脚本：

```text
tests/gpt5/phase750_natural_subunit_suppressor_necessity.py
tests/gpt5/run_phase750_natural_subunit_suppressor_necessity_round.sh
```

结果目录：

```text
results/glm5_phase750_natural_subunit_suppressor_necessity/
```

确认轮摘要：

```text
results/glm5_phase750_natural_subunit_suppressor_necessity/confirm/phase750_cross_model_summary.md
```

三轮测试命令：

```bash
tests/gpt5/run_phase750_natural_subunit_suppressor_necessity_round.sh smoke --max-pairs 1 --top-audits 1 --max-components 1 --top-k-vocab 10 --max-topk-tokens 6 --max-route-classes 4 --headset-sizes 1 2 --channelset-sizes 16 --individual-heads 1 --individual-channels 1 --log-every 1

tests/gpt5/run_phase750_natural_subunit_suppressor_necessity_round.sh main --max-pairs 4 --top-audits 2 --max-components 3 --top-k-vocab 16 --max-topk-tokens 10 --max-route-classes 6 --headset-sizes 1 2 4 --channelset-sizes 16 64 --individual-heads 1 --individual-channels 1 --log-every 2

tests/gpt5/run_phase750_natural_subunit_suppressor_necessity_round.sh confirm --max-pairs 6 --top-audits 2 --max-components 3 --top-k-vocab 18 --max-topk-tokens 12 --max-route-classes 7 --headset-sizes 1 2 4 --channelset-sizes 16 64 --individual-heads 1 --individual-channels 1 --log-every 2
```

三个模型按：

```text
qwen3 -> GLM4 -> DS7B
```

顺序运行，并继续使用：

```text
--hard-exit-after-model
```

避免 GPU 显存残留。

## 3. 测试原理

Phase 750 与 Phase 749 的关键区别：

```text
Phase 749:
  给 recipient（受体）注入 donor-recipient delta，
  看子单元能不能修补或抑制竞争路线。

Phase 750:
  不注入 donor delta，
  在 natural donor / natural recipient forward（自然供体/受体前向）中直接擦除子单元，
  看自然生成是否退化。
```

attention erase（注意力擦除）：

```text
在 o_proj（输出投影）前，
把指定 head（注意力头）的 final token（最后词元）输入 slice 置零。
```

MLP erase（多层感知机擦除）：

```text
在 MLP output（多层感知机输出）中，
把指定 residual output channels（残差输出通道）置零。
```

衡量指标：

```text
target_logit_drop_after_erase:
  擦除后目标答案 logit 下降量。

total_positive_route_release_after_erase:
  擦除后竞争路线最大 logit 的正向释放总量。

mean_margin_drop_target_vs_routes:
  目标答案相对竞争路线的平均间隔下降。

top1_loss_rate:
  原来目标答案 top1，擦除后失去 top1 的比例。
```

## 4. qwen3 客观结果

确认轮最靠前的 qwen3 结果：

```text
L32:attn_out:H25 / natural_recipient
base top1 = 1.000
after top1 = 1.000
top1 loss = 0.000
target drop = -1.812
route release = 3.062
coverage = 3.50
margin drop = -0.979
effect = erase_improves_or_inverse_effect
```

以及：

```text
L32:attn_out:topH4 / natural_donor
base top1 = 1.000
after top1 = 1.000
top1 loss = 0.000
target drop = -1.292
route release = 1.979
coverage = 3.00
margin drop = -0.919
effect = erase_improves_or_inverse_effect
```

解释：

```text
qwen3 的自然擦除结果不支持“这些 topH 是自然必要 suppressor”。
擦除后竞争路线会释放，但目标答案反而经常增强或 margin 不退化。
```

这说明 Phase 749 中 qwen3 的 headset patch 效果更可能是：

```text
donor-recipient delta 下的可利用修补方向，
而不是自然前向中稳定必要的路线抑制器。
```

MLP 结果也类似：

```text
L32:mlp_out:topC64 / natural_donor
base top1 = 1.000
after top1 = 1.000
target drop = -0.417
route release = 1.854
margin drop = 0.022
effect = small_or_no_effect
```

所以 qwen3 的结论应收紧为：

```text
有 route release（路线释放）现象，
但没有稳定自然必要性证据。
```

## 5. GLM4 客观结果

GLM4 确认轮整体更弱：

```text
L36:mlp_out:topC64 / natural_donor
base top1 = 1.000
after top1 = 1.000
top1 loss = 0.000
target drop = -0.198
route release = 0.969
coverage = 4.33
margin drop = 0.023
effect = small_or_no_effect
```

注意力结果：

```text
L35:attn_out:topH4 / natural_donor
base top1 = 1.000
after top1 = 1.000
target drop = -0.115
route release = 0.740
coverage = 3.33
margin drop = 0.000
effect = small_or_no_effect
```

少量单 head 有自然必要性候选：

```text
L34:attn_out:H4 / natural_donor
target drop = 0.312
route release = 0.375
coverage = 3.00
margin drop = 0.250
effect = natural_suppressor_necessity_candidate
```

但样本数只有：

```text
n = 2
```

因此不能作为强结论。

GLM4 的结论：

```text
多数子单元擦除只造成弱路线释放，
不破坏 top1 closure，
自然必要性证据很弱。
```

## 6. DS7B 客观结果

DS7B 是 Phase 750 最重要的模型。

最强 headset 结果：

```text
L22:attn_out:topH4 / natural_donor
base top1 = 0.750
after top1 = 0.500
top1 loss = 0.250
target drop = 1.438
route release = 0.682
coverage = 1.58
margin drop = 1.423
effect = target_support_necessity_candidate
```

对应 topH2：

```text
L22:attn_out:topH2 / natural_donor
base top1 = 0.750
after top1 = 0.667
top1 loss = 0.083
target drop = 0.776
route release = 0.641
coverage = 1.67
margin drop = 0.801
effect = target_support_necessity_candidate
```

单 head 结果更接近自然 suppressor：

```text
L22:attn_out:H1 / natural_donor
base top1 = 0.857
after top1 = 0.714
top1 loss = 0.143
target drop = 0.518
route release = 0.777
coverage = 2.29
margin drop = 0.616
effect = natural_suppressor_necessity_candidate
```

L23 也有稳定必要性信号：

```text
L23:attn_out:topH4 / natural_recipient
base top1 = 0.750
after top1 = 0.667
top1 loss = 0.083
target drop = 0.542
route release = 0.740
coverage = 2.08
margin drop = 0.542
effect = target_support_necessity_candidate
```

解释：

```text
DS7B 的 L22 / L23 attention subunits（第22/23层注意力子单元）
不只是 donor-delta patch candidate，
在自然前向擦除中也会造成目标下降、竞争释放、margin 退化和部分 top1 loss。
```

这比 Phase 749 的证据强一层。

但 MLP 结果较弱：

```text
L25:mlp_out:topC64 / natural_donor
base top1 = 0.750
after top1 = 0.750
target drop = -0.104
route release = 0.578
margin drop = -0.076
effect = small_or_no_effect
```

因此 DS7B 的自然必要性主要集中在：

```text
attention headset / head
```

而不是：

```text
MLP residual output channelset
```

## 7. 对 Phase 749 的修正

Phase 749 的正结果必须收紧：

```text
能用 donor delta 修补或抑制竞争路线，
不等于该子单元在自然前向中必要。
```

经过 Phase 750，三模型出现明显分化：

```text
qwen3:
  patch suppressor 明显；
  natural necessity 弱，甚至出现 erase improves / inverse effect。

GLM4:
  多数是 maintenance / weak route release；
  strong necessity 不稳定。

DS7B:
  L22 / L23 attention 子单元出现自然必要性信号；
  是目前最接近自然功能图谱的证据。
```

因此当前不能说：

```text
已经证明全局 suppressor 的自然机制。
```

只能说：

```text
DS7B 上已经出现从 patch suppressor 到 natural subunit necessity 的局部桥接证据。
```

## 8. 对“全局 suppressor 破解语言编码机制”的最终边界

附件理论的大方向正确：

```text
语言生成不是单一 value boost；
它包含路线竞争、路线抑制、读出闭合。
```

但 Phase 750 进一步说明：

```text
全局 suppressor 不是一个可以直接从 patch 结果外推出来的单一实体。
```

更准确的机制图谱是：

```text
patch-visible suppressor（修补可见抑制器）
  只能说明某个差分方向可以压制竞争路线；

natural suppressor unit（自然抑制单元）
  必须在自然前向擦除时导致目标下降、竞争释放或闭合丢失；

full coding mechanism（完整编码机制）
  还需要解释 source、writer、rewriter、booster、suppressor、readout 的自然协同。
```

因此当前最稳妥结论：

```text
suppressor 是破解语言编码机制的关键拼图；
DS7B 的 L22/L23 attention 子单元是目前最接近自然 suppressor 的证据；
但完整语言编码机制还没有闭合。
```

## 9. 当前硬伤

1. 擦除本身是人工干预，可能造成 off-manifold perturbation（离流形扰动）。

2. qwen3 和 GLM4 的自然必要性证据弱，说明 patch suppressor 与 natural suppressor 不能简单等同。

3. DS7B 的自然必要性集中在小模型局部层，不能直接外推到大模型。

4. attention head erase（注意力头擦除）仍是 head 级证据，不是 neuron（神经元）级证据。

5. MLP channelset（多层感知机通道集合）多数没有自然必要性，说明残差通道图谱离真实 MLP 神经元机制还有距离。

6. 当前 route taxonomy（路线分类）仍依赖 top-k token 和人工类别，可能漏掉真实竞争路线。

## 10. 下一阶段任务

Phase 750 已经完成从：

```text
patch candidate
```

到：

```text
natural necessity candidate
```

的第一轮筛选。

下一阶段不应该继续扩大普通组件，而应聚焦 DS7B 的最强自然候选：

```text
L22:attn_out:H1
L22:attn_out:topH2
L22:attn_out:topH4
L23:attn_out:topH4
```

Phase 751 建议：

```text
Natural Attention Head Mechanism Backtrace
自然注意力头机制回溯
```

核心问题：

```text
1. 这些 head 在自然前向中主要 attend（关注）哪些 source token（源词元）？
2. 它们的 value vector（值向量）到底携带 target support（目标支持）还是 route suppression（路线抑制）？
3. 擦除 head 的 Q/K pattern（查询/键模式）和擦除 V/O content（值/输出内容）能否分离？
4. L22 与 L23 是同一条路线的连续层，还是两个不同机制？
```

如果 Phase 751 成立，图谱将从：

```text
自然必要 head
```

推进到：

```text
自然注意力头的 source-token -> value-vector -> readout-effect 路径图谱。
```

这才是真正从 head 图谱向机制图谱推进。

## Phase 751：自然注意力头机制回溯 [2026-06-29 11:12]

## 1. 对上传分析的判断

上传内容的主线基本正确：当前研究确实已经从“局部 patch（修补）能不能成功”推进到三个更接近真实机制的层级：

```text
1. 自回归训练迫使模型形成预测充分状态。
2. 预测充分状态在层间表现为 source -> writer -> rewriter -> readout competition 的路径图谱。
3. suppressor 不是单个向量，而是路线竞争中的一类可定位功能。
```

但需要收紧三点：

```text
1. 不能说已经破解语言编码机制。
2. 当前证据仍集中在小模型、局部任务、head 级路径。
3. attention mass 只能说明 Q/K pattern 可见，不能直接说明因果内容。
```

所以本阶段继续任务是合理的，而且与前面 Phase 745-750 属于同一阶段目标：从 route-level suppressor（路线级抑制器）推进到自然注意力头内部的 source-token -> V/O content -> readout effect 图谱。

## 2. 脚本和命令

新增脚本：

```text
tests/gpt5/phase751_natural_attention_head_mechanism_backtrace.py
tests/gpt5/run_phase751_natural_attention_head_mechanism_backtrace_round.sh
```

静态检查：

```bash
python -m py_compile tests/gpt5/phase751_natural_attention_head_mechanism_backtrace.py
```

dry-run：

```bash
python tests/gpt5/phase751_natural_attention_head_mechanism_backtrace.py --dry-run --round-name smoke --max-pairs 1 --top-audits 1 --max-components 1 --top-k-vocab 10 --max-topk-tokens 6 --max-route-classes 4 --headset-sizes 1 2 --individual-heads 1 --max-focus-heads 1 --max-source-groups 4
```

第一轮冒烟测试：

```bash
tests/gpt5/run_phase751_natural_attention_head_mechanism_backtrace_round.sh smoke --max-pairs 1 --top-audits 1 --max-components 1 --top-k-vocab 10 --max-topk-tokens 6 --max-route-classes 4 --headset-sizes 1 2 --individual-heads 1 --max-focus-heads 1 --max-source-groups 4 --log-every 1
```

第二轮主测试：

```bash
tests/gpt5/run_phase751_natural_attention_head_mechanism_backtrace_round.sh main --max-pairs 4 --top-audits 1 --max-components 2 --top-k-vocab 14 --max-topk-tokens 8 --max-route-classes 5 --headset-sizes 1 2 4 --individual-heads 1 --max-focus-heads 2 --max-source-groups 8 --log-every 1
```

第三轮确认测试：

```bash
tests/gpt5/run_phase751_natural_attention_head_mechanism_backtrace_round.sh confirm --max-pairs 6 --top-audits 1 --max-components 2 --top-k-vocab 16 --max-topk-tokens 10 --max-route-classes 6 --headset-sizes 1 2 4 --individual-heads 1 --max-focus-heads 2 --max-source-groups 8 --log-every 2
```

说明：本阶段为了获得 per-head attention map（逐注意力头注意力图）并在 o_proj（输出投影）前做源贡献移除，使用 bf16、非量化、eager attention。三模型均按 qwen3 -> GLM4 -> DS7B 顺序运行，并使用 `--hard-exit-after-model`，每个模型结束后显存释放正常。

## 3. 测试原理

本阶段把一个注意力头拆成两层证据：

```text
Q/K pattern:
某个 head 在自然前向中把注意力分配给哪些 source token。

V/O content:
这些 source token 通过 value vector 和 o_proj 写入的内容，移除后是否改变 target logit 和 route competition。
```

核心计算流程：

```text
1. 对自然 prompt 前向，记录每个候选层的 attention weights 和 v_proj 输出。
2. 把 prompt 切成 target_record_line、target_value_tokens、records_all、object_tokens、relation_tokens 等 source group。
3. 对候选 head / headset 计算对每个 source group 的 attention mass。
4. 用 attention weights * value vectors 得到 source contribution。
5. 只在 o_proj 输入处移除指定 head 从指定 source group 带来的 contribution。
6. 比较移除前后的 target logit、route release、margin drop、top1 loss。
```

这一步比普通 head erase 更细，因为它不擦掉整个 head，而是问：

```text
这个 head 从哪个 source group 写入了什么可读出内容？
```

## 4. 结果文件

```text
results/glm5_phase751_natural_attention_head_mechanism_backtrace/smoke/
results/glm5_phase751_natural_attention_head_mechanism_backtrace/main/
results/glm5_phase751_natural_attention_head_mechanism_backtrace/confirm/
```

确认轮行数：

```text
qwen3:      768 rows
GLM4:      912 rows
DS7B:     1040 rows
```

跨模型摘要：

```text
results/glm5_phase751_natural_attention_head_mechanism_backtrace/confirm/phase751_cross_model_summary.md
```

## 5. 客观结果

### 5.1 qwen3

确认轮最强结果：

```text
L33:attn_out:H15 / natural_donor / target_record_line
attention mass = 0.307
source target contribution = 10.700
source route suppression contribution = 0.494
remove target drop = 1.125
route release = 0.125
top1 loss = 0.000
role = target_support_content
```

同一个 H15 在 target_value_tokens 和 records_all 上也给出相同级别的 target drop。说明 qwen3 存在很强的自然 target support content（目标支持内容）头，但它更多表现为目标值支撑，不是强路线释放。

qwen3 的 topH4：

```text
L33:attn_out:topH4 / natural_donor / target_record_line
n = 6
attention mass = 0.183
source target contribution = 9.563
source route suppression contribution = 1.140
remove target drop = 0.583
route release = 0.625
role = mixed_target_support_and_route_guard
```

这说明 qwen3 的多头集合里出现了 target support 和 route guard 混合证据。

### 5.2 GLM4

确认轮最强结果：

```text
L35:attn_out:H29 / natural_recipient / records_all
attention mass = 0.622
source target contribution = 0.986
source route suppression contribution = 0.106
remove target drop = 0.250
route release = 0.000
top1 loss = 0.000
role = target_support_content
```

GLM4 也存在自然 target support 头，但量级明显弱于 qwen3 和 DS7B。L34:H4 有较高 attention mass，但移除效果弱，很多样本被归为：

```text
qk_pattern_visible_content_weak
```

这再次证明：

```text
高 attention mass 不等于强因果内容。
```

### 5.3 DS7B

确认轮最强单头：

```text
L22:attn_out:H24 / natural_donor / target_record_line
attention mass = 0.817
source target contribution = 0.281
source route suppression contribution = 0.320
remove target drop = 1.250
route release = 0.000
top1 loss = 0.000
role = target_support_content
```

H24 在 records_all 和 target_value_tokens 上也稳定：

```text
records_all:
attention mass = 0.926
remove target drop = 1.000
margin drop = 0.922

target_value_tokens:
attention mass = 0.598
remove target drop = 1.000
margin drop = 0.641
```

确认轮最强 headset：

```text
L22:attn_out:topH4 / natural_donor / records_all
n = 6
attention mass = 0.427
source target contribution = 1.918
source route suppression contribution = 0.922
remove target drop = 1.031
route release = 0.177
margin drop = 0.803
top1 loss = 0.000
role = target_support_content
```

在 natural_recipient 上，L22 topH4 更接近混合机制：

```text
L22:attn_out:topH4 / natural_recipient / records_all
n = 6
attention mass = 0.425
source target contribution = 1.319
source route suppression contribution = 0.399
remove target drop = 0.833
route release = 0.365
top1 loss = 0.333
role = target_support_content
```

Phase750 中重点关注的 H1 也得到确认：

```text
L22:attn_out:H1 / natural_recipient / target_record_line
n = 6
attention mass = 0.384
source target contribution = 1.602
source route suppression contribution = 0.368
remove target drop = 0.708
route release = 0.188
top1 loss = 0.167
role = target_support_content
```

另外出现一个 L23 结果：

```text
L23:attn_out:H6 / natural_recipient / relation_tokens
attention mass = 0.600
source target contribution = 3.754
source route suppression contribution = 0.267
remove target drop = 0.688
route release = 0.125
role = target_support_content
```

这提示 L23 可能不是单纯 suppressor，而可能参与 relation-conditioned target support（关系条件化目标支撑）。

## 6. 当前进展

Phase 751 完成了从：

```text
head erase proves necessity
```

推进到：

```text
source-group restricted V/O contribution removal proves source-content path
```

更具体地说，现在可以把一条自然注意力头路径写成：

```text
source token group
  -> attention head Q/K selects source
  -> V vector carries content
  -> O projection writes residual
  -> target logit / route competition changes
```

这是比 Phase750 更强的一步，因为 Phase750 只能说明“这个 head 或 headset 被擦掉后结果变差”，Phase751 开始定位“它从哪个 source group 带来的内容有因果作用”。

## 7. 对当前理论的影响

上传内容提出的“全局 suppressor 是破解语言编码机制的关键”需要修正为：

```text
suppressor 是破解语言编码机制的关键入口；
但自然机制中更基础的单位可能是 source-conditioned writer / supporter / guard path。
```

当前证据显示，最强自然头不总是纯 suppressor。DS7B L22:H24 更像 target support head，L22:topH4 和 H1 才出现 target support + route guard 的混合迹象。

因此更稳的理论表达是：

```text
语言生成不是先生成语义再抑制错误答案，
而是在自然前向中形成多条 source-conditioned route，
每条 route 同时包含 target support、route guard、format control、readout competition 等功能。
```

## 8. 严格审视

1. 本阶段仍是 head 级和 source group 级，不是 neuron 级。

2. source group 仍由字符串和 token span 规则构造，可能不等于模型内部真实分组。

3. 移除 V/O contribution 仍是人工干预，可能造成局部 off-manifold effect。

4. qwen3 和 DS7B 的 target contribution 数值尺度不同，不能直接比较绝对值。

5. L22:H24 是新出现的强候选，但它不是 Phase750 最初列出的 H1/H7/TopH4，因此需要下一阶段确认它是不是稳定 writer，还是被本阶段动态筛选方法偏向选出来。

6. L23:H6 的 relation_tokens 结果很重要，但目前 n=1，不能作为稳定结论。

## 9. 阶段性结论

最保守结论：

```text
Phase751 证明，至少在小模型的苹果—水果—属性微域中，
自然注意力头不只是抽象地重要，
而是可以回溯到具体 source token group 的 V/O 内容贡献。
```

最重要拼图：

```text
DS7B L22:H24:
更像 target support writer。

DS7B L22:topH4:
更像 target support + route guard 混合集合。

DS7B L22:H1:
延续 Phase750 的自然必要性证据，但更偏 target support。

DS7B L23:H6:
可能是 relation-conditioned support，但证据量不足。
```

## 10. 下一阶段任务

下一阶段仍然属于同一阶段性目标，不需要转向大而全图谱。应该做 Phase 752：

```text
Natural Writer Stability and Path Chain Validation
自然写入器稳定性与路径链验证
```

核心任务：

```text
1. 固定 DS7B L22:H24、L22:H1、L22:topH4、L23:H6。
2. 扩展苹果—水果—属性中的 object、relation、answer 类型。
3. 检查这些 head 是 object-specific、relation-specific，还是 answer-value-specific。
4. 追踪 L22:H24 写入后，L23、MLP、readout 是否继续使用同一路径。
5. 对比 head removal、source contribution removal、downstream residual patch 三者是否闭合。
```

如果 Phase752 成立，就可以把图谱从：

```text
单层 source-content path
```

推进到：

```text
source -> writer head -> downstream carrier/rewriter -> readout competition 的自然路径链。
```

## Phase 752：自然写入器稳定性与路径链验证 [2026-06-29 11:25]

### 1. 对上传内容的判断

上传内容的主线基本正确：当前“苹果—水果—属性”图谱已经不能继续只看普通 patch（修补）成功率，而应该把问题推进到自然路径链：

```text
source token（源词元）
-> natural writer head（自然写入注意力头）
-> downstream carrier / rewriter（下游承载器 / 重写器）
-> readout competition（读出竞争）
```

但需要收紧两点：

```text
1. attention mass（注意力质量）只能说明 Q/K pattern（查询/键模式）自然看向哪里，不能单独证明因果写入。
2. source-restricted V/O contribution removal（源限制值/输出贡献移除）更接近因果证据，但仍然只是 head-source path（注意力头-源路径）层级证据，不等于神经元级完整图谱。
```

因此 Phase 751 的方向正确，Phase 752 继续做固定 head（注意力头）跨 object / relation / answer（对象/关系/答案）稳定性验证，是同一阶段性目标内的自然推进。

### 2. 生成脚本

```text
tests/gpt5/phase752_natural_writer_stability_path_chain.py
tests/gpt5/run_phase752_natural_writer_stability_path_chain_round.sh
```

脚本没有使用量化方案。每个模型按顺序运行，并通过 runner 传入 `--hard-exit-after-model`，避免多模型同时占用显存。

### 3. 测试命令

语法检查：

```bash
python -m py_compile tests/gpt5/phase752_natural_writer_stability_path_chain.py
```

冒烟测试：

```bash
tests/gpt5/run_phase752_natural_writer_stability_path_chain_round.sh smoke --max-pairs 1 --top-audits 1 --max-candidates 2 --max-source-groups 3 --top-k-vocab 10 --max-topk-tokens 6 --max-route-classes 4 --log-every 1
```

主测试：

```bash
tests/gpt5/run_phase752_natural_writer_stability_path_chain_round.sh main --max-pairs 8 --include-extended-relations --top-audits 1 --max-candidates 4 --max-source-groups 5 --top-k-vocab 14 --max-topk-tokens 8 --max-route-classes 5 --log-every 2
```

确认测试：

```bash
tests/gpt5/run_phase752_natural_writer_stability_path_chain_round.sh confirm --max-pairs 12 --include-extended-relations --top-audits 1 --max-candidates 4 --max-source-groups 5 --top-k-vocab 16 --max-topk-tokens 10 --max-route-classes 6 --log-every 3
```

### 4. 测试原理

Phase 752 不再动态寻找新的 top head（最高分注意力头），而是固定 Phase 751 找到的候选：

```text
qwen3: L33:H15, L33:H23, L32:H11, L32:H0
GLM4: L35:H29, L34:H4, L34:H9
DS7B: L22:H24, L22:H1, L22:H7, L23:H6
```

对每个候选 head（注意力头）和 source group（源组）执行：

```text
1. 记录自然注意力和 V/O contribution（值/输出贡献）。
2. 只移除该 head 从指定 source group 写入的贡献。
3. 观察 target logit drop（目标词元 logit 下降）、route release（路线释放）、top1 loss（第一名丢失）。
4. 同时记录下游 hidden delta norm（隐藏态扰动范数），判断扰动是否向后传播。
```

判定逻辑：

```text
target drop 高且 support rate 高 -> 更像稳定写入器。
route release 高 -> 更像路线守门 / suppressor（抑制器）参与。
final hidden delta 高 -> 扰动向后传播，但不等于证明完整链路闭合。
```

### 5. 客观结果

确认轮输出：

```text
qwen3: 480 rows
GLM4: 360 rows
DS7B: 480 rows
```

结果文件：

```text
results/glm5_phase752_natural_writer_stability_path_chain/confirm/phase752_cross_model_summary.md
```

qwen3 结果：

```text
最佳候选：natural_donor L32:H11 target_record_line
n=12, relations=6
support rate=0.333
mean target drop=0.062
route guard rate=0.250
final delta=5.809
判断：relation_conditioned_writer
```

qwen3 没有形成稳定全局 writer（写入器），更多是 relation-conditioned（关系条件化）或 answer-value-specific（答案值特异）片段。

GLM4 结果：

```text
最佳候选：natural_donor L35:H29 records_all
n=12, relations=6
support rate=0.167
mean target drop=0.094
route guard rate=0.000
final delta=2.775
判断：weak_or_unstable
```

GLM4 没有出现可称为稳定自然写入器的证据。

DS7B 结果：

```text
natural_recipient L22:H24 records_all
n=12, relations=6
support rate=0.917
mean target drop=0.688
route guard rate=0.500
mean route release=0.516
top1 loss=0.167
final delta=14.240
判断：stable_mixed_writer_guard
```

```text
natural_recipient L22:H24 target_record_line
n=12, relations=6
support rate=0.833
mean target drop=0.594
route guard rate=0.417
mean route release=0.484
final delta=12.786
判断：stable_mixed_writer_guard
```

```text
natural_recipient L22:H1 records_all
n=12, relations=6
support rate=0.750
mean target drop=0.474
route guard rate=0.667
mean route release=0.620
final delta=11.413
判断：stable_mixed_writer_guard
```

```text
natural_donor L22:H24 records_all
n=12, relations=6
support rate=0.750
mean target drop=0.401
route guard rate=0.167
mean route release=0.104
final delta=9.938
判断：stable_target_writer
```

### 6. 阶段性进展

Phase 752 把 Phase 751 的自然注意力头回溯推进了一层：

```text
Phase 751:
某些 head 自然看向 source，而且 source V/O contribution removal 会影响目标输出。

Phase 752:
固定这些 head 后，DS7B L22:H24 和 L22:H1 在扩展 object / relation / answer 条件下仍然稳定有效。
```

最重要的客观现象：

```text
1. DS7B L22:H24 是当前最稳定的自然写入器候选。
2. DS7B L22:H1 更像 target writer + route guard（目标写入器 + 路线守门器）的混合组件。
3. qwen3 的相关 head 更像条件化片段，不是稳定全局写入器。
4. GLM4 在该任务上没有出现强稳定写入器。
5. DS7B 的 final hidden delta 明显更大，说明 L22 写入扰动会向后层传播。
```

### 7. 严格问题和硬伤

本阶段不能过度解释：

```text
1. hidden delta（隐藏态扰动）只证明扰动传播，不证明下游模块正确使用了同一路径。
2. source group（源组）仍然是外部 token span（词元片段）标签，不是模型内部自带的语义单元。
3. DS7B 是小模型，内部结构可能存在偏差，不能直接推广到大模型。
4. qwen3 / GLM4 没有复现 DS7B 的稳定 L22 写入器，说明机制可能模型特异，或者当前任务设计只适合 DS7B。
5. 当前仍然是 head-source path（注意力头-源路径）图谱，不是 neuron-level graph（神经元级图谱）。
```

### 8. 当前理论收紧

当前最稳妥的说法是：

```text
语言生成不是单个语义向量被直接读出，而是由 source-conditioned writer（源条件化写入器）、route guard（路线守门器）、downstream carrier（下游承载器）和 readout competition（读出竞争）共同形成的条件化路径。
```

其中 DS7B 的局部证据已经比较清楚：

```text
source record/value
-> L22:H24 / L22:H1
-> downstream residual perturbation
-> route competition changes
-> final answer logit changes
```

但还不能写成完整闭环：

```text
source -> writer -> exact downstream carrier -> exact rewriter -> exact readout
```

中间的 exact downstream carrier / rewriter（精确下游承载器 / 重写器）还没有被因果定位。

### 9. 下一阶段任务

下一阶段仍然属于同一阶段性目标，但不应该继续扩大 head 列表。应该做 Phase 753：

```text
Downstream Carrier Closure for L22 Writer Path
L22 写入器路径的下游承载闭合验证
```

核心任务：

```text
1. 固定 DS7B L22:H24 和 L22:H1。
2. 移除它们的 source contribution 后，在 L23-L27 的 residual / attn / MLP 位置尝试恢复。
3. 如果某个下游位置能恢复 target logit，同时压回 route release，说明它更可能是 carrier / rewriter。
4. 对比 target_record_line、target_value_tokens、records_all 三种源组，判断下游承载是否依赖源粒度。
5. 不扩大模型范围，先把 DS7B 的自然路径链闭合，再考虑跨模型复现。
```

## Phase 755：跨语义域路线不变量图谱第一版 [2026-06-29 12:17]

### 1. 对上传内容的判断

上传内容中“必须从苹果-水果局部图谱扩展到植物、动物、物品、工具、抽象概念”的判断是正确的。原因不是为了增加复杂度，而是当前苹果-水果图谱只能说明 fruit-domain（水果域）内部的 route competition（路线竞争），无法回答三个关键问题：

```text
1. format / echo / suppression route 是否跨语义域复用。
2. DS7B L22:H24 / L22:H1 是否只是 fruit-domain writer，还是跨域 writer。
3. readout competition 的路线结构是否在不同语义域中保持相似。
```

但上传内容中“Graph Atlas v1.0 已经是语言编码机制全局图谱”的说法需要收紧。目前只能说：

```text
已经可以启动 Cross-Domain Route Invariance Atlas（跨语义域路线不变量图谱）。
不能说已经完成 language graph（语言全图）。
```

本轮 Phase 755 正是把 fruit graph（水果子图）扩展为第一版跨域 route atlas（路线图谱）。

### 2. 生成脚本

```text
tests/gpt5/phase755_cross_domain_route_invariance_atlas.py
tests/gpt5/run_phase755_cross_domain_route_invariance_atlas_round.sh
```

脚本使用 bf16（bfloat16）加载，不使用量化。由于本轮需要 `output_attentions`（输出注意力）和 V/O contribution hook（值/输出贡献钩子），实际使用 eager attention（普通注意力实现）来保证能捕获注意力权重和 V 投影。

### 3. 测试命令

语法检查：

```bash
python -m py_compile tests/gpt5/phase755_cross_domain_route_invariance_atlas.py
```

冒烟测试：

```bash
tests/gpt5/run_phase755_cross_domain_route_invariance_atlas_round.sh smoke --max-pairs 2 --max-candidates 1 --max-source-groups 2 --top-k-vocab 8 --max-topk-tokens 5 --max-route-classes 4 --log-every 1
```

主测试：

```bash
tests/gpt5/run_phase755_cross_domain_route_invariance_atlas_round.sh main --max-pairs 30 --max-candidates 3 --max-source-groups 3 --top-k-vocab 16 --max-topk-tokens 10 --max-route-classes 6 --log-every 5
```

确认测试：

```bash
tests/gpt5/run_phase755_cross_domain_route_invariance_atlas_round.sh confirm --max-pairs 60 --max-candidates 3 --max-source-groups 3 --top-k-vocab 18 --max-topk-tokens 12 --max-route-classes 6 --log-every 10
```

实际有效 pair 数为 58，因为部分 tool / abstract 条件中 conflict value（冲突值）与 explicit value（显式值）相同，被自动跳过。

### 4. 测试原理

测试域：

```text
fruit: apple, banana
animal: cat, bird
plant: oak, rose
object: chair, stone
tool: hammer, knife
abstract: freedom, time
```

关系类型：

```text
category, color, taste, shape, edible, grows_on_tree
```

每个样本构造 explicit_profile（显式事实）和 conflict_profile（冲突事实），但本轮主要看 explicit_profile 下的自然路线结构。

每个样本测试两类现象：

```text
1. route profile（路线轮廓）
   观察 top vocabulary（最高候选词）属于 donor_answer、format_or_schema、echo_object_or_relation 等哪一类。
   计算不同 domain 的 route profile JS divergence（JS散度）。

2. fixed head/source contribution removal（固定注意力头/源贡献移除）
   固定 Phase 751-752 中的候选 head：
   qwen3: L33:H15, L33:H23, L32:H11
   GLM4: L35:H29, L34:H4, L34:H9
   DS7B: L22:H24, L22:H1, L22:H7

   分别移除 target_record_line、target_value_tokens、records_all 的 V/O source contribution。
   观察 target logit drop、route release、top1 loss。
```

判断标准：

```text
support rate 高 + mean target drop 高 + 多 domain 成立
=> cross-domain writer candidate（跨域写入器候选）

route guard rate 高 + route release 高
=> cross-domain route guard / suppressor candidate（跨域路线守门/抑制候选）
```

### 5. 客观结果

确认轮输出：

```text
qwen3: 580 rows, 58 route observations, 522 source removals
GLM4: 580 rows, 58 route observations, 522 source removals
DS7B: 580 rows, 58 route observations, 522 source removals
```

结果文件：

```text
results/glm5_phase755_cross_domain_route_invariance_atlas/confirm/phase755_cross_model_summary.md
```

route profile（路线轮廓）：

```text
qwen3 mean pairwise domain JS = 0.0433
top class counts = donor_answer 45, format_or_schema 13

GLM4 mean pairwise domain JS = 0.0970
top class counts = donor_answer 58

DS7B mean pairwise domain JS = 0.0542
top class counts = donor_answer 34, format_or_schema 18, echo_object_or_relation 3, punctuation_or_stop 2, other_vocab 1
```

这个结果说明：

```text
1. 三个模型都存在跨 domain 的 route profile similarity（路线轮廓相似性）。
2. GLM4 在这个显式事实任务上几乎总是 donor_answer top1，所以看不到明显竞争。
3. DS7B 的 route competition 更复杂，仍出现 format / echo 等竞争路线。
```

qwen3 固定 head/source 移除结果：

```text
最佳候选：L33:H23 records_all
n=58, domains=6
support rate=0.224
mean target drop=0.110
route guard rate=0.241
mean route release=0.108
判断：domain_specific_or_weak
```

GLM4 固定 head/source 移除结果：

```text
最佳候选：L35:H29 records_all
n=58, domains=6
support rate=0.034
mean target drop=0.023
route guard rate=0.069
mean route release=0.055
判断：domain_specific_or_weak
```

DS7B 固定 head/source 移除结果：

```text
L22:H24 records_all
n=58, domains=6
support rate=0.862
mean target drop=0.528
route guard rate=0.310
mean route release=0.200
top1 loss=0.121
判断：cross_domain_writer_candidate
```

```text
L22:H1 records_all
n=58, domains=6
support rate=0.810
mean target drop=0.554
route guard rate=0.328
mean route release=0.189
top1 loss=0.121
判断：cross_domain_writer_candidate
```

```text
L22:H24 target_record_line
n=58, domains=6
support rate=0.810
mean target drop=0.448
route guard rate=0.328
mean route release=0.223
判断：cross_domain_writer_candidate
```

```text
L22:H1 target_record_line
n=58, domains=6
support rate=0.672
mean target drop=0.435
route guard rate=0.379
mean route release=0.204
判断：cross_domain_mixed_writer_guard
```

```text
L22:H24 target_value_tokens
n=58, domains=6
support rate=0.534
mean target drop=0.268
route guard rate=0.379
mean route release=0.218
判断：cross_domain_route_guard_candidate
```

### 6. 阶段性进展

Phase 755 的关键进展是：

```text
DS7B L22:H24 / L22:H1 不只是 fruit-domain writer。
它们在 fruit / animal / plant / object / tool / abstract 六个 domain 上都表现出稳定 target-support effect。
```

这把之前的结论从：

```text
苹果-水果局部路径：
source -> L22:H24/H1 -> route competition
```

推进到：

```text
跨语义域路径：
domain-specific facts -> L22:H24/H1 -> target support / route competition
```

但 qwen3 和 GLM4 没有同等复现。准确说：

```text
跨域稳定 writer 目前是 DS7B-local result（DS7B 局部结果），不是 cross-model invariant（跨模型不变量）。
```

### 7. 严格问题和硬伤

必须谨慎解释：

```text
1. route JS 低只能说明路线轮廓相似，不能单独证明存在真正数学不变量。
2. L22:H24/H1 的跨域效应目前只在 DS7B 强成立，qwen3 / GLM4 不强。
3. 当前 domain 词表仍然很小，abstract domain（抽象域）尤其粗糙。
4. 使用的是显式事实提示，不等价于自然语言知识网络。
5. 仍然是 head-source path（注意力头-源路径）图谱，不是 neuron-level atlas（神经元级图谱）。
6. 本轮没有定位 L22 写入后由哪个 downstream carrier / rewriter 承载。
```

### 8. 理论收紧

上传内容中“扩展到植物-动物-物品是必要的”得到支持。

但是理论应写成：

```text
语言模型中可能存在跨语义域复用的 route competition skeleton（路线竞争骨架）。
DS7B 中 L22:H24 / L22:H1 是目前最强的跨域 writer / guard 候选。
```

而不是：

```text
已经完成 language graph（语言全图）。
```

当前最稳妥的图谱形态：

```text
domain facts
-> source-conditioned writer
-> route competition field
-> route guard / suppressor
-> readout competition
```

其中 DS7B 的强证据是：

```text
records_all / target_record_line
-> L22:H24 / L22:H1
-> target logit drop after removal
-> route release after removal
```

### 9. 下一阶段任务

下一阶段不应该继续盲目扩大 domain，也不应该马上声称全局图谱完成。应该做 Phase 756：

```text
Cross-Domain Writer Control and Downstream Carrier Test
跨域写入器控制组与下游承载验证
```

核心任务：

```text
1. 固定 DS7B L22:H24 / L22:H1。
2. 加入 same-layer random head、L22:H7、L23:H6 作为控制。
3. 对 records_all / target_record_line 做 source removal + downstream residual restore。
4. 检查 L23-L27 哪些位置能恢复 target logit，同时压回 route release。
5. 如果 L22:H24/H1 强于随机头，且存在可恢复的 downstream carrier，才可以把 Phase 755 的跨域 writer 结果写入 Graph Atlas v1。
```

阶段性目标是从：

```text
跨域 writer 候选
```

推进到：

```text
跨域 writer -> downstream carrier 的可验证路径链。
```
