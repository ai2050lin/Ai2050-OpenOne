提问：
如果深度神经网络内部类似齿轮，能不能用暴力计算的方式测试出来齿轮的形状，通过多个齿轮的数据，尝试破解编码机制，现在的方式，全部都是在不停的测量某个局部齿轮的形状，极为低效，而且容易建立错误的因果关系


回答：
可以，而且我认为你这个判断非常关键。

现在的很多实验确实像是在：

```text
拿显微镜反复观察某一个局部齿轮的一个齿，
然后试图从这个齿的形状推断整台机器的传动原理。
```

这种方法的问题是：

```text
1. 极低效；
2. 容易把局部 patch（补丁）成功误认为真实机制；
3. 容易把测量工具制造出的因果关系当成模型内部自然因果；
4. 很难发现多个组件之间的组合关系；
5. 很难识别“同构齿轮”，也就是不同层 / 不同组件执行同一功能角色。
```

所以，应该从“局部齿轮测量”转向：

```text
全局齿轮响应图谱
```

也就是用高通量、结构化暴力计算，系统测量大量组件在大量任务、供体、上下文、输出指标下的响应形状，再从整体响应矩阵里反推出“齿轮类型”和“传动规则”。

---

# 一、你的“齿轮”类比基本正确

如果把深度神经网络内部看成一组齿轮，那么一个“齿轮”不应该被定义为：

```text
某个 neuron（神经元）
某个 attention head（注意力头）
某个 MLP channel（MLP 通道）
某个 residual direction（残差方向）
```

这些只是物理位置。

真正的齿轮应该定义为：

```text
一个条件化状态转移角色。
```

也就是：

```text
在什么输入条件下，
它把状态从哪一种边界类，
推向哪一种边界类，
同时压低哪些 blocker（阻塞者），
增强哪些答案路线，
是否破坏 protocol（协议），
是否引入 echo（回声），
是否能跨任务复用。
```

用公式写，某个组件或子空间 (m) 的“齿轮形状”不是它的权重，而是它的响应函数：

$$
\mathcal{G}_m:
(x,d,c)
\longrightarrow
\Delta \Phi
$$

其中：

```text
x = 任务样本
d = donor condition（供体条件）
c = recipient context（受体上下文）
\Delta \Phi = 输出场变化画像
```

(\Delta \Phi) 不能只包含目标 token logit（目标词元对数几率），而应该包含：

$$
\Delta \Phi
===========

\left(
\Delta B_{\text{boundary}},
\Delta S_{\text{target}},
\Delta S_{\text{contrast}},
\Delta S_{\text{generic}},
\Delta E_{\text{echo}},
\Delta P_{\text{protocol}},
\Delta R_{\text{rank}},
\Delta C_{\text{span}},
\Delta C_{\text{rollout}}
\right)
$$

也就是说，齿轮形状是一个“响应画像”，不是一个单点分数。

---

# 二、为什么现在的方法低效

现在很多测试仍然是在问：

```text
这个 head（注意力头）有没有用？
这个 MLP channel（MLP 通道）有没有用？
这个 patch（补丁）能不能让目标答案上升？
这个方向能不能恢复某个样本？
```

这类问题有用，但它们是局部问题。

真正的问题应该是：

```text
这个组件属于哪一类齿轮？
它和哪些齿轮同构？
它在什么 donor / context（供体 / 上下文）下启用？
它和哪些齿轮相容？
它和哪些齿轮会互相干扰？
它改变的是 target route（目标路线）、contrast route（对照路线）、echo route（回声路线）、protocol route（协议路线），还是 blocker field（阻塞者场）？
```

你前面阶段已经反复遇到这个问题。例如 Phase 832 已经证明，route boundary（路线边界）比 readout-alignment（读出对齐）更强，但仍然依赖行为标签，不是内生机制闭合；Phase 834 又证明 qwen3（通义千问三模型）可以用 first-step blocker profile（首步阻塞者画像）过滤误激活，而 DS7B（DeepSeek 七十亿蒸馏模型）不遵守同一个 rank-improvement（排名改善）规则。  

这正说明：继续局部测某个“齿”的形状，已经不够了。

---

# 三、能不能用暴力计算测试齿轮形状？

可以，但不是暴力穷举所有参数。

真正可行的是：

```text
结构化暴力计算
+
全局响应图谱
+
稀疏因果辨识
+
跨任务预测验证
```

不能做的是：

```text
枚举所有 neuron / head / channel / layer 组合。
```

因为组合数量爆炸。

应该做的是：

```text
用大量标准化扰动，测量大量组件的响应画像；
再用压缩、聚类、分解、预测任务去恢复齿轮类型。
```

---

# 四、齿轮形状应该怎么测

## 1. 建立统一响应向量

对每个组件或子空间 (m)，在任务 (x)、供体 (d)、上下文 (c) 下做标准化干预，记录响应：

$$
R_m(x,d,c)
==========

\Phi
\left(
f(h+\Delta h_m)
\right)
-------

\Phi
\left(
f(h)
\right)
$$

其中：

```text
\Phi = 输出场画像函数
```

(\Phi) 至少包括：

$$
\Phi
====

\left(
B_{\text{boundary}},
S_{\text{target}},
S_{\text{contrast}},
S_{\text{generic}},
E_{\text{echo}},
P_{\text{protocol}},
C_{\text{span}},
C_{\text{rollout}},
V_{\text{full-vocab}}
\right)
$$

这样，每个组件不再被一个 logit（对数几率）描述，而是被一个高维响应指纹描述。

---

## 2. 构造全局响应张量

如果有：

```text
m = 组件 / 子空间
t = 任务样本
d = 供体条件
k = 输出指标
```

可以构造一个响应张量：

$$
\mathcal{X}_{m,t,d,k}
=====================

R_{m,k}(x_t,d)
$$

这就是“齿轮响应图谱”。

然后做分解：

$$
\mathcal{X}
\approx
\sum_{r=1}^{R}
A_{m,r}
B_{t,r}
C_{d,r}
D_{k,r}
$$

其中 (r) 是潜在齿轮类型。

这个分解的意义是：

```text
A_{m,r}: 哪些组件属于同一种齿轮；
B_{t,r}: 这种齿轮在哪些任务中起作用；
C_{d,r}: 它依赖哪些 donor condition（供体条件）；
D_{k,r}: 它主要改变哪些输出指标。
```

这比单个 patch（补丁）强得多，因为它直接寻找“齿轮类型”，而不是盯着一个局部组件。

---

## 3. 用随机组合做因果组测

不要枚举所有 pair（组件对）和 triple（三组件组合）。应该用随机组合测试：

$$
Y
=

\sum_i
\beta_i x_i
+
\sum_{i<j}
\beta_{ij}x_ix_j
+
\epsilon
$$

其中：

```text
x_i = 第 i 个组件是否被启用；
\beta_i = 单组件主效应；
\beta_{ij} = 两组件交互效应；
Y = 输出场响应，例如 boundary transition（边界迁移）或 global atlas score（全局图谱分数）。
```

这样可以通过少量随机组合估计：

```text
哪些组件是主齿轮；
哪些组件只在组合中有用；
哪些组件互相干扰；
哪些组件是门控齿轮；
哪些组件是阻塞者抑制齿轮。
```

这才是“暴力计算”的正确方式。

---

# 五、暴力计算应该测什么，不应该测什么

## 不应该只测

```text
target logit 是否上升；
目标 token rank 是否改善；
某个 patch 是否成功；
某个 head 是否单独有效。
```

这些指标太局部。

## 应该测

```text
1. boundary class transition（边界类迁移）
2. blocker class profile（阻塞者类别画像）
3. target route / contrast route / echo route / protocol route 的变化
4. first-token profile（首词元画像）
5. span profile（片段画像）
6. rollout profile（自然展开画像）
7. donor-specific response（供体特异响应）
8. cross-component interference（跨组件干扰）
9. gate predictability（门控可预测性）
10. cross-case reuse（跨样本复用）
```

---

# 六、为什么这比现在的方法更接近破解编码机制

现在的方法主要在做：

```text
局部因果显微镜。
```

它回答：

```text
这个地方是不是有用？
```

但“齿轮图谱法”回答的是：

```text
模型内部有哪些齿轮类型？
这些齿轮如何组合？
哪些齿轮跨模型复用？
哪些齿轮只是小模型伪机制？
哪些齿轮对应语言的知识、推理、语法？
```

真正的目标不是证明某个 head（注意力头）有用，而是建立：

$$
G_{\text{gear}}
===============

\left(
V_{\text{gear}},
E_{\text{interaction}},
T_{\text{condition}},
R_{\text{response}},
C_{\text{closure}}
\right)
$$

其中：

```text
V_gear = 齿轮类型节点；
E_interaction = 齿轮交互边；
T_condition = 启用条件；
R_response = 响应画像；
C_closure = 闭合能力。
```

---

# 七、怎样避免建立错误因果关系

你指出的“容易建立错误因果关系”非常重要。

局部 patch（补丁）经常只能证明：

```text
在人工供体和人工受体设置下，
这个组件能造成某个输出变化。
```

但这不等于：

```text
模型自然运行时使用这个组件完成该功能。
```

所以必须把因果证据分级。

## 最低级因果

```text
patch 后输出变了。
```

这只能说明可干预，不说明自然机制。

## 中级因果

```text
同一组件在多个任务 / 供体 / 模型上产生相似响应画像。
```

说明它可能是一类齿轮。

## 高级因果

```text
从响应图谱中预测一个未测试任务中的组件作用，并验证成功。
```

这才是机制解释。

## 最高级因果

```text
找到最小充分齿轮组合；
去掉它失效；
加入它恢复；
跨任务 / 跨模型 / 跨提示成立。
```

这才接近破解。

---

# 八、应该怎么设计“暴力齿轮图谱”实验

我建议下一阶段不要继续只做 echo-route（回声路线）局部拆解，而是把它纳入一个更大的全局齿轮图谱实验。

## Phase 837：全局齿轮响应图谱试点

英文名：

```text
Global Gear Response Atlas Pilot（全局齿轮响应图谱试点）
```

---

## 目标

从局部 patch（补丁）转向系统辨识：

```text
用大规模标准化扰动，测出多个组件的响应指纹；
通过响应指纹聚类和组合测试，反推出齿轮类型和交互规则。
```

---

## 任务 1：定义齿轮响应指纹

对每个组件 (m)，记录：

$$
F_m(x,d,c)
==========

\left(
\Delta B,
\Delta S_{\text{target}},
\Delta S_{\text{contrast}},
\Delta S_{\text{generic}},
\Delta E_{\text{echo}},
\Delta P_{\text{protocol}},
\Delta C_{\text{span}},
\Delta C_{\text{rollout}},
\Delta V_{\text{full-vocab}}
\right)
$$

---

## 任务 2：扩大样本维度

不能只测：

```text
heart -> body organ
triangle -> geometric shape
```

要覆盖：

```text
body organ；
geometric shape；
animal；
plant；
tool；
color；
material；
season；
profession；
function；
part-whole；
yes/no；
multi-token phrase；
自由生成。
```

---

## 任务 3：扩大组件维度

每个模型统一抽样：

```text
layer residual（层残差）；
attention output（注意力输出）；
attention head（注意力头）；
MLP output（MLP 输出）；
MLP channel group（MLP 通道组）；
top-difference subspace（最大差异子空间）；
random matched subspace（随机匹配子空间）。
```

---

## 任务 4：标准化输出指标

每次干预统一记录：

```text
1. target token rank；
2. answer span score；
3. contrast span score；
4. generic blocker score；
5. echo score；
6. protocol validity；
7. boundary transition；
8. full-vocabulary top-k blocker classes；
9. rollout output；
10. degradation score。
```

---

## 任务 5：响应图谱分解

构造：

$$
\mathcal{X}_{m,t,d,k}
=====================

R_{m,k}(x_t,d)
$$

做低秩分解、聚类和稀疏因果建模，得到齿轮类型：

```text
target writer（目标写入齿轮）
contrast suppressor（对照抑制齿轮）
echo suppressor（回声抑制齿轮）
protocol gate（协议门控齿轮）
category refiner（类别精化齿轮）
boundary stabilizer（边界稳定齿轮）
harmful mixer（有害混合齿轮）
generic blocker reducer（通用阻塞降低齿轮）
```

---

## 任务 6：随机组合组测

用随机组合估计交互：

$$
Y
=

\sum_i
\beta_i x_i
+
\sum_{i<j}
\beta_{ij}x_ix_j
+
\epsilon
$$

重点找：

```text
协同齿轮；
互斥齿轮；
门控齿轮；
条件依赖齿轮；
跨任务复用齿轮。
```

---

## 任务 7：前瞻预测验证

这是最关键的。

从已测图谱预测：

```text
某个未测试 case 中，
哪个组件应该是 contrast suppressor；
哪个组件应该引入 echo；
哪个组合应该退化；
哪个组合应该恢复 boundary。
```

然后验证。

如果预测成功，说明图谱有解释力。

---

# 九、这个方案如何对应你说的“齿轮形状”

齿轮形状不是静态几何，而是响应曲面：

$$
Shape(m)
========

\left{
F_m(x,d,c):
x\in \mathcal{X},
d\in \mathcal{D},
c\in \mathcal{C}
\right}
$$

两个齿轮同型，不要求它们在同一层，也不要求它们权重相似，只要求：

$$
Shape(m_1)
\approx
Shape(m_2)
$$

也就是说：

```text
它们在不同任务、供体、上下文下产生相似响应画像。
```

这才是机制同构。

---

# 十、当前理论公式如何改

现有局部公式通常是：

$$
g_i(d,c)
========

\Omega
\left(
a_i,
R_i,
Q_d,
B_i
\right)
$$

这仍然是局部门控公式。

全局齿轮图谱应该写成：

$$
\mathcal{M}(x)
==============

\operatorname{Closure}
\left[
h_0(x)
+
\sum_{m\in\mathcal{G}}
g_m(x)
\cdot
\mathcal{T}_m
\left(
h(x)
\right)
\right]
$$

其中：

```text
\mathcal{G} = 齿轮集合；
g_m(x) = 当前输入下该齿轮是否启用；
\mathcal{T}_m = 齿轮状态转移算子；
Closure = 输出闭合过程。
```

更完整地写：

$$
y
=

\operatorname{Readout}
\left(
h_R
+
\sum_m
g_m(x,d,c)
\cdot
\mathcal{T}_m(h_R,h_D)
\right)
$$

而每个齿轮的真实定义来自它的响应画像：

$$
\mathcal{T}_m
\sim
Shape(m)
========

\mathbb{E}_{x,d,c}
\left[
\Delta \Phi_m(x,d,c)
\right]
$$

---

# 十一、当前研究应该如何转向

我建议分成两条线。

## 线一：保留局部链条，但降级为验证工具

比如 Phase 837 echo-route false positive decomposition（回声路线误激活拆解）仍然有价值，但它不应继续作为主线。

它应该成为全局图谱中的一个齿轮类型：

```text
echo suppressor（回声抑制齿轮）
```

---

## 线二：主线转向全局齿轮响应图谱

也就是：

```text
不再问某个局部齿轮是否能解释某个 case；
而是问所有齿轮响应画像能否预测未见 case。
```

这是从：

```text
局部因果解释
```

升级到：

```text
系统辨识。
```

---

# 十二、最终回答

可以用“暴力计算”测试齿轮形状，而且这是下一步更正确的方向。

但要注意：

```text
不是暴力枚举参数；
不是暴力测所有 pair；
不是继续局部 patch；
而是大规模、标准化、多任务、多供体、多指标的响应图谱采集。
```

真正要破解编码机制，应该把研究目标从：

```text
找某个组件的因果作用
```

改成：

```text
恢复模型内部齿轮类型、齿轮交互、齿轮启用条件和闭合规则。
```

最关键的转向是：

```text
从局部 patch 成功
转向
全局响应图谱预测能力。
```

如果一个“齿轮图谱”能预测未见任务、未见 donor、未见组合中的 boundary transition（边界迁移）、blocker profile（阻塞者画像）、echo route（回声路线）、protocol validity（协议有效性）和 rollout（展开生成），那才说明我们真正接近了语言编码机制。

---

# 十三、Phase 849-850 后的图谱修正

Phase 849 到 Phase 850 之后，当前全局齿轮图谱需要从“响应画像表”继续升级为“带证据等级的机制候选图谱”。

最重要的修正是：

```text
不能只记录某个齿轮对平均 residual（残差）的拟合效果；
必须单独记录 strong-edge（强交互边）是否能在 object_holdout / prompt_holdout 中被预测。
```

原因是 Phase 849 已经显示：

```text
qwen3:
  residual_projection gate 可以改善平均误差，也能改善一部分 strong-edge。

DS7B:
  blocker_field gate 有弱正结果，但跨对象 / 跨提示泛化不稳。

GLM4:
  当前测试集几乎全是 additive（可加性）样本，无法评价 strong-edge gate。
```

Phase 850 进一步把评价对象从平均误差改成 strong-edge precision / recall / F1（强边精确率 / 召回率 / F1）。

在 confirm（确认）轮中：

```text
qwen3:
  in_sample strong F1:
    global_combo = 0.4688
    residual_projection_combo = 0.8923

  object_holdout strong F1:
    global_combo = 0.3947
    residual_projection_combo = 0.5313

  prompt_holdout strong F1:
    global_combo = 0.4054
    residual_projection_combo = 0.4839

DS7B:
  in_sample blocker_field strong F1 = 1.0000
  object_holdout blocker_field strong F1 = 0.0000
  prompt_holdout blocker_field strong F1 = 0.0000

GLM4:
  strong rows = 0
```

这说明当前最稳的结论不是“已经找到全局齿轮闭合”，而是：

```text
qwen3 的 residual projection 更接近可泛化的 strong-edge gate；
DS7B 的 blocker field 目前更像样本内局部信号；
GLM4 需要先构造能产生非 additive interaction 的任务集。
```

因此，全局齿轮图谱应新增一个强边证据层：

$$
L_{\text{strong}}(g)
=
\left(
F1_{\text{in}},
F1_{\text{object}},
F1_{\text{prompt}},
Recall_{\text{object}},
Recall_{\text{prompt}}
\right)
$$

其中：

```text
g = 某个齿轮或门控候选
F1_in = 样本内强边 F1
F1_object = 跨对象强边 F1
F1_prompt = 跨提示强边 F1
```

一个齿轮候选不能只因为样本内 F1 高就进入核心图谱，至少要满足：

$$
F1_{\text{object}}(g) > F1_{\text{global}}(g)
\quad
\text{and}
\quad
F1_{\text{prompt}}(g) > F1_{\text{global}}(g)
$$

这条标准会把很多“局部拟合齿轮”挡在核心图谱之外。

---

# 十四、历史研究记录并入后的全局齿轮图谱

综合前面阶段，当前齿轮图谱应包含五类节点、四类边、七级证据。

## 1. 五类节点

```text
state node（状态节点）:
  residual state / hidden state / readout state。

route node（路线节点）:
  answer route / contrast route / echo route / protocol route / blocker route。

gear node（齿轮节点）:
  head / MLP / residual subspace / signed channel / projection direction。

gate node（门控节点）:
  boundary gate / residual projection gate / blocker-field gate / format gate。

boundary node（边界节点）:
  semantic boundary / protocol boundary / rank boundary / exact-natural boundary。
```

## 2. 四类边

```text
causal edge（因果边）:
  干预后稳定改变目标指标。

interaction edge（交互边）:
  两个齿轮组合后出现 synergy（协同）或 antagonistic（拮抗）。

transport edge（传输边）:
  信息从 source writer（源写入器）传到 carrier（承载器）再传到 readout（读出）。

closure edge（闭合边）:
  从内部状态变化闭合到 token / class / span / protocol / rollout。
```

## 3. 七级证据

```text
L0: activation correlation（激活相关）
L1: logit effect（目标词元影响）
L2: blocker effect（阻塞者影响）
L3: route boundary effect（路线边界影响）
L4: holdout prediction（跨对象 / 跨提示预测）
L5: strong-edge prediction（强交互边预测）
L6: exact-natural consistency（精确生成与自然生成一致）
L7: rollout closure（展开生成闭合）
```

当前 Phase 850 把 qwen3 的 residual_projection gate 推进到了接近 L5 的位置，但还没有达到 L6 / L7。

DS7B 的 blocker_field gate 目前只能算 L3-L4 的候选，因为 strong-edge 在 holdout 中没有稳定成立。

GLM4 目前缺少 strong-edge 样本，不能直接比较。

## 4. 修正后的全局图谱公式

原来的响应张量：

$$
\mathcal{X}_{m,t,d,k}
=
R_{m,k}(x_t,d)
$$

现在需要扩展为带证据等级和强边标签的图谱：

$$
\mathcal{G}_{\text{gear}}
=
\left(
V_{\text{state}},
V_{\text{route}},
V_{\text{gear}},
V_{\text{gate}},
V_{\text{boundary}},
E_{\text{causal}},
E_{\text{interaction}},
E_{\text{transport}},
E_{\text{closure}},
\Omega_{\text{evidence}}
\right)
$$

其中：

$$
\Omega_{\text{evidence}}(e)
=
\left(
L_0(e),L_1(e),L_2(e),L_3(e),L_4(e),L_5(e),L_6(e),L_7(e)
\right)
$$

强交互边定义为：

$$
c_{\text{strong}}(x,g)
=
\begin{cases}
1, & |R_g(x)| \ge \tau \\
0, & |R_g(x)| < \tau
\end{cases}
$$

门控候选的强边预测得分为：

$$
Score_{\text{strong}}(g)
=
\lambda_1 F1_{\text{object}}(g)
+
\lambda_2 F1_{\text{prompt}}(g)
+
\lambda_3 Recall_{\text{object}}(g)
+
\lambda_4 Recall_{\text{prompt}}(g)
$$

这比只看平均 MAE（平均绝对误差）更接近机制图谱，因为语言生成失败经常不是平均误差问题，而是少数强交互边、阻塞者和路线边界没有被正确预测。

## 5. 下一步图谱改进方向

下一步不应该继续只做单个 patch（补丁），而应该做：

```text
Phase 851:
Strong-edge data expansion and full-vocabulary blocker atlas
（强交互边数据扩展与全词表阻塞者图谱）
```

核心目标：

```text
1. 为 qwen3 扩大 strong-edge 样本，验证 residual projection 是否稳定；
2. 为 DS7B 增加强边样本，区分 blocker_field 是真实门控还是样本内拟合；
3. 为 GLM4 构造更困难任务，先制造可评价的非 additive interaction；
4. 把评估从 target token 推向 full-vocabulary blocker field；
5. 把图谱节点从“组件响应”升级为“路线边界和闭合证据”。
```

如果 Phase 851 成功，全局齿轮图谱才会从：

```text
局部响应图谱
```

推进到：

```text
可预测未见强边的机制候选图谱
```

---

# 十五、Phase 851：图谱结构审计后的进一步修正

Phase 851 没有进行新的模型前向测试，而是把 Phase 849 的 feature rows（特征行）和 Phase 850 的 strong-edge（强边）结果转成机器可读的 atlas（图谱）审计。

新增结果位置：

```text
tests/result/phase851_global_atlas_schema_orthogonality_audit/
```

它回答三个问题：

```text
1. 哪些 gate（门控）真的能进入证据图谱？
2. 哪些变量更像 protocol（协议）、semantic（语义）或 entangled（纠缠）变量？
3. 哪些 gear（齿轮）可以作为 counterfactual min-cut（反事实最小割）预候选？
```

## 1. 证据等级修正

confirm（确认）轮中，qwen3 的证据等级变为：

```text
residual_projection_combo:
  L5_strong_edge_holdout_candidate

internal_strength_combo:
  L5_strong_edge_holdout_candidate
  但 object / prompt holdout F1 只略高于 global，强度弱于 residual_projection。

blocker_field_combo:
  L4_partial_holdout_candidate

global_combo:
  L2_weak_strong_edge_signal
```

DS7B：

```text
blocker_field_combo:
  L3_in_sample_only

residual_projection_combo:
  L3_in_sample_only
```

GLM4：

```text
所有 strong-edge gate:
  L0_untriggered
```

所以当前全局图谱应明确区分：

```text
核心候选:
  qwen3 residual_projection_combo

弱核心候选:
  qwen3 internal_strength_combo

部分候选:
  qwen3 blocker_field_combo

样本内候选:
  DS7B blocker_field_combo

不可评价:
  GLM4
```

## 2. 分层门控假设的可检验版本

上传内容提出的 hierarchical gating hypothesis（分层门控假设）方向正确，但不能直接写成一个大函数：

$$
\alpha_j(x)
=
\Psi_{\text{micro}}
\left(
\Psi_{\text{meso}}
\left(
\Psi_{\text{macro}}(x)
\right)
\right)
$$

更稳妥的写法是把它拆成可测的三层：

$$
\alpha_j(x)
=
G_{\text{micro}}
\left(
h(x),
G_{\text{meso}}(x),
G_{\text{macro}}(x)
\right)
$$

其中：

```text
G_macro:
  prompt_variant / task_type / protocol context。

G_meso:
  sign_pattern / combo_type / edit_mode / gear_count。

G_micro:
  residual_projection / blocker_pressure / route_gap / topk_entropy。
```

Phase 851 的正交性审计给出一个初步证据：

```text
qwen3 protocol-like features:
  resid_target_blocker_span
  topk_entropy
  best_target_object_resid_final
  original_margin

qwen3 semantic-like features:
  best_target_blocker_resid_final
  neg_count
  signed_sum
  signed_mean

qwen3 entangled features:
  blocker_pressure
  route_gap
  target_minus_blocker_logit
```

这说明：

```text
protocol（协议）和 semantic（语义）不是完全混在一起；
但 blocker / route-gap 类变量明显纠缠。
```

因此下一版 gate 不应直接训练一个黑盒 \(\Psi\)，而应先分层：

```text
先分 protocol / semantic / entangled；
再在各层内部找 micro gate。
```

## 3. 协议-语义正交性审计公式

对任意特征 \(z\)，用 object（对象）和 prompt_variant（提示变体）分别计算组间解释率：

$$
\eta^2_{\text{object}}(z)
=
\frac{
\sum_o n_o(\bar{z}_o-\bar{z})^2
}{
\sum_i(z_i-\bar{z})^2
}
$$

$$
\eta^2_{\text{prompt}}(z)
=
\frac{
\sum_p n_p(\bar{z}_p-\bar{z})^2
}{
\sum_i(z_i-\bar{z})^2
}
$$

分类规则：

```text
prompt_eta >> object_eta:
  protocol-like

object_eta >> prompt_eta:
  semantic-like

两者都高:
  entangled_or_shared

两者都低:
  low_axis_signal
```

注意：

```text
这不是最终正交分解；
只是低成本审计，用来避免把协议齿轮误判为语义齿轮。
```

## 4. 反事实最小割的低成本预筛

真正的 counterfactual min-cut（反事实最小割）需要新的 ablation（消融）实验。

Phase 851 只能做预筛：

$$
Lift(g)
=
\frac{
P(\text{strong}\mid g\in S)
}{
P(\text{strong})
}
$$

confirm 轮预候选：

```text
qwen3:
  L29C1532
    total = 270
    strong = 30
    lift = 1.6667

  L27C2767
    total = 180
    strong = 20
    lift = 1.6667

DS7B:
  L27C2295
    total = 180
    strong = 3
    lift = 3.0000
    但 strong 总数太少，只能视为低置信候选。

GLM4:
  无 strong-edge，无法产生最小割候选。
```

因此，真正的下一步不是立刻声称找到 min-cut，而是：

```text
对这些候选齿轮做新的反事实 ablation / restoration；
检查切断它们是否能改变 strong-edge / blocker field / exact-natural consistency。
```

## 5. 图谱升级后的准入规则

一个节点进入核心 atlas（图谱）至少要满足：

```text
1. 有明确 node type:
   state / route / gear / gate / boundary。

2. 有明确 edge type:
   causal / interaction / transport / closure。

3. 有 holdout 证据:
   object_holdout 和 prompt_holdout 至少同时优于 global baseline。

4. 有 strong-edge 证据:
   不能只降低平均 MAE。

5. 没有明显 protocol-semantic 混淆:
   或者被标记为 entangled 高风险节点。
```

这能防止图谱继续堆积“看起来有用但不可泛化”的局部组件。

## 6. 下一步是否属于同一阶段

Phase 851 已经完成了当前阶段的结构审计目标：

```text
把战略升级方案转成可运行的 atlas schema audit。
```

下一步仍属于同一个大方向：

```text
全局齿轮图谱
```

但已经是新的模型前向阶段：

```text
Phase 852:
Strong-edge expansion forward test
（强交互边扩展前向测试）
```

Phase 852 的目标应是：

```text
1. 扩大 qwen3 strong-edge 样本；
2. 为 DS7B 增加强边留出样本；
3. 为 GLM4 构造能触发非 additive interaction 的任务；
4. 对 qwen3 的 L29C1532 / L27C2767 做反事实最小割验证；
5. 把 full-vocabulary blocker field 纳入同一输出画像。
```

# Phase 853：strong-edge expansion and natural closure validation（强交互边扩展与自然闭合验证）

时间：2026-07-02 16:58

## 1. 本阶段任务

Phase 851 / 852 已经完成结构升级和理论整合，但没有新增模型前向数据。

本阶段回到模型测试，目标是验证：

```text
1. 扩大 gear set / combo set 后，strong-edge 是否显著增加；
2. qwen3 residual_projection gate 是否还能在扩展集上保持 holdout 优势；
3. GLM4 是否只是未触发，扩大组合后是否出现 strong-edge；
4. DS7B blocker_field 是否仍只是样本内捷径；
5. strong-edge 是否能推进到 exact-natural consistency / natural closure。
```

新增脚本：

```text
tests/glm5/phase853_strong_edge_expansion_natural_closure_validation.py
tests/glm5/run_phase853_strong_edge_expansion_natural_closure_validation_round.sh
```

结果目录：

```text
tests/result/phase853_strong_edge_expansion_natural_closure_validation/
```

三轮测试：

```text
smoke:
  1 object / 1 prompt / top4 gears

main:
  5 objects / 3 prompts / top8 gears / 18 pairs / 8 triplets

confirm:
  5 objects / 3 prompts / top10 gears / 24 pairs / 12 triplets
```

模型顺序：

```text
qwen3 -> GLM4 -> DS7B
```

加载方式：

```text
bf16
quantization off
flash_attention_2 优先，环境无 FlashAttention2 时自动回退 sdpa
```

## 2. 测试原理

本阶段不是只复用旧表，而是重新加载模型进行 forward pass。

每个模型执行：

```text
1. 读取 Phase 844 top gears。
2. 加入 Phase 851 counterfactual min-cut pre-candidates。
3. 构造 single / pair / triplet / focus combo。
4. 对每个 object / prompt 做原始输出、单齿轮干预、组合齿轮干预。
5. 计算组合交互残差：
```

```text
R_S(x)
  =
Delta_S(x)
  -
sum_{g in S} Delta_g(x)
```

其中：

```text
Delta_S(x):
  组合齿轮干预后的 target-object margin delta。

Delta_g(x):
  单齿轮干预后的 target-object margin delta。
```

strong-edge 定义：

```text
R_S(x) >= 0.5:
  synergy

R_S(x) <= -0.5:
  antagonistic

否则:
  additive
```

同时计算自然闭合：

```text
exact_natural_consistency
  =
target_transition
and
best_target_rank == 1
```

也就是说，strong-edge 只有同时推动自然输出成为目标类，并且首词元目标排名第一，才算接近自然闭合。

## 3. confirm 轮核心结果

```text
qwen3:
  rows = 1395
  interaction rows = 1080
  strong rows = 62
  classes = additive 1018 / synergy 33 / antagonistic 29
  strong rows 中 target_equivalent = 38
  exact-natural strong = 0

GLM4:
  rows = 1395
  interaction rows = 1080
  strong rows = 1
  classes = additive 1079 / antagonistic 1
  exact-natural strong = 0

DS7B:
  rows = 1425
  interaction rows = 1110
  strong rows = 8
  classes = additive 1102 / synergy 8
  strong rows 中 target_equivalent = 5
  exact-natural strong = 0
```

和 Phase 850 对比：

```text
qwen3 strong rows:
  34 -> 62

GLM4 strong rows:
  0 -> 1

DS7B strong rows:
  3 -> 8
```

说明：

```text
扩展 gear / combo 确实能增加 strong-edge；
但仍没有把 strong-edge 推到 exact-natural closure。
```

## 4. qwen3 gate 结果收紧

confirm 轮 qwen3：

```text
global_combo:
  in_sample F1 = 0.4098
  object_holdout F1 = 0.3151
  prompt_holdout F1 = 0.3684
  object balanced F1 = 0.5227
  prompt balanced F1 = 0.6022

residual_projection_combo:
  in_sample F1 = 0.8500
  object_holdout F1 = 0.4167
  prompt_holdout F1 = 0.4615
  object balanced F1 = 0.5376
  prompt balanced F1 = 0.5581
```

判断：

```text
raw object / prompt holdout 仍优于 global；
但 balanced prompt holdout 低于 global。
```

因此，Phase 850-851 的 qwen3 residual_projection L5 判断需要收紧：

```text
旧 confirm 集：
  可称为 L5 strong-edge holdout candidate。

Phase 853 扩展集：
  更稳妥称为 L4 partial holdout candidate。
```

这不是推翻 qwen3 residual_projection，而是说明：

```text
它是强候选；
但还不是稳定跨扩展集、跨平衡集的闭合 gate。
```

## 5. GLM4 和 DS7B 结果

GLM4：

```text
confirm strong rows = 1 / 1080
object_holdout F1 = 0
prompt_holdout F1 = 0
exact-natural = 0
```

解释：

```text
扩大组合后 GLM4 终于出现 1 条 strong-edge，
但数量仍太少，不能进入机制判断。
当前仍应视为 weakly triggered / under-triggered。
```

DS7B：

```text
confirm strong rows = 8 / 1110
object_holdout F1 = 0
prompt_holdout F1 = 0
exact-natural = 0
```

解释：

```text
DS7B strong-edge 数量从 3 增加到 8，
但 blocker_field / train_selected 在样本内强，holdout 仍失败。
这继续支持“DS7B blocker_field 是样本内捷径或小模型压缩结构”的判断。
```

## 6. 最重要结论

Phase 853 的结果是：

```text
strong-edge 可以被扩展出来；
但 strong-edge 仍然不能保证自然生成闭合。
```

更严格地说：

```text
当前找到的是：
  条件齿轮边对边界场的局部变形能力。

还没有找到：
  能清空 full-vocabulary blocker field 并通过 exact-natural boundary 的闭合机制。
```

这说明全局齿轮图谱路线仍然正确，但必须继续向：

```text
full-vocabulary blocker field
answer-class closure
span closure
rollout closure
```

推进。

## 7. 当前硬伤

```text
1. qwen3 strong rows 虽从 34 增加到 62，但未达到 3 倍扩展目标。
2. qwen3 residual_projection 在扩展集上不再满足严格 L5。
3. GLM4 仍几乎未触发 strong-edge。
4. DS7B holdout 继续失败。
5. 三模型 exact-natural strong 全部为 0。
6. 当前 blocker 只做了 proxy，尚未做完整 full-vocabulary blocker field 清空验证。
7. Phase 851 min-cut 候选已被纳入 focus combo，但还没有做真正 do-style ablation / restoration。
```

## 8. 下一阶段

下一阶段仍属于全局齿轮图谱大阶段，但任务必须继续收紧：

```text
Phase 854:
Full-Vocabulary Blocker Field and Min-Cut Causal Validation
```

核心任务：

```text
1. 对 qwen3 的 strong-edge rows 做 full-vocabulary blocker field 分解。
2. 只保留 strong-edge 中 target_equivalent 但 exact-natural 失败的样本。
3. 找出阻止 exact-natural 的 top blocker / format blocker / object echo blocker。
4. 对 qwen3 L29C1532 / L27C2767 做 do-style ablation / restoration。
5. 判断 min-cut 候选是否能改变 blocker field，而不是只改变 residual。
6. GLM4 需要另设更困难任务触发 strong-edge，不宜继续在当前 geometry route 上堆组合。
7. DS7B 需要区分 blocker_field 样本内捷径和真实因果门控。
```

# Phase 854：全词表阻塞场与最小割因果验证

时间：2026-07-02 17:28

## 1. 本阶段任务

Phase 854 接续 Phase 853，不再继续扩大 strong-edge 数量，而是追问：

```text
Phase 853 的 strong-edge 为什么 exact-natural strong 全部为 0？
是目标类没有赢得首词元竞争，
还是 strict token 标准过窄、rollout 尚未闭合？
```

因此本阶段建立了新的跨模型脚本：

```text
tests/glm5/phase854_full_vocab_blocker_min_cut_validation.py
tests/glm5/run_phase854_full_vocab_blocker_min_cut_validation_round.sh
```

结果保存到：

```text
tests/result/phase854_full_vocab_blocker_min_cut_validation/
```

模型仍按 qwen3 -> GLM4 -> DS7B 顺序运行，bf16，未使用量化；FlashAttention2 不可用时自动回退到 sdpa。

## 2. 测试原理

对 Phase 853 的 interaction rows 分三类抽样：

```text
strong_target_not_exact:
  strong-edge 且 target_equivalent，但 exact-natural 失败。

strong_non_target_or_unknown:
  strong-edge 但自然边界仍不是目标类。

additive_control:
  非 strong-edge 对照行。
```

对每个 source row 计算以下条件：

```text
original
full_combo
candidate_only
without_candidate
```

其中 without_candidate 是 leave-one-gear min-cut 检查：

```text
如果 full_combo 清空 blocker，
但去掉某个 candidate 后 blocker 重新出现，
则该 candidate 暂记为 necessary_blocker_reducer。
```

## 3. 新增闭合分层

Phase 854 发现 exact-natural=0 不能只按“完全失败”理解，必须拆成三层：

```text
strict token closure:
  canonical target first token 排名第一。

answer-class closure:
  Geometry / Shape / Polygon / geometric / polygon 等答案类别名集合中至少一个 token
  压过所有非答案类 token。

rollout closure:
  自然生成片段和协议边界也正确。
```

核心公式为：

```text
C_answer-class(x)
  =
1[
  max_{t in A(y)} z_t(x)
  >
  max_{u notin A(y)} z_u(x)
]
```

其中 A(y) 是目标答案类的首词元别名集合。

全词表 blocker 定义为：

```text
B_full(x)
  =
{
  u notin A(y):
  z_u(x) > max_{t in A(y)} z_t(x)
}
```

答案类 blocker 清空为：

```text
C_full-blocker(x)
  =
1[B_full(x)=empty]
```

这比 Phase 853 的 strict exact-natural 更合理，因为模型经常输出：

```text
Geometry
Shape
Polygon
polygon
shape
```

这些在语义上属于目标答案类，但不一定等于 canonical first token。

## 4. confirm 轮核心结果

```text
qwen3:
  source rows = 60
  full combo rows = 60
  answer-class closure = 42
  strict token closure = 0
  improved class blockers = 11
  worsened class blockers = 18
  necessary blocker reducer = 10

GLM4:
  source rows = 13
  full combo rows = 13
  answer-class closure = 12
  strict token closure = 0
  improved class blockers = 1
  worsened class blockers = 0
  necessary blocker reducer = 2

DS7B:
  source rows = 20
  full combo rows = 20
  answer-class closure = 17
  strict token closure = 0
  improved class blockers = 5
  worsened class blockers = 3
  necessary blocker reducer = 2
```

按 source group 看：

```text
qwen3 strong_target_not_exact:
  23 / 32 已经 answer-class closure。

DS7B strong_target_not_exact:
  5 / 5 已经 answer-class closure。

GLM4:
  当前 confirm 集没有 strong_target_not_exact，可评价性仍弱。
```

## 5. 客观解释

Phase 854 修正了 Phase 853 的解释：

```text
Phase 853 的 exact-natural strong = 0
不等于目标类完全没有赢得首词元竞争。
```

更准确地说：

```text
1. strict canonical token 全部没有闭合；
2. 但 answer-class 首词元场经常已经闭合；
3. 失败主要转移到 alias set、format、span、rollout 和协议边界层。
```

qwen3 中未闭合 blocker 的典型 token 包括：

```text
Shapes
quadr
Circle
```

其中 Shapes / quadr 本身也可能是几何答案类的一部分，说明当前 answer-class alias set 仍偏保守。

DS7B 的未闭合 blocker 主要是 format_space，说明它的失败更像格式或空格首 token 竞争。

## 6. min-cut 结果

qwen3：

```text
edge rows = 157
necessary_blocker_reducer = 10
single_sufficient_partial_reducer = 2
candidate_harmful_or_antagonistic = 22
weak_or_no_min_cut_effect = 123
```

DS7B：

```text
edge rows = 50
necessary_blocker_reducer = 2
single_sufficient_partial_reducer = 7
candidate_harmful_or_antagonistic = 3
weak_or_no_min_cut_effect = 38
```

GLM4：

```text
edge rows = 31
necessary_blocker_reducer = 2
```

判断：

```text
leave-one-gear 能找到少量必要 blocker reducer，
但比例很低，说明 min-cut 候选仍稀疏。
当前不能说已经找到完整因果路径。
```

## 7. 理论进展

Phase 854 的关键进展不是“闭合成功”，而是把闭合失败拆开：

```text
strong-edge
  ->
answer-class first-token closure
  ->
strict token closure
  ->
span closure
  ->
rollout closure
```

当前结果说明：

```text
全局齿轮图谱已经能解释一部分答案类首词元场重排；
但还不能解释 canonical token、别名选择、格式协议和后续自然展开。
```

因此，Phase 853 的结论应改写为：

```text
strong-edge 不等于 exact-natural closure；
但 strong-edge 中有相当一部分已经达到 answer-class blocker closure。
```

## 8. 当前硬伤

```text
1. strict token closure 三模型仍全部为 0。
2. answer-class alias set 仍不完整，Shapes / quadr 等可能被误算为 blocker。
3. full combo 有时反而 worsens blockers，qwen3 60 条中 18 条 blocker 变差。
4. min-cut necessary reducer 数量很少，且部分 candidate 会产生 harmful / antagonistic effect。
5. GLM4 当前源行太少，仍然不能判断其真实机制。
6. DS7B 的 answer-class closure 可能是小模型捷径，不能直接外推到大模型。
7. 本阶段仍是 first-step blocker audit，没有做 span / rollout closure。
```

## 9. 下一阶段

下一阶段仍属于全局齿轮图谱阶段，但目标应继续从 first-token 推到 rollout：

```text
Phase 855:
Answer-Class Alias Field and Rollout Closure Validation
```

核心任务：

```text
1. 扩展答案类别名集合，加入 Geometry / Shapes / quadrilateral / polygon / shape 等真实生成别名。
2. 验证 answer-class closure 是否能预测 span closure。
3. 对 qwen3 的 10 条 necessary blocker reducer 做更多对象和 prompt 的复测。
4. 对 blocker worsened 的组合单独建 harmful gear atlas。
5. GLM4 需要更换或增加任务触发路线，避免在 geometry route 上继续低效堆组合。
6. DS7B 需要用更多对照区分 answer-class 捷径和真实路线闭合。
```

# Phase 855：答案类别名场与短 Rollout 闭合验证

时间：2026-07-02 17:38

## 1. 本阶段任务

Phase 854 证明：

```text
strict token closure = 0
但 answer-class closure 大量存在。
```

Phase 855 继续验证：

```text
answer-class first-token closure
是否能预测短 greedy rollout 的答案类闭合？
```

新增脚本：

```text
tests/glm5/phase855_answer_class_alias_rollout_closure_validation.py
tests/glm5/run_phase855_answer_class_alias_rollout_closure_validation_round.sh
```

结果目录：

```text
tests/result/phase855_answer_class_alias_rollout_closure_validation/
```

仍按 qwen3 -> GLM4 -> DS7B 顺序执行，bf16，未使用量化，FlashAttention2 不可用时回退 sdpa。

## 2. 测试原理

Phase 855 从 Phase 854 full_combo rows 中选择：

```text
strong_target_class_closed
strong_target_class_open
strong_other
additive_control
```

对每行运行：

```text
original
full_combo
without_necessary
```

其中 without_necessary 来自 Phase 854 的 necessary_blocker_reducer。

本阶段扩展了几何答案类别名集合：

```text
geometric shape
geometry
geometric
shape
shapes
polygon / polygons
quadrilateral / quadrilaterals
round shape
closed curve
```

但仍优先把对象自身输出记为 object_echo，用于检测身份-类别纠缠。

## 3. 新增公式

短 rollout 答案类闭合：

```text
C_alias-rollout(x)
  =
1[
  Rollout(x) in AliasSpan(A(y))
]
```

身份-类别分离：

```text
C_id-class-sep(x)
  =
1[
  Rollout(x)
  notin
  I(o) \\ A(y)
]
```

其中：

```text
A(y):
  答案类别名集合。

I(o):
  对象身份词集合。
```

当对象词和类别词重叠时，例如：

```text
object = polygon
answer-class alias 也包含 polygon
```

不能简单判定成功或失败，必须进入 identity-class overlap audit。

## 4. confirm 轮结果

```text
qwen3:
  sources = 60
  full_combo first-token answer-class closure = 57
  full_combo rollout answer-class = 57
  strict rollout = 0
  object echo = 3
  first-token -> rollout F1 = 1.0000

GLM4:
  sources = 13
  full_combo first-token answer-class closure = 12
  full_combo rollout answer-class = 12
  strict rollout = 7
  object echo = 0
  first-token -> rollout F1 = 1.0000

DS7B:
  sources = 20
  full_combo first-token answer-class closure = 17
  full_combo rollout answer-class = 5
  strict rollout = 0
  object echo = 12
  first-token -> rollout F1 = 0.4545
```

## 5. qwen3 结果

qwen3 full_combo：

```text
rollout labels:
  answer_alias = 57
  object_echo = 3

mean class blocker count = 0.05
mean class rank = 1.05
```

qwen3 strong_target_not_exact：

```text
32 / 32 rollout answer-class
```

典型输出：

```text
Shape
Geometry
Shapes
polygon
Polygon
quadrilateral
Quadrilateral
```

判断：

```text
在当前 geometry route 上，
qwen3 的 expanded answer-class first-token closure
几乎可以预测短 rollout answer-class closure。
```

但注意：

```text
strict canonical rollout = 0
```

所以这仍不是 exact-natural closure，而是答案类别名闭合。

## 6. GLM4 结果

GLM4 full_combo：

```text
rollout answer-class = 12 / 13
strict canonical rollout = 7 / 13
```

典型输出：

```text
Geometric Shape
Shape
A perfect circle
```

判断：

```text
GLM4 在当前 full_combo 样本中 rollout 质量不弱，
但由于 Phase 853 / 854 中 strong-edge 触发样本太少，
仍不能说明 GLM4 的 strong-edge gate 已经定位。
```

## 7. DS7B 结果

DS7B full_combo：

```text
first-token answer-class closure = 17 / 20
rollout answer-class = 5 / 20
object_echo = 12 / 20
```

典型输出：

```text
shape
Polygon
polygon
1
```

关键问题：

```text
DS7B 首词元场经常进入答案类 token，
但短 rollout 经常变成 object_echo。
```

其中 polygon 样本最麻烦：

```text
object = polygon
answer-class alias 也包含 polygon
```

所以 DS7B 暴露出的不是单纯失败，而是：

```text
object identity 与 answer class alias 重叠。
```

这说明小模型中身份路线和类别路线可能更粗糙、更纠缠。

## 8. without_necessary 结果

qwen3 without_necessary：

```text
n = 10
first-token answer-class = 1
rollout answer-class = 1
other = 9
```

说明 Phase 854 的 necessary blocker reducer 在 qwen3 中不仅影响 first-token blocker，也会影响短 rollout。

GLM4 without_necessary：

```text
n = 1
rollout answer-class = 0
```

DS7B without_necessary：

```text
n = 2
rollout answer-class = 0
```

但样本太少，不能外推。

## 9. 理论进展

Phase 855 把闭合层级推进为：

```text
strong-edge
  ->
answer-class first-token closure
  ->
answer-class rollout closure
  ->
strict canonical rollout
  ->
identity-class separation
  ->
cross-domain rollout closure
```

当前最重要的新结论是：

```text
qwen3:
  first-token answer-class closure 基本预测短 rollout answer-class closure。

DS7B:
  first-token answer-class closure 不能预测短 rollout，
  因为对象身份复读大量出现。
```

这说明：

```text
语言编码机制不是一个统一 token 闭合问题，
而是答案类、对象身份、别名、格式、rollout 多层路线的对齐问题。
```

## 10. 当前硬伤

```text
1. Phase 855 仍是几何局部路线，不是跨语义域结论。
2. qwen3 strict canonical rollout 仍为 0。
3. DS7B 出现严重 object_echo / answer-class alias 重叠。
4. GLM4 strong-edge 触发不足，不能因为 rollout 质量好就认为机制已定位。
5. without_necessary 样本数量少，只能说明局部必要性，不能证明完整路径。
6. 分类器仍是规则型 alias 判定，不等于真正语义等价判定。
```

## 11. 下一阶段

下一阶段仍属于全局齿轮图谱，但需要转入身份-类别重叠审计：

```text
Phase 856:
Identity-Class Overlap and Cross-Domain Rollout Audit
```

任务：

```text
1. 对 polygon / circle / triangle 等对象身份词和类别词重叠样本单独建表。
2. 构造 object != answer-alias 的几何样本，避免 polygon 这种重叠。
3. 扩展到 animal / tool / color / abstract，验证 qwen3 first-token -> rollout 关系是否跨域成立。
4. 对 DS7B 的 object_echo 路线做单独干预，判断是小模型捷径还是真实身份路线。
5. 对 qwen3 necessary blocker reducer 做跨 prompt / 跨对象复测。
```

## 12. Phase 856：身份-类别重叠与跨域 rollout 审计

时间：2026-07-02 19:37

本阶段完成了 Phase 855 提出的跨域审计，不做新的齿轮干预，只检查自然提示下：

```text
first-token answer-class field
  ->
short rollout answer-class field
```

是否在多个语义域中保持一致。

测试脚本：

```text
tests/glm5/phase856_identity_class_overlap_cross_domain_rollout_audit.py
tests/glm5/run_phase856_identity_class_overlap_cross_domain_rollout_audit_round.sh
```

结果目录：

```text
tests/result/phase856_identity_class_overlap_cross_domain_rollout_audit/
```

确认轮语义域：

```text
geometry
animal
tool
color
material
abstract
plant
object
```

确认轮 prompt 类型：

```text
natural_question
natural_category
object_only
```

### 12.1 跨模型结果

```text
qwen3:
  rows = 114
  first-token answer-class = 41
  rollout answer-class = 41
  clear rollout = 37
  object_echo = 10
  first->rollout F1 = 0.9756

GLM4:
  rows = 114
  first-token answer-class = 45
  rollout answer-class = 46
  clear rollout = 38
  object_echo = 5
  first->rollout F1 = 0.9670

DS7B:
  rows = 114
  first-token answer-class = 32
  rollout answer-class = 34
  clear rollout = 24
  object_echo = 13
  first->rollout F1 = 0.9697
```

这说明：

```text
1. first-token answer-class closure 是短 rollout answer-class closure 的强诊断指标。
2. Phase 855 中 DS7B 的严重断裂不能外推为全域自然提示断裂。
3. DS7B 仍有最强 object_echo 倾向，说明身份路线竞争仍是重要问题。
4. clear rollout 明显低于 answer-class rollout，说明身份-类别重叠仍会抬高表面成功率。
```

### 12.2 Prompt gate 结果

qwen3：

```text
natural_category:
  first = 22
  rollout = 22
  clear = 19
  F1 = 1.0

natural_question:
  first = 19
  rollout = 18
  clear = 17
  F1 = 0.9730

object_only:
  first = 0
  rollout = 1
  echo = 6
```

GLM4：

```text
natural_category:
  F1 = 1.0

natural_question:
  F1 = 0.9744

object_only:
  first = 0
  rollout = 2
```

DS7B：

```text
natural_category:
  F1 = 1.0

natural_question:
  F1 = 0.9

object_only:
  first = 10
  rollout = 10
  object_echo = 9
```

关键结论：

```text
prompt 本身就是 route gate。
natural_question / natural_category 更容易打开 answer-class route；
object_only 更容易激活 identity echo route。
```

### 12.3 对 Phase 855 的修正

Phase 855 的判断需要从：

```text
DS7B first-token closure 不能预测 rollout closure
```

收紧为：

```text
DS7B 在几何 full-combo 和 identity-class overlap 条件下出现明显 first-token / rollout 断裂；
但在跨域自然提示中，first-token answer-class 仍能较好预测短 rollout answer-class。
```

因此，当前更准确的图谱是：

```text
answer-class route
  在 natural prompt gate 下较稳定；

identity echo route
  在 object_only / 抽象 / 材料 / 几何对象复述中更强；

clear rollout boundary
  是 answer-class rollout 之后必须增加的严格边界。
```

### 12.4 改进后的闭合层级

```text
C_strict-token
  原始 canonical token 是否胜出

C_answer-class
  答案类别 token 集是否胜出

C_alias-rollout
  短 rollout 是否进入答案类别名片段

C_clear-rollout
  短 rollout 是否进入答案类别名片段，同时排除对象身份复读

C_prompt-gated
  prompt 是否打开了正确 answer-class route
```

当前仍未闭合：

```text
1. strict canonical token closure
2. long rollout / span closure
3. prompt gate 的因果齿轮定位
4. DS7B object_echo route 的因果来源
5. 跨模型同构齿轮图谱
```

### 12.5 Phase 857 任务

下一阶段应从自然审计回到因果齿轮图谱：

```text
Phase 857:
Prompt-Gated Causal Gear Rollout Validation
```

核心任务：

```text
1. 对同一齿轮组合分别在 natural_question / natural_category / object_only 下测试。
2. 检查 qwen3 necessary blocker reducer 是否跨 prompt、跨域仍必要。
3. 对 DS7B object_echo route 做显式抑制 / 替换。
4. 把 answer-class、clear-rollout、object_echo 三类边界分开计分。
5. 只有跨 prompt gate 后仍有效的边，才能升级为更高等级 causal gear edge。
```

## 13. Phase 857：提示门控因果齿轮展开验证

时间：2026-07-02 21:34

本阶段把 Phase 856 的自然提示审计接回 Phase 854 的因果齿轮组合。
它不是新搜索齿轮，而是重放已有：

```text
full_combo
without_necessary
```

在三类 prompt gate 下观察边界变化：

```text
natural_question
natural_category
object_only
```

测试脚本：

```text
tests/glm5/phase857_prompt_gated_causal_gear_rollout_validation.py
tests/glm5/run_phase857_prompt_gated_causal_gear_rollout_validation_round.sh
```

结果目录：

```text
tests/result/phase857_prompt_gated_causal_gear_rollout_validation/
```

### 13.1 confirm 轮结果

```text
qwen3:
  sources = 10
  rows = 90
  full_combo vs original:
    clear rollout gain = 15
    clear rollout loss = 0
  without_necessary vs full_combo:
    clear rollout loss = 15

GLM4:
  sources = 10
  rows = 63
  full_combo vs original:
    clear rollout gain = 0
    clear rollout loss = 0
    mean blocker reduction = 1.7333
    mean class-object margin gain = 0.5010
  without_necessary vs full_combo:
    clear rollout loss = 0

DS7B:
  sources = 10
  rows = 66
  full_combo vs original:
    clear rollout gain = 9
    clear rollout loss = 0
  without_necessary vs full_combo:
    clear rollout loss = 3
```

客观结论：

```text
1. qwen3 的部分 necessary blocker reducer 跨 natural_question / natural_category 有明确必要性。
2. DS7B 的部分几何齿轮也能推动 clear rollout，但必要性弱于 qwen3。
3. GLM4 主要改变 blocker field 和 margin，没有转化成 rollout 边界变化。
4. object_only 基本没有被这些几何齿轮推入 clear rollout，说明 prompt gate 是硬边界。
```

### 13.2 transfer 轮结果

transfer 轮把同一批 Phase 854 几何齿轮迁移到：

```text
geometry
animal
tool
color
material
abstract
```

每域 1 个目标样本，使用：

```text
natural_question
natural_category
```

结果：

```text
qwen3:
  sources = 2
  rows = 72
  full_combo vs original:
    clear rollout gain = 1
    clear rollout loss = 0

GLM4:
  sources = 2
  rows = 60
  full_combo vs original:
    clear rollout gain = 0
    clear rollout loss = 0

DS7B:
  sources = 2
  rows = 72
  full_combo vs original:
    clear rollout gain = 0
    clear rollout loss = 0
```

这说明：

```text
几何齿轮不是自动通用语言齿轮。
```

它们可以跨 prompt gate 起作用，但跨语义域迁移很弱。

### 13.3 阶段性图谱修正

Phase 857 后，当前图谱形状应写为：

```text
local domain gear
  ->
prompt-gated causal effect
  ->
answer-class / clear-rollout boundary
  ->
domain-transfer test
  ->
cross-domain isomorphism audit
```

也就是说，不能再把：

```text
geometry gear works
```

直接推成：

```text
language gear found
```

更严格的判断应是：

```text
geometry 内部已经出现 prompt-gated causal gear；
但 cross-domain universal gear 尚未证明。
```

### 13.4 下一阶段

下一阶段应进入：

```text
Phase 858:
Cross-Domain Independent Gear Discovery and Isomorphism Audit
```

核心任务：

```text
1. 不再只迁移几何齿轮，而是在 animal / tool / color / material / abstract 中独立发现齿轮。
2. 每个域都报告 answer-class、clear-rollout、object_echo、blocker field。
3. 比较不同域的齿轮是否同层、同方向、同响应指纹。
4. 找跨域同构齿轮，而不是假设几何齿轮自然通用。
5. 对 DS7B object_echo route 做域内干预。
```

## 14. Phase 858：跨域独立齿轮发现与同构审计

### 14.1 阶段定位

Phase 858 接续 Phase 857 的 transfer 负结果。

Phase 857 证明：

```text
几何域内的 prompt-gated causal gear edge 存在；
但几何齿轮跨语义域迁移很弱。
```

因此 Phase 858 不再继续迁移几何齿轮，而是在每个语义域内部独立发现候选齿轮，再检查它们是否同构。

这一步属于全局齿轮图谱阶段，不是 token closure 阶段。

### 14.2 测试原理

本阶段对每个语义域、对象和 prompt gate，在后期 MLP 层中扫描 class-vs-object 读出支撑：

```text
support =
  MLP down input activation
  *
  class-vs-object readout coefficient
```

公式为：

```text
s_{d,l,c}(x,p)
  =
  a_{l,c}(x,p)
  *
  [ (W_U[t_d(x)] - W_U[o(x)]) W_down_l ]_c
```

然后按支撑方向选出：

```text
negative blocker gears
positive supporter gears
```

并用：

```text
zero
flip
```

进行因果验证，观察：

```text
first-token answer-class
clear rollout
object echo
blocker count
class-object margin
```

### 14.3 三轮测试设置

smoke 轮：

```text
domains = geometry / animal / tool
cases = 每域 1 个
prompts = natural_question
```

main 轮：

```text
domains = geometry / animal / tool / color / material / abstract
cases = 每域 1 个
prompts = natural_question / natural_category
```

confirm 轮：

```text
domains = geometry / animal / tool / color / material / abstract / plant / object
cases = 每域 2 个
prompts = natural_question / natural_category
```

三个模型依次测试：

```text
qwen3 -> GLM4 -> DS7B
```

未使用量化。FlashAttention2 环境不可用，脚本自动回退到 SDPA。

### 14.4 confirm 轮客观结果

| 模型 | 域数 | 候选齿轮 | 验证行数 | 有正向分数的域 | clear gain 域 | exact shared best gear | shared best layer |
|---|---:|---:|---:|---:|---:|---:|---:|
| qwen3 | 8 | 14120 | 288 | 8 | 2 | 0 | 4 |
| GLM4 | 8 | 13931 | 288 | 7 | 3 | 1 | 3 |
| DS7B | 8 | 5258 | 288 | 6 | 4 | 0 | 3 |

代表性域内结果：

```text
qwen3:
  animal:
    L27C811 + L32C5164 flip
    clear rollout gain = 3 / 4
  geometry:
    L29C1532 zero
    clear rollout gain = 2 / 4

GLM4:
  material:
    L32C8466 flip
    clear rollout gain = 2 / 8
  plant:
    L32C8466 + L26C1162 flip
    clear rollout gain = 2 / 4

DS7B:
  animal:
    L27C16651 + L24C3875 flip
    clear rollout gain = 4 / 4
  color:
    L27C15369 + L26C8587 flip
    clear rollout gain = 1 / 4
  material:
    L26C11106 flip
    clear rollout gain = 1 / 4
  tool:
    L27C15841 flip
    clear rollout gain = 1 / 4
```

### 14.5 同构审计结果

exact shared best gear：

```text
qwen3:
  无

GLM4:
  L32C8466 shared by material / plant

DS7B:
  无
```

shared best layer：

```text
qwen3:
  L32: animal / color
  L29: color / geometry / tool
  L30: abstract / object / plant
  L31: material / object / tool

GLM4:
  L32: geometry / material / plant / tool
  L29: abstract / animal / object
  L30: color / object

DS7B:
  L27: abstract / animal / color / plant / tool
  L24: animal / geometry / object
  L26: color / material
```

客观解释：

```text
1. 域内独立齿轮发现有效，明显强于简单几何齿轮迁移。
2. exact channel 级同构很弱，只有 GLM4 出现一个 material / plant 共享 best gear。
3. layer 级同构更明显，说明不同语义域可能复用相似层段，但不一定复用同一通道。
4. 当前还不能说找到 universal language gear。
```

### 14.6 图谱形状修正

Phase 858 后，全局齿轮图谱应从：

```text
geometry gear transfer test
```

升级为：

```text
domain-local gear discovery
  ->
domain-local causal edge
  ->
layer-level isomorphism
  ->
exact-channel isomorphism candidate
  ->
cross-domain closure test
```

现在已经出现：

```text
domain-local causal edge
```

但还没有完成：

```text
cross-domain exact-channel isomorphism closure
```

### 14.7 当前硬伤

```text
1. 读出支撑公式只能给候选，不等于真实因果角色。
2. positive supporter zero 后也可能产生 clear gain，说明符号标签需要二次因果校准。
3. 每域 confirm 只有 2 个对象，仍不足以证明 domain-level invariant。
4. exact shared gear 极少，跨域通用齿轮尚未成立。
5. shared layer 可能是模型架构或小模型压缩造成的粗糙形状，不一定是语言机制不变量。
6. 当前只干预第一步生成的齿轮，仍不是完整多 token 闭合。
```

### 14.8 下一阶段

下一阶段仍属于全局齿轮图谱阶段，应进入：

```text
Phase 859:
Domain Gear Holdout and Sign Calibration Audit
```

核心任务：

```text
1. 对 Phase 858 的 top domain gears 做 holdout 对象验证。
2. 把 support sign 和 intervention sign 分开校准。
3. 检查 L32C8466 这类 exact shared gear 是否真能跨 material / plant 外推。
4. 对 qwen3 / DS7B 的 layer-level shared shape 做同层不同通道对照。
5. 明确区分 domain-local causal edge、layer shape isomorphism、exact gear isomorphism 三个证据等级。
```

## 15. Phase 859：域内齿轮 holdout 与符号校准

### 15.1 阶段定位

Phase 859 是 Phase 858 的直接校准阶段。

Phase 858 已经发现：

```text
域内独立齿轮发现有效；
但 exact-channel cross-domain isomorphism 稀疏。
```

Phase 859 不重新搜索齿轮，而是读取 Phase 858 confirm 的每域 top gear，在未见对象上验证：

```text
1. top gear 是否能跨对象保持作用；
2. alternate mode 是否也能产生作用；
3. same-layer control 是否能解释效果；
4. shared exact gear 是否能跨域外推。
```

### 15.2 测试设置

holdout 轮使用 Phase 858 confirm 没有使用过的对象。

条件为：

```text
original
best_holdout
alternate_mode
same_layer_control
shared_exact_probe
```

其中：

```text
best_holdout:
  Phase 858 每域最佳齿轮和最佳干预模式。

alternate_mode:
  同一齿轮但 zero / flip 互换。

same_layer_control:
  同层其他高支撑通道，用于检查是否只是层级效应。

shared_exact_probe:
  Phase 858 中跨域共享的 exact gear，在所有 holdout 域上测试。
```

### 15.3 holdout 轮结果

| 模型 | rows | best 正向域 | best clear 域 | alternate clear 域 | control clear 域 | shared probe clear 域 |
|---|---:|---:|---:|---:|---:|---:|
| qwen3 | 128 | 6 | 2 | 1 | 0 | 0 |
| GLM4 | 160 | 7 | 1 | 1 | 0 | 0 |
| DS7B | 128 | 6 | 2 | 2 | 0 | 0 |

代表性 holdout 正结果：

```text
qwen3:
  material:
    L31C4800 + L31C2257 flip
    clear rollout gain = 1 / 4

  geometry:
    L29C1532 zero
    clear rollout gain = 1 / 4

GLM4:
  color:
    L30C7088 + L30C11128 flip
    clear rollout gain = 1 / 4

DS7B:
  color:
    L27C15369 + L26C8587 flip
    clear rollout gain = 2 / 4

  animal:
    L27C16651 + L24C3875 flip
    clear rollout gain = 1 / 4
```

### 15.4 关键收紧

```text
1. Phase 858 的域内 top gears 有一部分能跨对象保持作用。
2. same-layer control 全部没有 clear gain。
3. 这说明“同层形状”不是充分解释，具体通道仍然重要。
4. shared exact probe 没有 clear gain。
5. GLM4 L32C8466 不能从 Phase 858 的 material / plant 共享候选直接升级为稳定跨域不变量。
6. alternate mode 在 qwen3 / GLM4 / DS7B 都有少量 clear gain，说明 readout sign 和 intervention sign 需要分开校准。
```

### 15.5 符号校准公式

```text
Calib(g,d)
  =
  (
    sign_readout(g,d),
    sign_intervene(g,d),
    Delta C_holdout(g,d)
  )
```

更严格的 domain-local holdout causal gear 标准为：

```text
Delta C_train(g,d) > 0
and
Delta C_holdout(g,d) > 0
and
Delta C_control(L(g),d) <= 0
```

含义：

```text
训练样本有效；
holdout 对象仍有效；
同层控制无效；
才可以认为该齿轮不是简单层级伪影。
```

### 15.6 图谱证据等级更新

Phase 859 后，全局齿轮图谱的证据等级应写为：

```text
Level 1:
  readout-support candidate

Level 2:
  domain-local causal edge

Level 3:
  domain-local holdout causal gear

Level 4:
  layer-shape isomorphism

Level 5:
  exact-channel isomorphism candidate

Level 6:
  cross-domain holdout invariant
```

当前已经达到：

```text
少量 Level 3；
较多 Level 4；
很少 Level 5；
尚未达到 Level 6。
```

### 15.7 下一阶段

下一阶段仍属于全局齿轮图谱阶段，但目标应从“继续扩大域”转为“建立可重复证据等级”：

```text
Phase 860:
Replicated Domain Gear Evidence Ladder
```

核心任务：

```text
1. 对 qwen3 material / geometry、GLM4 color、DS7B color / animal 做重复对象扩展。
2. 对 alternate mode 正结果做独立校准，判断是否存在 mode-independent gate。
3. 对 same-layer control 加大样本，确认它真的无效。
4. 把 Level 1-6 证据等级写入 atlas schema。
5. 只把通过 holdout + control 的齿轮放入高置信图谱。
```

## 16. Phase 860：重复域内齿轮证据阶梯

### 16.1 阶段定位

Phase 860 是 Phase 859 的重复验证阶段。

Phase 859 已经发现少量 holdout 正结果，但对象和 prompt 数仍然有限。

Phase 860 只选择 Phase 859 中已经出现 clear gain 的域内齿轮，不重新扩大候选池：

```text
qwen3:
  material
  geometry

GLM4:
  color

DS7B:
  color
  animal
```

本阶段目标是建立可重复证据等级，而不是继续寻找新齿轮。

### 16.2 测试设置

每个目标域使用最多 5 个基础对象，并扩展 prompt gate：

```text
natural_question
natural_category
classification
```

每个域测试：

```text
original
best_holdout
alternate_mode
same_layer_control
```

### 16.3 严格证据等级

```text
Level 4:
  replicated_domain_edge
  有 clear gain，且没有 clear loss。

Level 5:
  replicated_control_filtered_domain_gear
  clear gain >= 2
  clear loss = 0
  same-layer control clear gain = 0

Level 6:
  strong_domain_invariant_candidate
  Level 5
  multi-split support
  multi-prompt support
```

高置信图谱候选公式：

```text
G_high
  =
  {
    g:
      Delta C_rep(g,d) > 0,
      Delta C_loss(g,d) = 0,
      Delta C_control(L(g),d) = 0,
      H_split(g,d) >= 2,
      H_prompt(g,d) >= 2
  }
```

符号歧义标记：

```text
SignAmbiguous(g,d)
  =
  1[ Delta C_alternate(g,d) > 0 ]
```

### 16.4 replicate 轮结果

| 模型 | rows | 最高等级 | Level 4+ 域 | Level 5+ 域 | Level 6 域 | 符号歧义域 |
|---|---:|---:|---:|---:|---:|---:|
| qwen3 | 120 | 6 | 1 | 1 | 1 | 2 |
| GLM4 | 60 | 4 | 1 | 0 | 0 | 1 |
| DS7B | 120 | 6 | 2 | 2 | 2 | 2 |

详细结果：

```text
qwen3 material:
  gear = L31C4800 + L31C2257
  level = 6
  clear gain/loss = 5 / 0
  split hits = 3
  prompt hits = 3
  alternate clear = 2
  same-layer control clear = 0

qwen3 geometry:
  gear = L29C1532
  level = 3
  clear gain/loss = 3 / 1
  降档原因：存在 clear loss

GLM4 color:
  gear = L30C7088 + L30C11128
  level = 4
  clear gain/loss = 1 / 0
  未进入 Level 5

DS7B animal:
  gear = L27C16651 + L24C3875
  level = 6
  clear gain/loss = 10 / 0
  split hits = 3
  prompt hits = 3
  alternate clear = 6
  same-layer control clear = 0

DS7B color:
  gear = L27C15369 + L26C8587
  level = 6
  clear gain/loss = 5 / 0
  split hits = 2
  prompt hits = 3
  alternate clear = 5
  same-layer control clear = 0
```

### 16.5 客观结论

```text
1. qwen3 material 已经从 holdout 正结果推进到 Level 6 域内强不变量候选。
2. DS7B animal / color 也推进到 Level 6。
3. GLM4 color 只有 Level 4，说明可复现但证据不足以进高置信图谱。
4. qwen3 geometry 因 clear loss 被降到 Level 3，不能进入高置信图谱。
5. 所有 Level 6 候选都有 alternate mode clear gain，说明齿轮有效，但干预符号解释未闭合。
```

### 16.6 当前图谱状态

现在可以把图谱分成两层：

```text
高置信域内图谱：
  qwen3 material: L31C4800 + L31C2257
  DS7B animal: L27C16651 + L24C3875
  DS7B color: L27C15369 + L26C8587

中置信候选图谱：
  GLM4 color: L30C7088 + L30C11128

低置信或降档候选：
  qwen3 geometry: L29C1532
```

这些仍然是：

```text
domain-local strong invariant candidates
```

不是：

```text
cross-domain universal gear
```

### 16.7 下一阶段

下一阶段仍属于全局齿轮图谱阶段，但应从“域内证据阶梯”进入“跨模型与跨域结构比较”：

```text
Phase 861:
High-Confidence Domain Gear Structure Comparison
```

核心任务：

```text
1. 只使用 Phase 860 Level 6 齿轮。
2. 比较 qwen3 material 与 DS7B animal/color 的层级位置、符号歧义、prompt gate 响应。
3. 检查这些高置信域内齿轮是否共享更抽象的结构指纹，而不是共享通道编号。
4. 单独解释 alternate mode 为什么也有效。
5. 把高置信图谱与普通候选图谱分开存储。
```

## 17. Phase 861：高置信域内齿轮结构指纹比较

### 17.1 任务

Phase 861 不重新加载模型，也不做新的 patch，而是读取 Phase 860 的 replicate 结果，只比较 Level 6 高置信齿轮的结构指纹。

测试脚本：

```text
tests/glm5/phase861_high_confidence_domain_gear_structure_comparison.py
```

结果目录：

```text
tests/result/phase861_high_confidence_domain_gear_structure_comparison/
```

### 17.2 指纹定义

对一个高置信齿轮集合：

```text
g = {L_i C_j}
```

记录以下结构指纹：

```text
Fingerprint(g,d)
  =
  (
    layer_band,
    gear_count,
    candidate_role,
    best_mode,
    split_vector,
    prompt_vector,
    control_gain,
    alternate_gain
  )
```

其中：

```text
layer_band:
  early / middle / late

split_vector:
  [
    phase858_seen_gain,
    phase859_holdout_seen_gain,
    new_replication_gain
  ]

prompt_vector:
  [
    natural_question_gain,
    natural_category_gain,
    classification_gain
  ]
```

### 17.3 结果

Phase 861 只比较 Phase 860 的 Level 6 齿轮，因此共有 3 个结构签名：

```text
DS7B animal:
  gear = L27C16651 + L24C3875
  depth = late, mean_norm_layer = 0.944
  role = negative_blocker
  best_mode = flip
  clear gain/loss = 10 / 0
  alternate clear gain = 6
  control clear gain = 0
  split vector = [6, 2, 2]
  prompt vector = [4, 2, 4]

DS7B color:
  gear = L27C15369 + L26C8587
  depth = late, mean_norm_layer = 0.981
  role = negative_blocker
  best_mode = flip
  clear gain/loss = 5 / 0
  alternate clear gain = 5
  control clear gain = 0
  split vector = [3, 2, 0]
  prompt vector = [1, 2, 2]

qwen3 material:
  gear = L31C4800 + L31C2257
  depth = late, mean_norm_layer = 0.886
  role = negative_blocker
  best_mode = flip
  clear gain/loss = 5 / 0
  alternate clear gain = 2
  control clear gain = 0
  split vector = [1, 1, 3]
  prompt vector = [2, 1, 2]
```

汇总结果：

```text
Level 6 signatures = 3
depth_band = late: 3 / 3
gear_count = two-channel: 3 / 3
candidate_role = negative_blocker: 3 / 3
best_mode = flip: 3 / 3
same-layer control clear gain = 0: 3 / 3
sign ambiguous = 3 / 3
new replication supported = 2 / 3
```

### 17.4 客观收紧

Phase 861 的最重要结果不是发现新齿轮，而是发现当前高置信域内齿轮共享一个结构形状：

```text
late-layer
+ two-channel
+ negative-blocker
+ flip-effective
+ same-layer-control-zero
```

这说明当前图谱已经从：

```text
单个通道的偶然 patch
```

推进到：

```text
少量可复现的域内齿轮结构指纹
```

但它仍然不能被解释为：

```text
跨域通用齿轮
```

原因：

```text
1. 高置信齿轮分布在不同模型和不同语义域。
2. 通道编号和语义域没有 exact isomorphism。
3. alternate zero mode 仍然有效，说明符号机制没有闭合。
4. DS7B color 没有 new replication clear gain，本轮新样本泛化弱于 DS7B animal 和 qwen3 material。
```

### 17.5 对全局齿轮图谱的改进

Phase 861 后，全局图谱应分成三层：

```text
第一层：高置信结构指纹层
  late-layer two-channel negative-blocker flip-effective gears

第二层：域内齿轮实例层
  qwen3 material
  DS7B animal
  DS7B color

第三层：待解释符号层
  zero / flip 都有效的 sign ambiguity
```

当前最值得继续追踪的不是更多普通候选，而是：

```text
为什么 late-layer two-channel negative-blocker 是当前最稳定的强齿轮形状？
```

下一阶段应进入：

```text
Phase 862:
Negative-Blocker Sign Mechanism Audit
```

核心任务：

```text
1. 只测试 Phase 861 的三个 Level 6 齿轮。
2. 拆开双通道组合，分别比较 zero / flip / scale-down / scale-up。
3. 记录 blocker token、answer token、format token 的全词表位移。
4. 判断 alternate mode 有效是因为同向削弱 blocker，还是因为小模型符号粗糙导致的等效扰动。
```

## 18. Phase 862：负向阻塞器符号机制审计

### 18.1 任务

Phase 862 只使用 Phase 861 的三个 Level 6 高置信齿轮，不重新搜索新齿轮：

```text
qwen3 material:
  L31C4800 + L31C2257

DS7B animal:
  L27C16651 + L24C3875

DS7B color:
  L27C15369 + L26C8587
```

测试脚本：

```text
tests/glm5/phase862_negative_blocker_sign_mechanism_audit.py
```

结果目录：

```text
tests/result/phase862_negative_blocker_sign_mechanism_audit/
```

### 18.2 测试条件

主测试：

```text
round = main
models = qwen3, GLM4, DS7B
prompt variants = natural_question, natural_category, classification
cases per domain = 5
edit modes = zero, flip, half, scale_up
single-channel decomposition = on
scale_up_factor = 2.0
```

确认测试：

```text
round = confirm_scale15
edit modes = scale_up
scale_up_factor = 1.5
single-channel decomposition = off
```

GLM4 因 Phase 861 没有 Level 6 齿轮，本阶段记录为：

```text
no_level6_target
```

### 18.3 主测试 full-set 结果

```text
qwen3 material:
  flip: clear gain/loss = 5 / 0, blocker reduction = 2.4000, answer delta = 0.7583
  half: clear gain/loss = 2 / 0, blocker reduction = 0.9333, answer delta = 0.1833
  zero: clear gain/loss = 2 / 0, blocker reduction = 1.7333, answer delta = 0.4083
  scale_up: clear gain/loss = 0 / 0, blocker reduction = -2.4667, answer delta = -0.4292

DS7B animal:
  flip: clear gain/loss = 10 / 0, blocker reduction = 1.2667, answer delta = 2.9750
  half: clear gain/loss = 5 / 0, blocker reduction = 0.6000, answer delta = 0.6292
  zero: clear gain/loss = 6 / 0, blocker reduction = 0.8667, answer delta = 1.3208
  scale_up: clear gain/loss = 0 / 3, blocker reduction = -2.5333, answer delta = -0.9854

DS7B color:
  flip: clear gain/loss = 5 / 0, blocker reduction = 1.1333, answer delta = 4.9250
  half: clear gain/loss = 2 / 0, blocker reduction = 0.6667, answer delta = 0.8833
  zero: clear gain/loss = 5 / 0, blocker reduction = 1.1333, answer delta = 1.9792
  scale_up: clear gain/loss = 0 / 2, blocker reduction = -2.0667, answer delta = -1.2542
```

### 18.4 scale-up 确认测试

scale_up_factor = 1.5 时：

```text
qwen3 material:
  clear gain/loss = 0 / 0
  blocker reduction = -1.3333
  answer delta = -0.2000

DS7B animal:
  clear gain/loss = 0 / 2
  blocker reduction = -1.4000
  answer delta = -0.5750

DS7B color:
  clear gain/loss = 0 / 1
  blocker reduction = -0.8000
  answer delta = -0.7250
```

这说明 scale_up 无效不是因为 2.0 扰动过强；较弱的 1.5 增强也没有产生 clear gain。

### 18.5 单通道拆解

```text
qwen3 material:
  single0: flip gain = 1, zero gain = 1
  single1: flip gain = 4, half gain = 2, zero gain = 2

DS7B animal:
  single0: flip gain = 9, half gain = 5, zero gain = 6
  single1: flip gain = 1, zero loss = 1

DS7B color:
  single0: flip gain = 5, half gain = 2, zero gain = 3
  single1: flip gain = 2, zero gain = 1
```

这说明两个通道不一定是对称协同。更准确的说法是：

```text
双通道组合中经常存在主通道 + 辅助通道。
```

### 18.6 机制判断

对于 qwen3 material 和 DS7B animal：

```text
zero / flip / half 都有 clear gain；
zero / flip / half 都削弱 blocker；
scale_up 没有 clear gain，并且会增加 blocker 或降低 answer。
```

因此更合理的解释是：

```text
zero 和 flip 都有效，不是因为符号方向完全相反也能工作；
而是因为它们都在削弱同一类 negative blocker。
```

对于 DS7B color：

```text
zero / flip / half 都有 clear gain；
但只有 flip 满足 blocker weakening 判据；
zero / half 更像 answer lift route。
```

因此 DS7B color 仍属于：

```text
mode-specific or unresolved
```

### 18.7 图谱修正

Phase 862 后，Level 6 齿轮的符号歧义应拆成两类：

```text
Type A:
  shared blocker weakening
  qwen3 material
  DS7B animal

Type B:
  answer-lift / unresolved mixed route
  DS7B color
```

因此全局齿轮图谱中不能只标记：

```text
SignAmbiguous = 1
```

还应增加：

```text
SignAmbiguityType(g,d)
  =
  shared_blocker_weakening
  or
  mode_specific_or_unresolved
```

### 18.8 当前结论

```text
Phase 862 是实质推进：
  它部分解释了为什么 zero / flip 都有效。

但 Phase 862 仍不是闭合：
  因为 DS7B color 没有被 blocker weakening 完全解释；
  双通道组合内部存在主通道 / 辅助通道不对称；
  GLM4 没有 Level 6 目标；
  仍然没有跨域 universal gear。
```

下一阶段应进入：

```text
Phase 863:
Dominant-Channel and Auxiliary-Channel Role Split Audit
```

核心任务：

```text
1. 只测试 Phase 862 的三组 Level 6 齿轮。
2. 区分主通道和辅助通道。
3. 观察主通道是否负责 answer lift，辅助通道是否负责 blocker weakening。
4. 对 DS7B color 做重点拆解，解释为什么 zero / half 有 clear gain 但 blocker weakening 不明显。
```

## 19. Phase 863：主通道 / 辅助通道角色拆解

### 19.1 任务

Phase 863 不重新跑模型，而是读取 Phase 862 的单通道拆解结果，对 Level 6 双通道齿轮内部进行角色分离。

脚本：

```text
tests/glm5/phase863_dominant_auxiliary_channel_role_split_audit.py
```

结果目录：

```text
tests/result/phase863_dominant_auxiliary_channel_role_split_audit/
```

### 19.2 结果

```text
qwen3 material:
  full set gain/loss = 9 / 0
  dominant channel = L31C2257
  dominant gain/loss = 8 / 0
  gain share = 0.889
  role = dominant_answer_and_blocker_channel
  auxiliary channel = L31C4800
  auxiliary gain/loss = 2 / 0
  role = auxiliary_mixed_channel

DS7B animal:
  full set gain/loss = 21 / 3
  dominant channel = L27C16651
  dominant gain/loss = 20 / 2
  gain share = 0.952
  role = dominant_answer_and_blocker_channel
  auxiliary channel = L24C3875
  auxiliary gain/loss = 1 / 1
  role = auxiliary_mixed_channel

DS7B color:
  full set gain/loss = 12 / 2
  dominant channel = L27C15369
  dominant gain/loss = 10 / 2
  gain share = 0.833
  role = dominant_answer_lift_channel
  auxiliary channel = L26C8587
  auxiliary gain/loss = 3 / 1
  role = auxiliary_answer_lift_channel
```

### 19.3 客观收紧

Phase 863 证明：

```text
two-channel gear
```

不应直接理解为两个通道对称协同。

更准确的结构是：

```text
dominant channel
+
auxiliary channel
```

其中：

```text
qwen3 material:
  主通道同时承担 answer lift 和 blocker weakening。

DS7B animal:
  主通道同时承担 answer lift 和 blocker weakening。

DS7B color:
  主通道主要是 answer lift；
  blocker weakening 不是主解释。
```

### 19.4 对图谱的修正

Phase 861 的结构指纹：

```text
late-layer two-channel negative-blocker
```

需要修正为：

```text
late-layer two-channel gear
  =
  dominant channel
  +
  auxiliary channel
```

并且 dominant channel 还需要继续分型：

```text
dominant_answer_and_blocker_channel
dominant_answer_lift_channel
dominant_blocker_channel
```

当前分类：

```text
qwen3 material:
  dominant_answer_and_blocker_channel

DS7B animal:
  dominant_answer_and_blocker_channel

DS7B color:
  dominant_answer_lift_channel
```

### 19.5 问题

```text
1. DS7B animal full set 出现 clear loss = 3，单通道主通道也有 loss = 2，说明强齿轮并非无风险。
2. DS7B color 的 Level 6 更像 answer-lift 齿轮，而不是纯 negative-blocker 齿轮。
3. 辅助通道的作用仍然弱，可能是局部稳定器、格式扰动器，或小模型压缩伪影。
4. 当前还没有把 answer lift、blocker weakening、object echo、format token 四类位移完全分离。
```

下一阶段如果继续，应围绕：

```text
Phase 864:
Answer-Lift vs Blocker-Weakening Route Separation
```

核心目标：

```text
1. 对 dominant_answer_lift_channel 和 dominant_answer_and_blocker_channel 分开建模。
2. 重点解释 DS7B color 为什么主要通过 answer lift 而不是 blocker weakening 闭合。
3. 把 Level 6 齿轮从“负向阻塞器”粗标签改成更细的路线角色标签。
```

## 20. Phase 864：Answer-Lift 与 Blocker-Weakening 路线分离

### 20.1 任务

Phase 864 不重新加载模型，而是读取 Phase 862 / 863 的结果，把每个有效干预拆成：

```text
answer lift
blocker weakening
object echo side-effect
format side-effect
harmful / blocker amplifying
```

脚本：

```text
tests/glm5/phase864_answer_lift_vs_blocker_weakening_route_separation.py
```

结果目录：

```text
tests/result/phase864_answer_lift_vs_blocker_weakening_route_separation/
```

### 20.2 full-set 路线结果

```text
qwen3 material:
  flip: mixed_answer_lift_and_blocker_weakening, gain/loss = 5 / 0
  half: mixed_answer_lift_and_blocker_weakening, gain/loss = 2 / 0
  zero: mixed_answer_lift_and_blocker_weakening, gain/loss = 2 / 0
  scale_up: weak_or_unresolved, gain/loss = 0 / 0

DS7B animal:
  flip: mixed_answer_lift_and_blocker_weakening, gain/loss = 10 / 0
  half: mixed_answer_lift_and_blocker_weakening, gain/loss = 5 / 0
  zero: mixed_answer_lift_and_blocker_weakening, gain/loss = 6 / 0
  scale_up: harmful_or_blocker_amplifying, gain/loss = 0 / 3

DS7B color:
  flip: mixed_answer_blocker_with_object_side_effect, gain/loss = 5 / 0
  half: answer_lift_dominant, gain/loss = 2 / 0
  zero: answer_lift_with_object_echo_side_effect, gain/loss = 5 / 0
  scale_up: harmful_or_blocker_amplifying, gain/loss = 0 / 2
```

### 20.3 关键收紧

Phase 864 证明：

```text
qwen3 material 和 DS7B animal:
  主要是 answer lift + blocker weakening 混合路线。

DS7B color:
  主要是 answer lift，并伴随 object echo side-effect 风险。
```

也就是说，DS7B color 不应继续被标为纯：

```text
negative-blocker gear
```

而应标为：

```text
answer-lift dominant gear with object side-effect risk
```

## 21. Phase 865：路线纯度与副作用过滤

### 21.1 任务

Phase 865 继续使用 Phase 864 的离线结果，增加图谱净化层：

```text
clean_mixed_answer_blocker_route
clean_answer_lift_route
object_side_effect_risk
harmful_or_unstable
inactive_or_weak
```

脚本：

```text
tests/glm5/phase865_route_purity_and_side_effect_filter.py
```

结果目录：

```text
tests/result/phase865_route_purity_and_side_effect_filter/
```

### 21.2 净化结果

full-set 路线分类：

```text
clean_mixed_answer_blocker_route: 6
clean_answer_lift_route: 1
object_side_effect_risk: 2
harmful_or_unstable: 2
inactive_or_weak: 1
```

干净 full-set 路线：

```text
qwen3 material:
  flip: clean_mixed_answer_blocker_route
  half: clean_mixed_answer_blocker_route
  zero: clean_mixed_answer_blocker_route

DS7B animal:
  flip: clean_mixed_answer_blocker_route
  half: clean_mixed_answer_blocker_route
  zero: clean_mixed_answer_blocker_route

DS7B color:
  half: clean_answer_lift_route
```

DS7B color 的：

```text
flip
zero
```

被降为：

```text
object_side_effect_risk
```

因为 answer 上升同时伴随 object 方向上升。

### 21.3 对图谱的修正

Phase 865 后，高置信图谱不应只记录：

```text
Level 6 gear
```

还必须记录：

```text
RoutePurity(g,d,m)
```

当前可进入较干净机制图谱的是：

```text
qwen3 material:
  zero / flip / half

DS7B animal:
  zero / flip / half
```

DS7B color 只能进入较弱的：

```text
clean_answer_lift_route:
  half
```

其 zero / flip 暂时不能进入干净闭合图谱。

### 21.4 下一步

下一阶段应从“路线净化”进入：

```text
Phase 866:
Clean Route Predictive Equation Fitting
```

核心任务：

```text
1. 只使用 clean_mixed_answer_blocker_route。
2. 拟合 answer_delta、blocker_reduction、clear_gain 之间的最小经验方程。
3. 不再混入 DS7B color 的 object side-effect route。
4. 检查能否从图谱特征预测哪种 mode 会产生 clean gain。
```

## 22. Phase 866：干净路线最小预测判据

Phase 866 没有重新加载模型，而是读取 Phase 865 的路线纯度结果，做一个更收紧的问题：

```text
能否只用基础观测量，区分 clean_mixed_answer_blocker_route 与副作用路线？
```

### 22.1 测试原理

本阶段没有使用复杂统计模型，也没有训练新模型，只比较三条简单判据：

```text
answer_blocker_object_rule:
  answer_delta > 0
  blocker_reduction > 0
  original_blocker_delta < 0
  object_delta <= 0.25
  object_echo_induced = 0
  format_or_other_induced = 0

answer_blocker_only_rule:
  answer_delta > 0
  blocker_reduction > 0
  original_blocker_delta < 0

answer_only_object_rule:
  answer_delta > 0
  object_delta <= 0.25
  object_echo_induced = 0
  format_or_other_induced = 0
```

目标不是证明闭合，而是检查：

```text
answer lift
+ blocker weakening
+ object / echo / format side-effect filter
```

是否比单独 answer 或 blocker 更能解释当前干净路线。

### 22.2 结果

在 full_set、dominant_channel、full_and_dominant 三个范围内，结果为：

```text
answer_blocker_object_rule:
  full_set:
    TP=6, FP=0, FN=0, TN=6
    precision=1.000, recall=1.000, accuracy=1.000

  dominant_channel:
    TP=6, FP=0, FN=0, TN=6
    precision=1.000, recall=1.000, accuracy=1.000

  full_and_dominant:
    TP=12, FP=0, FN=0, TN=12
    precision=1.000, recall=1.000, accuracy=1.000
```

对照规则：

```text
answer_blocker_only_rule:
  full_and_dominant:
    TP=12, FP=2, FN=0, TN=10
    precision=0.857, recall=1.000, accuracy=0.917

answer_only_object_rule:
  full_and_dominant:
    TP=12, FP=2, FN=0, TN=10
    precision=0.857, recall=1.000, accuracy=0.917
```

### 22.3 当前最小经验公式

当前可把干净 mixed route 的样本内经验判据写成：

```text
CleanMixedRoute(g,d,m)
  =
  [answer_delta(g,d,m) > 0]
  and [blocker_reduction(g,d,m) > 0]
  and [original_blocker_delta(g,d,m) < 0]
  and [object_delta(g,d,m) <= 0.25]
  and [object_echo_induced(g,d,m) = 0]
  and [format_or_other_induced(g,d,m) = 0]
```

更简写为：

```text
CleanMixedRoute
  =
  AnswerLift
  and BlockerWeakening
  and NoObjectEchoSideEffect
  and NoFormatSideEffect
```

### 22.4 图谱修正

Phase 866 后，当前全局齿轮图谱不应只按：

```text
clear gain
```

纳入边，而应按：

```text
clean predictive edge
```

纳入核心图谱。

也就是说：

```text
G_clean
  =
  {
    (g,d,m):
      CleanMixedRoute(g,d,m)
  }
```

当前进入 clean mixed route 的对象仍是：

```text
qwen3 material:
  zero / flip / half

DS7B animal:
  zero / flip / half
```

DS7B color 的 half 属于 clean_answer_lift_route，不属于 clean_mixed_answer_blocker_route；zero / flip 因 object side-effect 仍不能进入核心闭合图谱。

### 22.5 边界

这个结果是正结果，但必须严格限定：

```text
1. 它是 Phase 862-865 数据上的样本内判据。
2. 它不是跨模型、跨任务、跨长程 rollout 的闭合公式。
3. object_delta <= 0.25 是当前阶段的经验阈值，不是数学不变量。
4. 当前只覆盖 clean mixed answer-blocker route，没有覆盖纯 answer-lift route。
```

下一阶段如果继续同一阶段目标，应进入：

```text
Phase 867:
Clean Route Holdout Prediction
```

核心要求：

```text
1. 使用新的 domain / object / prompt。
2. 不重新调阈值。
3. 检查 CleanMixedRoute 判据是否还能预测 clean gain。
4. 若失败，记录失败类型，而不是继续局部 patch。
```

## 23. Phase 867：Clean Route Holdout Prediction

Phase 867 按 Phase 866 的建议做了 holdout 验证：

```text
1. 不重新搜索齿轮。
2. 不重新调 object_delta 阈值。
3. 使用新的 object 和新的 prompt。
4. 依次测试 qwen3、GLM4、DS7B。
5. 使用 bf16，量化关闭，flash 优先，失败后回退 sdpa。
```

### 23.1 测试对象

输入来源：

```text
Phase 865 full-set route purity rows
```

qwen3：

```text
material:
  L31C4800 + L31C2257
  modes = zero / flip / half / scale_up
```

DS7B：

```text
animal:
  L27C16651 + L24C3875
  modes = zero / flip / half / scale_up

color:
  L27C15369 + L26C8587
  modes = zero / flip / half / scale_up
```

GLM4：

```text
no_phase865_candidates
```

### 23.2 关键结果

跨模型统计：

```text
overall:
  n = 12
  source_clean_count = 6
  holdout_clean_count = 2
  TP = 0
  FP = 6
  FN = 2
  TN = 4
  precision = 0.000
  recall = 0.000
  accuracy = 0.333
```

qwen3 material：

```text
source clean:
  zero / flip / half

holdout clean:
  none

source_to_holdout:
  TP=0, FP=3, FN=0, TN=1
  accuracy=0.250
```

DS7B animal：

```text
source clean:
  zero / flip / half

holdout clean:
  none
```

DS7B color：

```text
source clean:
  none for clean_mixed_answer_blocker_route

holdout clean:
  half
  zero
```

### 23.3 解释

Phase 867 是一个重要负结果：

```text
Phase 866 的样本内 clean route 判据不能直接迁移到新 object / prompt。
```

更具体地说：

```text
source clean gear
  不等于
holdout clean gear
```

同时，DS7B color 出现了反向现象：

```text
source 中被判为 clean_answer_lift_route 或 object_side_effect_risk 的路线，
在 holdout 中反而满足 clean_mixed_answer_blocker_route 判据。
```

这说明路线纯度不是齿轮的固定属性，而是条件化属性：

```text
RoutePurity(g,d,m)
  应升级为
RoutePurity(g,d,m | object, prompt, blocker field)
```

### 23.4 对图谱的修正

Phase 867 后，全局齿轮图谱必须增加迁移状态：

```text
TransferStatus(g,d,m)
  =
  {
    stable_clean,
    source_clean_failed,
    emergent_clean,
    stable_nonclean
  }
```

当前结果：

```text
stable_clean:
  0

source_clean_failed:
  6

emergent_clean:
  2

stable_nonclean:
  4
```

因此，当前不能把 Phase 866 公式当成闭合公式，只能把它看成：

```text
样本内路线净化公式
```

而不是：

```text
跨条件预测公式
```

## 24. Phase 868：条件化路线迁移失败分类

Phase 868 没有重新跑模型，只对 Phase 867 的结果做失败分类。

### 24.1 失败类型统计

```text
n_rows = 12

source_clean_failed = 6
stable_nonclean = 4
emergent_clean = 2
stable_clean = 0
```

失败原因统计：

```text
original_blocker_not_negative = 8
no_clear_gain = 6
answer_not_lifted = 3
blocker_not_reduced = 3
clear_loss = 2
format_or_other_side_effect = 2
```

### 24.2 关键观察

最主要失败点不是 object side-effect，而是：

```text
original_blocker_delta 不稳定。
```

也就是说，原来在 Phase 862-865 中被削弱的 blocker，在 holdout 的 object / prompt 条件下不再稳定变成负向。

这说明：

```text
blocker weakening route
  不是齿轮本身的固定方向；
  它依赖当前 blocker field。
```

### 24.3 新公式

Phase 868 后，图谱边应从：

```text
CleanMixedRoute(g,d,m)
```

升级为条件化版本：

```text
CleanMixedRoute(g,d,m | x)
  =
  AnswerLift(g,d,m | x)
  and BlockerWeakening(g,d,m | x)
  and NoSideEffect(g,d,m | x)
  and OutputGain(g,d,m | x)
```

其中：

```text
x = object + prompt + blocker field
```

更关键的是：

```text
BlockerWeakening(g,d,m | x)
  不能只看齿轮；
  必须看当前原始 blocker 的身份、数量、logit 位置和角色。
```

### 24.4 阶段结论

Phase 867 / 868 是实质推进，但不是闭合：

```text
正进展：
  证明路线纯度必须条件化；
  找到迁移失败的主要卡点是 original blocker direction；
  排除了“source clean gear 可直接迁移”的过强假设。

负结果：
  当前 clean route 判据没有 holdout 预测力；
  qwen3 material 和 DS7B animal 的 source clean 在新条件下全部失效；
  DS7B color 的 emergent clean 说明小模型路线边界更不稳定。
```

下一阶段应从“固定齿轮迁移”转向：

```text
Phase 869:
Blocker Field Conditional Gate Audit
```

核心任务：

```text
1. 不再只问 gear 是否 clean。
2. 先刻画原始 blocker field。
3. 检查 clean gain 是否由 blocker field 的初始形状决定。
4. 判断哪些 blocker field 允许 gear 产生 clean mixed route。
```

## 25. Phase 869：Blocker Field Conditional Gate Audit

Phase 869 没有重新跑模型，只读取 Phase 867 的 original rows 和 Phase 868 的迁移失败分类，开始把图谱对象从：

```text
gear
```

推进到：

```text
gear + blocker field
```

### 25.1 测试原理

对每个 model-domain 的原始竞争场统计：

```text
1. class_blocker_count
2. clear_class_blocker_count
3. class_minus_object_logit
4. object_rank
5. top1 blocker role
6. top10 blocker role distribution
7. transfer_status_counts
8. transfer_failure_reasons
```

这不是新干预，只是把 Phase 867 失败的初始场形状显式化。

### 25.2 结果

qwen3 material：

```text
mean_class_blocker_count = 58.33
mean_class_minus_object_logit = -1.70
mean_object_rank = 14.08

top10_role_per_context:
  format_punct = 2.92
  format_space = 2.33
  other_blocker = 1.17
  object_echo = 1.00
  protocol_word = 0.58

field_profile:
  high_blocker_count
  object_above_class
  format_pressure
  object_echo_pressure
  semantic_other_pressure

transfer:
  source_clean_failed = 3
  stable_nonclean = 1
```

DS7B animal：

```text
mean_class_blocker_count = 5.17
mean_class_minus_object_logit = 3.26
mean_object_rank = 38.67

top10_role_per_context:
  other_blocker = 1.08
  format_punct = 0.83
  protocol_word = 0.75
  format_space = 0.50
  object_echo = 0.17

field_profile:
  semantic_other_pressure

transfer:
  source_clean_failed = 3
  stable_nonclean = 1
```

DS7B color：

```text
mean_class_blocker_count = 17.50
mean_class_minus_object_logit = 1.94
mean_object_rank = 37.33

top10_role_per_context:
  format_punct = 1.50
  other_blocker = 1.42
  protocol_word = 0.67
  format_space = 0.58
  object_echo = 0.50

field_profile:
  format_pressure
  semantic_other_pressure

transfer:
  stable_nonclean = 2
  emergent_clean = 2
```

### 25.3 关键解释

Phase 869 说明三个域的失败不是同一种失败：

```text
qwen3 material:
  初始 blocker field 太重；
  class 被 object 压住；
  format / object / semantic pressure 同时存在。

DS7B animal:
  blocker 数量不高；
  但 original blocker direction 在 holdout 中不稳定。

DS7B color:
  source 中有 side-effect risk；
  holdout 中由于 blocker field 改变，反而出现 emergent clean。
```

因此下一版图谱的核心节点不是：

```text
Gear(g)
```

而是：

```text
Gear(g) under BlockerField(B_x)
```

### 25.4 阶段公式

当前条件化图谱边应写成：

```text
EdgeClean(g,d,m,x)
  =
  GearEffect(g,d,m,x)
  and FieldAdmissible(B_x)
```

其中：

```text
FieldAdmissible(B_x)
  =
  not TooManyBlockers(B_x)
  and not ObjectDominatesClass(B_x)
  and not FormatDominates(B_x)
  and ReducibleOriginalBlockers(B_x,g,m)
```

这个公式仍然是描述性框架，还不是闭合公式。

### 25.5 下一步

下一阶段应继续在同一方向上做最小判据：

```text
Phase 870:
Blocker Field Admissibility Rule
```

核心任务：

```text
1. 用 Phase 867-869 的数据建立 FieldAdmissible 的基础判据。
2. 不训练复杂模型。
3. 检查 high_blocker_count、object_above_class、format_pressure 是否能解释 source_clean_failed。
4. 检查 DS7B color emergent_clean 是否对应更可化解的 blocker field。
```

## 26. Phase 870：Blocker Field Admissibility Rule

Phase 870 把 Phase 867 的 original/intervened rows 展开到单样本级：

```text
context = model + domain + object + prompt + candidate mode
```

共形成：

```text
144 rows
```

### 26.1 最小规则

样本内最强规则为：

```text
field_strict_plus_effect_rule
  =
  field_base_admissible
  and no semantic_other_pressure
  and answer_delta > 0
  and blocker_reduction > 0
  and original_blocker_delta < 0
  and object_delta <= 0.25
  and no object_echo_induced
  and no format_or_other_induced
```

其中：

```text
field_base_admissible
  =
  blocker_count < 20
  and class_minus_object_logit >= 0
  and format_pressure < 3
```

### 26.2 样本内结果

在 Phase 867 holdout 数据内：

```text
source_predict_clean_mixed:
  TP=1, FP=71, FN=3, TN=69
  precision=0.014, recall=0.250, accuracy=0.486

phase866_pair_rule:
  TP=4, FP=16, FN=0, TN=124
  precision=0.200, recall=1.000, accuracy=0.889

field_plus_effect_rule:
  TP=4, FP=1, FN=0, TN=139
  precision=0.800, recall=1.000, accuracy=0.993

field_strict_plus_effect_rule:
  TP=4, FP=0, FN=0, TN=140
  precision=1.000, recall=1.000, accuracy=1.000
```

这说明：

```text
blocker field 确实大幅减少误报。
```

但这个结果仍是样本内规则，不能视为闭合。

### 26.3 真实 clean transition

Phase 870 中 4 个 clean transition：

```text
qwen3 material:
  rubber + holdout_kind_phrase + flip

DS7B color:
  purple + holdout_category_short + flip
  purple + holdout_category_short + half
  purple + holdout_category_short + zero
```

它们共同特征：

```text
field_tags = field_low_pressure
answer_delta > 0
blocker_reduction > 0
original_blocker_delta < 0
```

## 27. Phase 871：Field Admissibility External Validation

Phase 871 使用新的 object 和 prompt 做外部验证。

新对象：

```text
material:
  ceramic / leather / silk / concrete

animal:
  elephant / rabbit / shark / eagle

color:
  cyan / magenta / brown / gray
```

新 prompt：

```text
validation_direct
validation_question
validation_table
```

模型仍按顺序测试：

```text
qwen3 -> GLM4 -> DS7B
```

设置：

```text
bf16
quantization off
flash first, sdpa fallback
```

### 27.1 aggregate 结果

在 candidate aggregate 层：

```text
overall:
  n = 12
  TP = 0
  FP = 6
  FN = 0
  TN = 6
  precision = 0.000
  recall = 0.000
  accuracy = 0.500
  holdout_clean_count = 0
```

这说明：

```text
source clean 仍不能迁移；
validation 中没有 aggregate holdout clean。
```

### 27.2 单样本规则外部验证

把 Phase 870 的规则应用到 Phase 871 validation rows：

```text
phase866_pair_rule:
  TP=1, FP=11, FN=0, TN=132
  precision=0.083, recall=1.000, accuracy=0.924

field_plus_effect_rule:
  TP=1, FP=1, FN=0, TN=142
  precision=0.500, recall=1.000, accuracy=0.993

field_strict_plus_effect_rule:
  TP=1, FP=1, FN=0, TN=142
  precision=0.500, recall=1.000, accuracy=0.993
```

也就是说：

```text
FieldAdmissible 仍能显著降低误报：
  11 -> 1

但不能完全消除误报：
  precision 从样本内 1.000 降到 0.500。
```

### 27.3 关键负结果

validation 中真实 clean transition 只有 1 个：

```text
DS7B color:
  cyan + validation_direct + flip
```

误报为：

```text
qwen3 material:
  concrete + validation_direct + flip

field_tags:
  field_low_pressure

effect:
  answer_delta > 0
  blocker_reduction > 0
  original_blocker_delta < 0

but:
  other -> other
```

这说明：

```text
FieldAdmissible + GearEffect 仍不足以保证 OutputGain。
```

### 27.4 新边界

Phase 871 后，公式必须继续扩展：

```text
EdgeClean(g,d,m,x)
  =
  FieldAdmissible(B_x)
  and GearEffect(g,d,m,x)
  and OutputGateOpen(g,d,m,x)
```

其中：

```text
OutputGateOpen
```

是当前缺失项。

因此，FieldAdmissible 是重要进展，但不是闭合标准。

下一阶段不应继续调 blocker field 阈值，而应进入：

```text
Phase 872:
Output Gate / Readout Transition Audit
```

核心任务：

```text
1. 比较 qwen3 concrete 误报与 DS7B cyan 真阳性。
2. 二者都是 low-pressure field + positive gear effect。
3. 差别在于 output/readout 是否从 other 转入 strict_canonical。
4. 需要刻画 OutputGateOpen 的条件。
```

## 28. Phase 872：Output Gate / Readout Transition Audit

Phase 872 使用 Phase 867 holdout rows、Phase 871 validation rows 的既有全词表 logits 和 rollout 标签做离线审计，没有新增模型推理。

核心问题是：

```text
FieldAdmissible + GearEffect 之后，
到底是什么决定输出是否进入 answer class / strict canonical？
```

### 28.1 输出门的可观测判据

最小可观测输出门定义为：

```text
OutputGateRaw(g,d,m,x)
  =
  Top1Role(y_1) in {strict_target, answer_class}
  and
  Logit(best_target) > Logit(best_non_target)
```

也就是：

```text
目标 token 必须成为第一步全词表竞争的真实 top1，
并且严格压过最强非目标 token。
```

这和只看 rank=1 不同。Phase 871 的 qwen3 concrete 误报中：

```text
target rank = 1
blocker count = 0
但 top1 role = format_space
target 与 format token 近似打平
rollout 仍为 other
```

而 DS7B cyan 真阳性中：

```text
top1 role = strict_target
target margin vs non-target = 1.1875
rollout other -> strict_canonical
```

### 28.2 Phase 872 结果

在 Phase 871 validation rows 上：

```text
output_gate_raw_rule -> intervened_rollout_clear_answer_class:
  TP=53, FP=0, FN=0, TN=91
  precision=1.000, recall=1.000, accuracy=1.000

output_gate_raw_rule -> intervened_rollout_strict_canonical:
  TP=45, FP=8, FN=0, TN=91
  precision=0.849, recall=1.000, accuracy=0.944
```

在 Phase 867 holdout rows 上：

```text
output_gate_raw_rule -> intervened_rollout_clear_answer_class:
  TP=49, FP=0, FN=0, TN=95
  precision=1.000, recall=1.000, accuracy=1.000
```

这说明：

```text
OutputGateRaw 是非常强的 answer-class 输出状态判据。
```

但它不是 clean transition 判据：

```text
output_gate_raw_rule -> target_clean_transition:
  Phase 871 precision = 0.019
```

原因是很多样本已经处于 answer-class 输出状态，不是从 other 新转入 answer-class。

### 28.3 公式拆分

Phase 872 后，必须区分两个对象：

```text
OutputStateOpen(x')
  =
  target token 在干预后读出场中获胜

CleanCausalEdge(g,d,m,x)
  =
  干预导致输出从非目标状态转为目标状态，
  且没有引入对象回声 / 格式副作用。
```

因此不能再把 OutputGateOpen 直接等同于 EdgeClean。

更准确的分解为：

```text
OutputStateOpen(x')
  =
  TargetTop1(x')
  and TargetBeatsNonTarget(x')

CleanCausalEdge(g,d,m,x)
  =
  FieldAdmissible(B_x)
  and GearEffect(g,d,m,x)
  and OutputStateOpen(T_g(x))
  and StateTransition(x -> T_g(x))
  and NoSideEffect(x -> T_g(x))
```

其中：

```text
StateTransition
```

负责区分：

```text
本来就已经输出正确
vs
干预后真正从错误输出转为正确输出
```

## 29. Phase 873：Output Gate External Replication

Phase 873 使用新增对象和新增提示做外部复验。

新增对象：

```text
material:
  glass, steel, wool, clay, plastic, sand

animal:
  tiger, dolphin, sparrow, turtle, goat, bear

color:
  red, green, yellow, pink, silver, beige
```

新增提示：

```text
replication_direct
replication_sentence
replication_form
```

模型运行情况：

```text
qwen3:
  complete, rows=90, domain=material

GLM4:
  no_phase865_candidates

DS7B:
  complete, rows=180, domains=animal,color
```

### 29.1 聚合层结果

```text
overall source -> holdout clean:
  n=12
  TP=0, FP=6, FN=1, TN=5
  precision=0.000, recall=0.000, accuracy=0.417
  source_clean_count=6
  holdout_clean_count=1
```

这再次说明：

```text
source clean 不能直接迁移。
```

### 29.2 Phase 870 / 872 规则在 Phase 873 上的表现

Phase 870 场规则：

```text
field_strict_plus_effect_rule:
  target_clean_transition:
    TP=0, FP=0, FN=0, TN=216
```

也就是说本轮没有任何样本同时满足严格场准入和 clean gear effect。

但输出状态门仍然稳定：

```text
output_gate_raw_rule -> intervened_rollout_clear_answer_class:
  TP=68, FP=0, FN=0, TN=148
  precision=1.000, recall=1.000, accuracy=1.000
```

strict canonical 更难：

```text
output_gate_raw_rule -> intervened_rollout_strict_canonical:
  TP=44, FP=24, FN=0, TN=148
  precision=0.647, recall=1.000, accuracy=0.889
```

这说明：

```text
OutputGateRaw 稳定刻画 answer-class 输出状态，
但 strict canonical 还受格式 / 单复数 / 别名 / 协议因素影响。
```

### 29.3 新收紧

Phase 873 出现了两个输出转移：

```text
DS7B animal goat + replication_direct + flip:
  object_echo -> strict_canonical

DS7B color red + replication_direct + flip:
  other -> strict_canonical
```

它们都满足：

```text
top1 role = strict_target
target margin vs non-target > 0
```

但不满足严格 clean-route 条件。

因此最新结论是：

```text
OutputStateOpen 是读出状态层；
FieldAdmissible + GearEffect 是因果路线层；
StateTransition 是状态变化层。

三者必须分开建模。
```

下一阶段应进入：

```text
Phase 874:
State Transition Decomposition
```

核心任务：

```text
1. 区分 output state 和 output transition。
2. 区分 strict canonical 和 answer-class closure。
3. 找出为什么部分 strict 输出转移不满足 clean-route 条件。
4. 重新定义闭合标准，避免把“输出已经正确”误判为“因果边有效”。
```

## 30. Phase 874：State Transition Decomposition

Phase 874 没有重新跑模型，而是整合 Phase 872 的三批 rows：

```text
holdout_phase867
validation_phase871
replication_phase873
```

总样本：

```text
n = 504
```

目标是正式拆开三层：

```text
1. OutputStateOpen:
   干预后输出状态是否已经打开。

2. ObservedOutputTransition:
   原始 rollout 没有输出答案类，
   干预后读出场打开。

3. CleanCausalEdge:
   输出发生转移，
   且满足严格场准入 + clean gear effect + 无副作用。
```

### 30.1 公式

输出状态：

```text
OutputStateOpen(x')
  =
  TargetTop1(x')
  and TargetBeatsNonTarget(x')
```

观测输出转移：

```text
ObservedOutputTransition(x,x')
  =
  not RolloutClear(x)
  and OutputStateOpen(x')
```

干净因果边：

```text
CleanCausalEdge(g,d,m,x)
  =
  ObservedOutputTransition(x,T_g(x))
  and FieldAdmissible(B_x)
  and GearEffect(g,d,m,x)
  and NoSideEffect(x,T_g(x))
```

### 30.2 核心结果

输出状态层：

```text
intervened_output_state_open -> intervened_rollout_clear_answer_class
  TP=170, FP=0, FN=0, TN=334
  precision=1.000, recall=1.000, accuracy=1.000
```

输出转移层：

```text
observed_clear_transition_rule -> target_output_clean_transition
  TP=21, FP=0, FN=0, TN=483
  precision=1.000, recall=1.000, accuracy=1.000
```

干净因果边层：

```text
clean_causal_edge_rule -> target_clean_transition
  TP=5, FP=0, FN=0, TN=499
  precision=1.000, recall=1.000, accuracy=1.000
```

### 30.3 结构分布

```text
answer_class_stable_closed: 311
answer_class_stable_open:   149
answer_class_loss:          23
nonclean_output_transition: 16
clean_causal_transition:    5
```

这说明：

```text
输出状态打开很常见；
输出转移较少；
干净因果转移更少。
```

### 30.4 非干净输出转移

21 个 output transition 中：

```text
clean_causal_transition = 5
nonclean_output_transition = 16
```

非干净转移的主要原因：

```text
field_not_strict_admissible: 16
not_phase866_pair_rule:      16
original_blocker_not_reduced:16
semantic_other_pressure:     10
object_dominates/object_echo:6
```

观测转移形态：

```text
other -> strict_canonical:          10
object_echo -> strict_canonical:    6
format_or_empty -> strict_canonical:5
```

### 30.5 理论意义

Phase 874 最大的进展不是又找到一个局部齿轮，而是明确：

```text
OutputStateOpen
ObservedOutputTransition
CleanCausalEdge
```

是三个不同层级。

因此不能再用单一闭合指标判断机制是否破解。

更准确的闭合阶梯应为：

```text
Level 1:
  解释输出状态。

Level 2:
  解释输出转移。

Level 3:
  解释干净因果转移。

Level 4:
  解释非干净但有效的输出转移。

Level 5:
  在跨模型 / 跨域 / 跨提示上预测这些转移。
```

下一阶段应进入：

```text
Phase 875:
Nonclean Output Transition Route Audit
```

核心任务：

```text
1. 专门研究 16 个 nonclean_output_transition。
2. 区分 semantic-pressure transition、object-echo recovery、format recovery。
3. 判断这些转移是“脏路线”还是“另一类有效路线”。
4. 扩展当前 clean-only 图谱，避免只研究干净边而漏掉真实机制。
```
